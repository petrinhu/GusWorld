> ESTRUTURA CANÔNICA: este arquivo termina na última linha da TABELA UNIFICADA. É proibido criar segunda tabela neste arquivo e é proibida linha em branco dentro da tabela. Legenda, scoring, rastreio e listas auxiliares vivem em bullets no cabeçalho, nunca em tabela.

# TODO.md - Tabela de pendências do GusWorld

## Legenda e critérios

- Status possíveis: `⏳ Pendente` (inicial de tudo), `🔄 Em andamento`, `🔍 Pendente verificação` (implementado, aguardando teste ou auditoria), `✅ Concluído` (só depois de verificado), `🟡 Parcial` (feito em parte: uma fatia entregue e outra ainda não, dentro do mesmo item), `⛔ Bloqueado`, `❌ Reprovado`, `🚫 Cancelado`.
- `Estado Auditado` começa em `—` e só é preenchido por auditoria de C-level `fable` (L-10), nunca por auto-relato de implementer.
- A ordem das linhas, de cima para baixo, É a ordem de execução. Onda = nível topológico do grafo de pré-requisitos (0 a 20): item entra na onda seguinte à do seu pré-requisito mais tardio, e **nenhuma onda contém item que dependa de outro da mesma onda**. Onda `0` = decisões do líder (custam tempo dele, não WIP de engenharia) e trabalho sem nenhum pré-requisito, disponível no dia zero. Onda `—` = item bloqueado por terceiro ou por portão externo ao grafo (tag de versão), sem data; a causa está na Descrição.
- Teste unitário NÃO é item desta tabela: nasce com o código pelo TDD estrito da L-19 (vermelho, verde, refatorar). Só teste não unitário (`TST-*`) e auditoria (`AUD-*`) têm linha, sempre downstream do que cobrem — desde a reordenação de 25/08/2026, sempre em onda estritamente POSTERIOR à do que cobrem, nunca a mesma.
- D12 da lista bruta NÃO é item de onda própria: o contrato comando/evento (L-17) é CRITÉRIO DE ACEITE de cada sistema D3 a D11, cobrado na revisão adversarial e na re-verificação do orquestrador de cada fatia. As descrições marcam isso como "aceite: ex-D12".
- Limite de WIP: 2 a 3 itens de código em progresso ao mesmo tempo, no máximo 1 build pesado por vez (ASan/UBSan, matriz de CI). Ondas de decisão e de documentação sem build não contam WIP de máquina. Na onda 11 o cap é explícito: 3 frentes (D5, F5, F1), com D17 e D7 na fila, nesta ordem. Fila sempre nomeada: a ordem das linhas é a fila.
- Ao commitar trabalho de um item, citar o ID na mensagem do Conventional Commit e tocar o Status no mesmo commit (implementação entregue vira `🔍 Pendente verificação`, nunca `✅` direto).
- Reordenação de 25/08/2026 (quatro lentes: arquitetura, produto, esforço, fluxo; consolidação do COO): TODOS os itens, inclusive os 13 que antes usavam pontuação do COO (G5, B7, B8, D15, TST-1, TST-2, AUD-1 a AUD-5), passam a usar CoD da lente de produto e Job Size da lente de esforço — régua única, para o WSJF ser comparável entre linhas. A Dificuldade de 8 itens foi corrigida pela medição da lente de esforço (D16, F4, H1 para cima ou para baixo conforme o caso; C1, C7, D7, E4, TST-1 para Alta).

## Fórmula WSJF (referência de scoring, não é tabela de trabalho)

- `CoD = Valor + Criticidade + Redução de Risco`
- `WSJF = CoD / Job Size`
- Régua de pontuação por dimensão e de Job Size: 1, 2, 3, 5, 8, 13, 20.
- **Dependência vence valor, sempre:** o WSJF ordena só DENTRO da onda. Empates de WSJF desfeitos por dependência primeiro (quem destrava mais) e irreversibilidade depois (ex.: A3 antes de G1 no empate em 7,0 porque A3 é cadeia crítica e porta de mão única).
- Exceção registrada de ordem intra-onda: **D5 abre a onda 11 apesar do menor WSJF dela** (1,1), por ser a cabeça da cadeia restante mais longa — maior cauda de dependentes (D6, D13, D14, F3, F4, F6). O WSJF dele afunda pelo Job Size 20, mas atrasar D5 atrasa o caminho crítico inteiro.

## Rastreio dos 28 itens do inicial.md (condição da L-21 para o líder poder apagá-lo)

- Item 1 (jogo 2d c++23): virou as leis L-02 e L-03. Resolvido e encerrado.
- Item 2 (só GlintFx e SO, bus, esperar parado): virou a LEI ZERO, L-05 e L-07. Resolvido e encerrado.
- Item 3 (camadas, proibido monolito, POCO por átomo): virou L-04 e L-17; execução em C1, C2 e todo o grupo D.
- Item 4 (inspiração Zelda SNES, Chrono Trigger, Stardew): virou a L-26 (perspectiva 3/4 top-down); canon em `docs/`. Resolvido e encerrado.
- Item 5 (pixelado mas luxuoso): virou a L-02. Resolvido e encerrado.
- Item 6 (FOSS, discutir licença): virou a L-08 (AGPL-3.0-or-later com ressalva offline); execução em A5 a A12.
- Item 7 (lore com outra licença, livro comercial): virou a L-08 (assets todos os direitos reservados, livros obra à parte); execução em A6 e A7.
- Item 8 (wayland/x11 indiferente, GlintFx intermedia): coberto pela LEI ZERO e L-05. Resolvido e encerrado.
- Item 9 (repo /petrinhu/GusWorld): remoto público já existe e está vazio; execução em A12.
- Item 10 (CI com 5 runners): virou L-09 e L-20; execução em C4.
- Item 11 (main orquestra, fable audita, sonnet implementa): virou a L-10. Resolvido e encerrado.
- Item 12 (bus e Gus Dragon): virou a L-07; prática contínua de vigilância dos canais; execução das ideias dele em F1 a F4.
- Item 13 (homenagens com aceite): virou a L-16. Resolvido e encerrado.
- Item 14 (Gus Vector Tavus Vance): coberto pela L-16 e pelo canon em `docs/`. Resolvido e encerrado.
- Item 15 (muitos arquivos no cwd): o corpus foi levantado; limpeza no grupo B, publicação em B7 e B8.
- Item 16 (AskUserQuestion obrigatório): virou a L-11. Resolvido e encerrado.
- Item 17 (GODS_LAWS.md): existe, com 28 leis e índice de gatilhos. Resolvido e encerrado.
- Item 18 (do zero, assentado em GlintFx): virou a L-01. Resolvido e encerrado.
- Item 19 (tabela de pendências com WSJF em bullets no cabeçalho): é este arquivo. Resolvido com este documento.
- Item 20 (levantar tudo do cwd, conversar exaustivamente): feito nesta fundação; segue contínuo via L-13 e L-14.
- Item 21 (html/rml/css traduzidos pelo GlintFx): virou a L-27; execução só com `present/`, bloqueada pelo GlintFx, sem data.
- Item 22 (TODO.md revista e adaptada ao projeto): esta É a revisão adaptada; manutenção contínua pela skill de tabela de pendências.
- Item 23 (testes e auditorias como no GlintFx, mesmo rigor): virou a L-19; `TESTES.md` e `AUDITORIAS.md` adaptados na raiz; execução em C5, C7, TST-1, TST-2 e AUD-1 a AUD-5.
- Item 24 (reformatar o documento, leis em GODS_LAWS): feito; `GODS_LAWS.md` é a casa das leis. Resolvido e encerrado.
- Item 25 (proteção de saves e mapas): virou L-18 e L-25; execução no grupo E.
- Item 26 (configs com as mesmas proteções): virou L-18 e L-25; execução em E1 e E5.
- Item 27 (PixelLab): o pipeline de arte é do líder (L-02) e não gera item de execução; o estudo exaustivo da API pedido no texto **foi executado**, em `docs/_processo/dossie-pixellab.md` (295 linhas, ponto a ponto em [FATO]/[INFERÊNCIA]/[NÃO DOCUMENTADO], token não exibido nem vazado), listado em `docs/_processo/_INDEX.md`. Ponta do item 27 fechada. O encerramento do `inicial.md` (L-21) segue aberto: falta a reconferência dos outros 27 itens do rastreio contra a fonte primária, trabalho e decisão à parte.
- Item 28 (uso web autorizado): registrado como preferência operacional permanente. Resolvido e encerrado.

## Itens bloqueados pelo GlintFx (sem data; o GlintFx hoje tem um header e nenhuma data prometida)

- Causa: falta criptografia no GlintFx (pedido E7 vai ao bus, L-07 e L-25): E1 (o selo), E5 (a checagem real do selo), E6 (o re-selo real), E8b (o pacote binário selado; a metade E8a, as tabelas compiladas no executável, ganhou onda real em 05/09/2026 por decisão do líder, mesmo tratamento de E1, E5 e E6), E9, E10, E11.
- Causa: falta janela, contexto gráfico, entrada e texto: a camada `present/` INTEIRA, que por L-06 e L-27 não tem item nesta tabela (só nasce quando o GlintFx existir), e as partes visuais de F1 (travessia na tela), F3 (VFX da explosão) e F4 (relógio visível ao jogador). Itens de onda `—` por esta causa: F7, P1, P2, P3 (o pedido de UI derivado de `REQUISITOS-UI.md`), P4 (redimensionamento de sprite pelo motor gráfico, decisão de 30/08/2026), P5 (relógio de escolha da carta do Volta, decisão de 31/08/2026) e P6 (sombra de sprite em tempo de execução, pedido aceito no GlintFx em 02/09/2026 como `R2D-SPRITE-SHADE`, sem data).
- Por transitividade (tag de versão pressupõe jogo jogável, que pressupõe `present/`): REL-1, H1 e H2. H1 e H2 dizem `—` desde a reordenação de 25/08/2026; REL-1 diz `—` desde 05/09/2026, por decisão do líder: estava na onda 21, contradizendo esta premissa, com a tabela agendando a release enquanto este texto a declarava bloqueada.
- Nenhum destes tem onda com data. Quando o vácuo chegar (domínio esgotado e as duas frentes esperando o GlintFx), o que o time faz nele é decisão do líder, não de agente (L-11); dublê é proibido (L-05).

- 🚧 **A cerca do escopo é a L-29 do `GODS_LAWS.md`:** **14 cortes vivos**, numerados `C-01` a `C-16` com **dois** números aposentados (`C-03` e `C-13`, revogados em 25/08/2026). ⚠️ **A lista NÃO se transcreve aqui, e a razão está nesta própria linha:** até 28/08/2026 ela anunciava "sem crafting", "sem conquistas" e "cartas nunca craftadas" como cortes vivos — os três já revogados pelo líder havia três dias — e ainda trazia a frase truncada "campanha de a duração da campanha". Cópia de canon apodrece; **leia a L-29**. **Item novo que atravesse a cerca vai ao líder como pedido de exceção citando o corte pelo número, nunca como proposta comum.**
- 📓 **Erros já pagos** ficam em `docs/licoes-aprendidas.md` e, os de teste, na L-19. Consulte antes de repetir uma classe de trabalho.
- ✅ **Reordenação executada em 25/08/2026**, por ordem do líder, com quatro lentes (arquitetura, produto, esforço, fluxo) e consolidação do COO: onda derivada por topologia pura, 25 pares onda↔pré-requisito corrigidos (a checagem de 24/08 contava 20; a lente de fluxo mediu 25, expandindo notação de faixa e pré-requisito em prosa), WSJF recalculado em régua única, INBOX drenada (ver seção INBOX). Contagem: 88 itens (78 anteriores, nenhum removido, mais 10 novos: G6 a G12, B10, D17, P3). Ponteiros da L-30: preservados todos os existentes, acrescentados onde as lentes acharam falta (D8, D10, E7) e nos itens novos; **a lacuna medida em 21/08 (28 itens sem ponteiro) segue parcialmente aberta** e fecha item a item, nunca por invenção.
- ✅ **Reordenação executada em 05/09/2026**, por ordem do líder, com cinco lentes (fluxo, engenharia, mecânica, produção, narrativa) e consolidação em régua única: 10 pares onda↔pré-requisito corrigidos (D23, D24, D25, D26, D27, D28, D29, D30 e G27 dentro da onda 0; D31 dentro da 17), mais quatro decisões do líder aplicadas: REL-1 para `—`, E8 fatiado em E8a/E8b, AUD-4 sem A11b como pré-requisito (prática contínua que nunca fecha vira conferência dentro da própria auditoria), G28 reclassificado como entregável de design; G14, G15 e G16 movidos para os blocos das ondas 10 e 14 em que o líder os pinou, com a coluna Onda intocada (o pino protege QUANDO o item é resolvido, não onde a linha mora). Contagem: 152 itens (140 anteriores, E8 fatiado em dois, 4 novos da consolidação: G32, D35, D36, D37; 7 abertos pela drenagem da INBOX: D38, D39, D40, D41, D42, D43, P6). Três alegações de lente refutadas por medição na árvore antes de virar trabalho: um item para "quando o personagem aprende a filtrar" (já decidido em 03/09 e 05/09), um crítico "sobrevivente" das auditorias resgatadas (17 de 17 vencidos), e "22 cenas" no `comic-reliefs.md` (são 19 numeradas mais 11 subseções internas). **Régua de conferência de ordem para itens de canon, registrada por decisão do líder de 05/09/2026 e NÃO aplicada à coluna:** `CoD = itens destravados (transitivo) + risco de contradição sob L-13`, sobre Job Size Fibonacci. Rodada contra a onda 1, concorda com a régua única em pôr D30 no topo e D28 acima de D23, e diverge em dois pontos anotados sem aplicar: D25 (job size 2) subiria para primeiro e D27 cairia para o fim. A coluna WSJF segue a régua única de 25/08 em todas as linhas, para continuar comparável.

## INBOX

Fila de exceção: descoberta que ainda não virou item. Drenada de novo na reordenação de 05/09/2026 (`--reorder`): das doze entradas que havia, duas saíram por vencidas contra a árvore, oito viraram item (`D35` a `D43` e `P6`), duas ficam por não terem sido medidas por lente nenhuma, e uma entrou (o capítulo 10 da era-1, sem porte). Abaixo fica só o que ainda não tem porte ou medição, mais os mapas do que saiu.

> **Registro histórico, não é fila:** o mapa da drenagem de 25/08/2026 saiu da lista de exceção em 25/08/2026, na reordenação, porque tudo que ele mapeia **já virou item** — ele descrevia para onde as descobertas foram, não trabalho pendente. Preservado aqui pela rastreabilidade:
>
> `[drenagem 2026-08-25]` **Mapa do que saiu da INBOX nesta drenagem:** material do backup → G7; `REQUISITOS-UI.md` → P3; `save-por-local.md` → G8; compilador → G6 (e pré-requisito novo de C1 e C4); catálogo de bugs do Gus Dragon → bug (1) já era D16, bug (2) virou D17, as três perguntas de protocolo → G9; speedrun → G10; contrato de mapa do GlintFx → B10 (e pré-requisito novo de D10); ADRs citando o ADR-010 → G11; os 20 pares onda↔pré-requisito → corrigidos pela própria reordenação (medição final da lente de fluxo: 25 pares, mais o ciclo G1↔B1 que a checagem de 24/08 não parseou); backup do projeto anterior → resolvido (`gusworld_legacy`), com a cauda "o que mais trazer de lá" absorvida por G7; três decisões antigas de conteúdo → G12.

> **Registro histórico, não é fila:** mapa da drenagem de 05/09/2026, preservado pela rastreabilidade:
>
> `[drenagem 2026-09-05]` **Mapa do que saiu da INBOX nesta drenagem:** dívida da L-33 (`era-1-pre-codigo.md` e `comic-reliefs.md`, registrada em 24/08 por ordem do líder para decidir depois) → D35, D36 e D37 por decisão do líder de 05/09/2026, com o capítulo 10 da era-1 de volta à INBOX sem porte; quatro auditorias resgatadas do `gusworld_legacy` com 17 achados críticos → reconferidos um a um contra a árvore em 05/09/2026, **17 de 17 vencidos, nenhum item** (o último de pé, CRIT-T7-02, datação da captura do Patch-Zero em -3 contra -8, caiu por cinco fontes concordando em -3: `docs/narrative/deep/antagonists/patch-zero-deep.md:53`, `docs/narrative/characters/patch-zero.md:60`, `docs/narrative/timeline.md:133`, `docs/narrative/environments/08-selve-profunda.md:194` e `docs/narrative/foreshadowing.md:138`; o -8 vinha da própria auditoria antiga, `docs/_processo/AUDIT-T7-PILLARS-V2.md:16`, lida como se fosse a árvore); dívida T4/T6 → D38 (reconferência, não refator); 36 âncoras autobiográficas → D39 (medição, não correção); CRIT-T8v2-05 → removido, já estava vencido; reconciliação genealógica → D40; sombra de sprite → P6 (pedido aceito no GlintFx em 02/09 como `R2D-SPRITE-SHADE`); gastos das passivas → D41 (escopo cresceu de 5 para 10 cartas em 03/09); camada pirata §15 → D42 (a metade do líder, §15.6, fechou em 03/09); bateria caseira §5 → D43 (as duas decisões do líder fecharam em 05/09, commit `dd4a322`; a fonte do zinco e do cobre virou G32; a ideia de um item para "o momento em que o personagem aprende a filtrar" morreu porque já estava decidido, `cartas-hardware-pirataria-energia.md:617-624`).

- `[inbox 2026-09-01]` **"Estilhaçamento" como termo informal ainda não tem exemplo vivo.** Decisão do líder de 01/09/2026: o evento fundador se chama "O Transbordamento", e "Estilhaçamento" sobrevive como termo informal de personagens e NPCs exaltados, com sabor de teoria de conspiração (verbatim dele: "O transbordamento. Mas personagens/npc mais exaltados usam informalmente Estilhaçamento, quase como teoria de conspiração"). Das 43 ocorrências examinadas, nenhuma era fala de personagem. A regra existe e ainda não tem uma linha de diálogo que a exemplifique. Ponteiro: `docs/narrative/deep/eras/cosmologia-origem-deep.md`. *(Mantida em 05/09/2026: nenhuma lente mediu; é trabalho de `narrative-writer` de uma linha quando o líder quiser.)*

- `[inbox 2026-09-02]` **A física da bateria (decisão do líder, 02/09/2026) deixa três frentes pendentes de número e de reindexação, todas com trabalho do `economy-designer` e ratificação do líder.** (1) A tabela de capacidade de `cartas-numeros-proposta.md` (§1a, linhas 15, 54-59, 61, 65-76, 80, 256) e os lookups de `cartas-spec-dados.md` (:130, :229, :237, :250, :295, :280-281, :335) hoje indexam a capacidade pela CLASSE DA CARTA; o líder decidiu, verbatim, "cada carta tem sua classe. cada bateria tem sua classe, sao coisas diferentes": capacidade é propriedade da BATERIA. Reindexar exige número novo, e a coluna "Pirata especial (clone-falso)" é classe de carta sem bateria correspondente, então o remapeamento não é mecânico. (2) Números pendentes: CCA por tipo de bateria; tensão de pico necessária e carga em repouso (standby) por carta; curva SoH → CCA efetivo; curva SoH → perda de carga; os três patamares da escala de 3 V; qual bateria vem de fábrica em cada classe de carta; recalibração do ritmo de combate e do racional do `ChargeCost` ≈ 6 de `cartas-technomagik.md`. (3) Falta a spec própria dos quatro tipos de POCO de bateria (cada um átomo por ordem do líder) e dos três campos novos da carta. Ponteiro (L-30): `docs/design/mecanicas/cartas-hardware-pirataria-energia.md` §5. *(Mantida em 05/09/2026: são três entregáveis de natureza distinta, reindexação, números e spec de POCO, e fatiá-los em um ou três itens é decisão do líder ainda não pedida; quem briefar o `economy-designer` lê o §5 atual, não esta entrada, porque duas peças fecharam em 03/09 e 05/09.)*

- `[inbox 2026-09-05]` **Capítulo 10 da era-1 não tem porte.** `docs/narrative/deep/eras/era-1-pre-codigo.md` `## 10.` (linhas 2727-4595 hoje, 1.869 linhas, zero subseção interna) exige leitura editorial para achar ponto de quebra antes de poder ser dimensionado; ficou fora do `D36` por L-08 e L-33. Ponteiro: o arquivo do capítulo 10 criado pelo `D35`.

## TABELA UNIFICADA

| WSJF | ID | Onda | Grupo | Descrição Técnica | Prioridade | Pré-requisito | Dificuldade | Status | Estado Auditado |
|---|---|---|---|---|---|---|---|---|---|
| 14,0 | E7 | 0 | E | Registrar no bus a necessidade de criptografia ao GlintFx: hash, MAC, cifra autenticada com nonce e dados associados, KDF, bytes aleatórios criptográficos, comparação em tempo constante (L-25). DISPARA NO INÍCIO da fatia do envelope (L-07), não depois de E1 a E6; sem classificar prioridade do outro lado. **Reordenação de 25/08/2026:** antecipado da onda 17 para a 0 por derivação topológica — nenhum pré-requisito de engenharia real, e cada onda de espera é tempo de resposta do GlintFx que não corre em paralelo com o resto (risco número um do projeto; maior salto da tabela). A antecipação é de AGENDA: o disparo efetivo do pedido é decisão do líder (L-11), porque a letra da L-07 pede o pedido quando o jogo esbarra de verdade na falta. ✅ **RESPONDIDO pelo GlintFx em 25/08/2026** (`inbox/gusworld/archive/20260825-1300-glintfx-resposta-cripto-envelope.md`). **Entra**, como **envelope selado genérico**: cifra autenticada sobre bytes opacos, **chave sempre fornecida por quem chama** — a biblioteca dá o mecanismo, chave e política são nossas. Primitivas padrão testadas contra os vetores oficiais dos RFCs. **Posição:** meio do roadmap deles, junto da trilha do formato de mapa, ~5 a 6 fatias, **sem data**. Nada nosso bloqueia hoje. ⚠️ **Correção que atinge nosso desenho:** *"a chave mora no jogo, cujo fonte o líder controla"* **não sobrevive à AGPL** — o GusWorld é distribuído publicamente e linka biblioteca AGPL, logo **publicamos o caminho por onde a chave chega**. A chave pode ser dado fora do fonte; o código que a carrega é público. Coerente com a L-25, que já proíbe prometer impedimento. **Nada aqui é assinatura de API:** ela nasce do esbarrão real, quando a fatia do envelope chegar perto (L-07). ✅ **AUDITADO em 28/08/2026** pelo `internal-auditor`, independente de quem implementou, e reconferido por mim. **PASSA.** ⚠️ **Ressalva registrada, e ela corrige o texto deste item:** o que foi ao bus **não foi o pedido formal** com os seis primitivos da L-25 — foi deliberadamente um **aviso de intenção**, com a frase *"isto não é um pedido"* no corpo, por decisão do líder. O pedido formal fica para quando a fatia do envelope começar (L-07). O auditor confirmou que este item **não inflava** isso. | Alta | Nenhum | Baixa | ✅ Concluído | ⚠ |
| 14,0 | G2 | 0 | G | **DECIDIDO em 24/08/2026:** formato **por natureza do dado** — diálogo e texto no formato próprio da casa, tabela de balanceamento em planilha; **tudo vira binário depois de compilar**, sem exceção (reforça L-18 e L-25: o jogador nunca recebe texto). **Convenção de nome fixada pelo líder, verbatim:** *"`.gw.text` e mantenha o padrão de `.gw.[tipo]` para nossos formatos próprios"* — os quatro arquivos de diálogo foram renomeados de `.dlg.txt` para `.gw.text` no mesmo dia, e os ponteiros mortos dentro deles (ADR e caminho de código do projeto anterior) foram removidos. Convenção canônica em `docs/tech/convencao-formatos-gw.md`, que registra também o caso do mapa (extensão `.gw.map` nossa, conteúdo no formato do GlintFx, L-30 dele intacta). A escolha de formato por natureza vence porque os dois usos divergem: diálogo tem ramificação e cabe em uma linha no formato próprio, tabela de número ganha em planilha por comparação em massa. Planilha é fonte de build de terceiro, não formato nosso, então mantém `.csv`. Destrava E8. **Fica em aberto e não foi decidido aqui:** a especificação escrita do `.gw.text` não existe (vive implícita nos quatro arquivos e no exemplo comentado em `docs/narrative/propostas/cena15-dlgtxt-PILOTO-PROPOSTA.md`), e não foi decidido se ferramenta de build de terceiro é permitida. | Média | Nenhum | Baixa | ✅ Concluído | — |
| 14,0 | G6 | 0 | G | **DECIDIDO em 25/08/2026.** Era a única decisão pendente DENTRO da cadeia crítica, bloqueando `C1` e `C4`. **Linux: GCC E CLANG, os dois** — razão do líder: os dois pegam bugs diferentes, e com `-Werror` a divergência aparece cedo, quando custa uma linha. **Windows: MSVC** — ABI nativa, binário sem runtime extra, e é o que mais diverge do Linux, o que faz dele o melhor detector de código que só funciona em POSIX. ⚠️ **Consequência declarada: a matriz de CI cresce de cinco para **NOVE** entradas (GCC e Clang nas quatro plataformas Linux, mais MSVC no Windows; o "sete" foi erro aritmético do orquestrador, pego pelo arquiteto, que se recusou a forçar o número, e corrigido pelo líder em 25/08/2026), e a L-20 proíbe entrada não bloqueante** — as nove são obrigatórias, e a razão do líder é que **cobertura desigual esconde bug**: se só uma distro rodasse Clang, um aviso que só o Clang de outra daria passaria despercebido. Decididos no mesmo ato, e registrados em ADR próprio: **CMake com versão mínima medida** nas cinco plataformas (não estimada), **Ninja** como gerador, o **GlintFx por submódulo git pinado num commit**, e ele entra **compartilhado (`.so`)**, como o próprio framework faz. ⚠️ **A interação entre as duas últimas é o ponto de atenção:** a nota de ABI do GlintFx sobre exceção atravessando `.so` fica coberta DENTRO do nosso CI, porque o submódulo compila com o mesmo compilador do jogo em cada entrada; quem compila da fonte fora desse caminho, misturando biblioteca de distro com outro compilador, assume o risco. Caminho do submódulo fixado por ele: **`framework/GlintFx`** — `vendor/` sugeriria fornecedor de terceiro, e o GlintFx é projeto irmão do mesmo autor; `framework/` deixa a fronteira que a LEI ZERO protege legível pelo nome do diretório. **Ninja nas cinco plataformas**, Windows incluído, divergindo de propósito do job Windows do próprio GlintFx, que usa o gerador do Visual Studio: um gerador só é um comportamento só. Registrado em `docs/tech/adr/ADR-022` a `ADR-025` (L-30). | Alta | Nenhum | Baixa | ✅ Concluído | — |
| 13,0 | A13 | 0 | A | **Backup da chave do `git-crypt`, autorizado pelo líder em 25/08/2026** após a auditoria de `A3` apontar que ela tem **uma única cópia conhecida**. Hoje: `~/.config/gusworld/git-crypt-key`, 148 bytes, permissão `600`, fora da árvore e fora de pasta de cloud-sync — correto, mas **ponto único de falha**. Pela L-25, perder a chave é perder **todo o histórico** de `docs/_secret/`, `docs/narrative/deep/stinger/`, `docs/book/` e `tools/security/termos_proibidos.txt`, não só o estado atual. Destino escolhido pelo líder: **Bitwarden**. ⚠️ **A chave nunca passa pela conversa nem pelo chat de agente** — o líder executa a cópia para a área de transferência na própria máquina. O registro guarda, além da chave: os quatro caminhos que ela abre, o repositório, e o procedimento de restauração. Ponteiro (L-30): `GODS_LAWS.md` L-25. ✅ **FEITO em 25/08/2026, confirmado pelo próprio líder:** a chave está no Bitwarden dele (plano pago) como **anexo** de nota segura, com o texto explicativo no corpo da nota. ⚠️ **Verificação por palavra do líder, não por medição minha** — o cofre dele não é observável por agente. Registrado também na memória do projeto (`reference_chave_git_crypt.md`), com o procedimento de restauração e as armadilhas de UI do Bitwarden que custaram tempo nesta sessão. | Alta | Nenhum | Baixa | ✅ Concluído | ✓ |
| 12,0 | A1 | 0 | A | `.gitignore`: `resources/livros/` (943 MB regenerável) e `resources/glb/` (402 MB, pipeline pessoal do líder) fora do git (L-15). Porta de mão única: depois do push, corrigir custa reescrita de histórico. **AUDITADO em 25/08/2026** por Narciso (CISO): **PASSA limpo.** `git log --all -- resources/livros resources/glb` devolve vazio, e a varredura dos 2.668 objetos do histórico não achou nenhum dos dois diretórios. Não é só a árvore estar limpa: eles **nunca** entraram. | Alta | Nenhum | Baixa | ✅ Concluído | ✓ |
| 12,0 | A14 | 0 | A | **Conserto dos três defeitos que reprovaram o `A7`** na auditoria de 25/08/2026 (Cláudio/CLO, confirmados no shell). **(1)** `resources/fonts/PixelOperatorMono.ttf` e `-Bold.ttf` caem na regra `resources/**` e são declarados copyright de Petrus, todos os direitos reservados — mas são de **Jayvee Enaguas (HarvettFox96), sob CC0 1.0**, como o próprio `THIRD-PARTY-LICENSES.md` documenta. Anotação própria para `resources/fonts/**` com `CC0-1.0` e o crédito real, mais `LICENSES/CC0-1.0.txt`, que hoje não existe. ⚠️ **Não há ilícito** (CC0 permite redistribuir sob qualquer termo), mas há **misdeclaração de proveniência**, que é exatamente o que o REUSE existe para impedir — e o repositório é público. **(2)** `tools/security/termos_proibidos.txt` está *tracked* e descoberto pelas três vias (sem anotação, sem `.license`, e cabeçalho é impossível porque o arquivo é cifrado). Anotação para `tools/**` ou para o arquivo. **(3)** `.bigtech-porte` está *tracked* e descoberto. Como o `A7` é pré-requisito do `A12`, registre-se que **corrigir o `A7` NÃO exige refazer o `A12`**: o defeito é de metadado, não do commit. ⚠️ **A ferramenta `reuse` NÃO está instalada** nesta máquina, então a claim "passa no lint" segue não verificável — instalar exige autorização do líder (L-28). Ponteiro (L-30): `GODS_LAWS.md` L-08. ⚠️ **Escopo corrigido em 25/08/2026, depois de o `reuse lint` rodar de verdade** (a ferramenta foi instalada por `pipx` nesta data, autorizada pelo líder sob a L-28): os arquivos descobertos são **SEIS**, não dois — o `.claude/settings.json`, o `.gitattributes`, o `.gitignore` e o `resumo.pdf` também estão sem cobertura. ⚠️ **O `resumo.pdf` não é configuração:** é `GUSWORLD(1) — Resumo da História`, o enredo do jogo em formato de página de manual, e pela L-08 lore é **todos os direitos reservados** — classificá-lo como AGPL licenciaria a história do jogo como software livre, erro de mão única em repositório público. ⚠️ **E o defeito mais grave, o da fonte, o lint NÃO pega:** ele reporta `Licenças irregulares: 0` porque só verifica que *existe* informação, não que ela é *verdadeira*. Lint verde não prova conserto da misdeclaração. **Decisão do líder em 25/08/2026:** o `.bigtech-porte` **fica versionado e ganha anotação**, não sai do git (L-14). **CONSERTADO em 25/08/2026.** Os seis arquivos ganharam cobertura e a fonte foi reclassificada. **`reuse lint` DEPOIS: `1957 / 1957` com informação de copyright e de licença, `Licenças irregulares: 0`, e `CC0-1.0` entra na lista de licenças usadas — o projeto está conforme à REUSE 3.3.** ⚠️ **A prova do defeito grave não é o lint, e sim o SPDX resolvido por arquivo** (`reuse spdx`): as duas `PixelOperatorMono` agora resolvem para `CC0-1.0` com `FileCopyrightText: Jayvee Enaguas (HarvettFox96)`, e o `resumo.pdf` para `LicenseRef-GusWorld-Assets-Reserved`, **não** AGPL. Contraprova de que nada quebrou de lado: `resources/cards/*.gw.card` continua em `AGPL-3.0-or-later`, como a fronteira do catálogo da L-25 manda. ⚠️ **Classificação dos quatro arquivos de configuração como AGPL é inferência do implementador, confirmada pelo orquestrador por precedente do próprio `REUSE.toml`** (a regra de raiz já põe `*.md` e `NOTICE` sob AGPL) — não é ordem verbatim do líder, e ele pode derrubá-la. ✅ **PROMOVIDO em 25/08/2026, sem reauditoria, por decisão do líder.** Ele perguntou *"por que faria reauditoria?"* e a pergunta estava certa: a regra "implementador ≠ revisor" existe para impedir **autocertificação de julgamento**, e o que houve aqui foi **verificação mecânica por terceiro** — um agente editou, o orquestrador rodou a ferramenta, e `reuse lint`/`reuse spdx` são determinísticos: dão a mesma resposta para quem os rode. Reauditar repetiria os mesmos comandos pelos mesmos números. **Evidência que sustenta a promoção:** `1957 / 1957` com copyright e licença, `Licenças irregulares: 0`, nenhum arquivo descoberto; as duas fontes resolvendo para `CC0-1.0` com crédito a Enaguas; `resumo.pdf` em reservado; `.gw.card` seguindo AGPL como contraprova. | Alta | Nenhum | Baixa | ✅ Concluído | ✓ |
| 11,0 | G3 | 0 | G | **DECIDIDO em 24/08/2026:** do desenho antigo dos modos de morte sobrevive **todo o design** (§1 a §5 de `docs/design/mecanicas/modos-morte.md`: os quatro modos, dificuldade fixa por save, quebra-cabeça de última chance no Hardcore, três marcos do Difícil, enquadramento narrativo, e as sinalizações abertas do §5). **Contagem corrigida em 25/08/2026 pelo líder:** este item dizia "sete", mas duas das sete já estavam fechadas dentro do próprio §5 (a (1) rejeitada por ele, a (2) resolvida em 10/07/2026); **as abertas são CINCO**, e ele decidiu no mesmo dia que elas ficam paradas até a onda de combate. **Morre o plano de implementação (§6)**, escrito contra o código anterior (esquema de save V4→V5, telas e tipos daquela árvore, marcos M2 e M7) e contra a LEI ZERO (mandava cripto de biblioteca vendorizada; a cripto vem do GlintFx, L-25). O §6 foi marcado como revogado no documento, não apagado — a ORDEM das fases segue válida. Gateia a parte de morte de D5 e E11. **Gera item novo:** re-derivar o plano sobre as cinco camadas, por agente especialista, na onda do núcleo de regra. | Média | Nenhum | Baixa | ✅ Concluído | — |
| 11,0 | G4 | 0 | G | **DECIDIDO em 24/08/2026.** O rótulo "depende de resposta de terceiro" estava vencido: o Gus Dragon respondeu tudo o que era dele em 22/08. As três decisões do líder, fechadas agora e detalhadas em `F1`: **(a) preso na parede** — a parede o **cospe de volta por onde entrou**, nunca para o outro lado, com preço em dano; **(b) bateria em batalha** — sim, sem exceção, segue o padrão canônico de 08/08; **(c) bloqueio** — fecha em **50/50**, o número do Gus vira canon. Destrava o fechamento de F1. ⚠️ **Coluna normalizada em 25/08/2026 (`B15`):** usava `—` onde o resto da tabela usa `Nenhum`. | Alta | Nenhum | Baixa | ✅ Concluído | — |
| 9,0 | A5 | 0 | A | `LICENSE` com o texto verbatim e imutável da AGPL-3.0 (L-08). A escolha da licença já é lei e é porta de mão única (relicenciar exige consentimento de contribuidor futuro); a gravação do arquivo é mecânica. **AUDITADO em 25/08/2026** por Cláudio (CLO): **PASSA com ressalva cosmética.** Seções 0 a 17 completas, `How to Apply` intacto, nenhum texto do projeto dentro, paridade posicional com `LICENSES/AGPL-3.0-or-later.txt` nas mesmas sete linhas-marco. Ressalva: 1 ou 2 quebras de linha a mais no fim — divergência de byte, não de texto. | Alta | Nenhum | Baixa | ✅ Concluído | ⚠ |
| 9,0 | G5 | 0 | G | **DECIDIDO em 24/08/2026** (adiada desde 21/08): o relógio da missão do Gus (F4) **congela na batalha no Fácil e no Médio** e **corre visível no Difícil e no Hardcore**, com prazo mais apertado no Hardcore. Fora da batalha corre nos quatro modos. Resolve a tensão com o Pilar 1 ("sem timer no turno do jogador") escalonando em vez de escolher um lado, na mesma escada que a spec já usava para a consequência de falhar. Custo assumido: nos dois modos inferiores, "encarar inimigo custa tempo" deixa de valer. Registrado em `docs/design/mecanicas/missoes-cronometradas.md`. Libera o aceite final de F4. | Média | Nenhum | Baixa | ✅ Concluído | — |
| 9,0 | G8 | 0 | G | Decisão do líder: as duas perguntas abertas de `docs/design/mecanicas/save-por-local.md` — quem define a intensidade do campo em cada uma das 13 dungeons, e como o jogador percebe fraca contra total sem tutorial. Afeta o desenho fino de D10 (mapa) e D11 (save): a decisão pode mudar o shape do save antes de D11 travar. Origem: INBOX de 21/08/2026. ✅ **METADE DECIDIDA em 28/08/2026:** a percepção de **campo fraco contra campo total** é por **sinal diegético no próprio lugar** — luz, som, estado do ambiente —, e o jogador aprende jogando. **Motivo:** casa com o corte `C-16`, que já proíbe tutorial em parede de texto e exige onboarding orgânico; e **não acrescenta campo ao save**, porque a intensidade já é dado do mapa — o que era justamente o risco de travar o `D11` com shape errado. ⚠️ **SEGUE ABERTA a outra metade deste item:** **quem define a intensidade do campo em cada uma das 13 dungeons**. Não perguntei junto de propósito — é decisão de conteúdo, não de arquitetura, e não trava o `D11`. ✅ **PROPOSTA ENTREGUE em 29/08/2026** (commit `1222f71`, `docs/design/mecanicas/proposta-intensidade-campo-save.md`). **Critério antes da distribuição:** hierarquia de 4 regras, a primeira que casa decide — abertura do jogo **fraca** (o onboarding orgânico do `C-16` proíbe punir de cara), clímax **total**, a dungeon-tema do PEM **total** por exceção temática, e as outras 10 pelo **tipo** que o `mundo-topologia.md` já declara. Resultado: **5 fraca, 8 total**. O comprimento é **indeterminado no corpus em 10 das 13**, e a tabela diz isso em vez de imaginar. **Verificado que NÃO acrescenta campo ao save** — a intensidade segue sendo dado do mapa, que é a propriedade que impede o `D11` de travar. ✅ **DECIDIDO em 30/08/2026** (líder, por `AskUserQuestion`): **12 dungeons em intensidade total e 1 em intensidade fraca**, a de abertura ("Dutos — aparato (abertura)") — substitui a distribuição de 5 fraca / 8 total que a proposta de 29/08/2026 apresentava. Nova definição de fraca fixada junto, verbatim do líder: "Sem auto-save. Save manual dentro. Carregar devolve para a entrada da dungeon." Item fechado por completo: as duas perguntas abertas do `save-por-local.md` (quem define a intensidade por dungeon, e como o jogador percebe fraca contra total) estão respondidas. Documento que especifica: `docs/design/mecanicas/save-por-local.md` §1.2 e §6 itens 2 e 4 (canon); `docs/design/mecanicas/proposta-intensidade-campo-save.md` (proposta, apreciada e superada, não canon). ✅ **DECIDIDO em 05/09/2026** (líder, por `AskUserQuestion`): o sinal do campo e o menu de save **somam**, com o ambiente como primário, por onde o jogador aprende, e o menu confirmando. Fecha a divergência entre a decisão de 28/08/2026, que vivia só nesta tabela, e o texto do documento, escrito antes, que dizia que o sinal era o menu. Gravado no §3 do `save-por-local.md`; a pergunta morta saiu da lista de pontos abertos e a intensidade do campo entrou na lista do que a área carregada precisa expor. | Média | Nenhum | Baixa | 🔍 Pendente verificação | — |
| 8,0 | B15 | 0 | B | **Higiene da coluna `Pré-requisito`: 18 ponteiros para ID inexistente, em 13 itens.** Achado em 25/08/2026 pela varredura de órfãos disparada junto da auditoria do bloco `A`. **Classe 1, a perigosa (7):** faixa escrita como ID falso — `D3aD10` (`D11`), `D5aD11` (`D15`), `D3aD11` (`E2`, `E3`), `E1aE6` (`AUD-3`, `E9`), `AUD-1aAUD-4` (`AUD-5`). As quatro faixas expandem para itens que **todos existem**, então são coerentes — mas nenhuma checagem mecânica as enxerga, e a dependência fica invisível. ⚠️ **Não expandir no automático:** o precedente do `A12` provou que faixa inclui item errado (a faixa `"A1 a A11"` abrangia o `A4`, que nunca bloqueou nada). Cada uma exige conferir a descrição item a item. O caso mais grave é o `AUD-5`, que é **portão de release** e cuja dependência dos quatro capítulos de auditoria hoje não é verificável por máquina. **Classe 2 (4):** `P1`, `P2`, `P3`, `F7` dizem `Nenhum (bloqueado por present/, GlintFx)` — honesto e correto, mas a vírgula dentro do parêntese quebra o parser, que lê dois IDs de lixo. Trocar por `Nenhum` na coluna e mover o motivo para a descrição. **Classe 3 (3):** `E6` diz `E1 (mesma fatia)` — o ID é real, o parêntese é que atrapalha. `H1` e `H2` dizem `B7, tag de versão`: ⚠️ **"tag de versão" é dependência real de algo que a tabela NÃO rastreia** — decidir se vira item ou se sai da coluna. Ponteiro (L-30): `GODS_LAWS.md` L-30 (contrato da tabela). ✅ **FEITO em 25/08/2026, e eram 19, não 18** — o `G4` usava `—` na coluna onde o resto da tabela usa `Nenhum`, e escapou da primeira contagem. **Prova:** varredura de todos os pré-requisitos contra a lista real de IDs devolve **zero órfãos**; nenhum ciclo no grafo de 111 itens; schema íntegro em 10 colunas, uma só tabela no arquivo. ⚠️ **Nenhuma faixa foi expandida no automático** — cada uma foi julgada pela descrição do próprio item, como o precedente do `A12` exige. O `D11` provou-se sozinho: a descrição dele diz *"agregando D3 a D10"*. ⚠️ **Dois achados de julgamento, NÃO consertados por serem decisão do líder:** (a) o `AUD-5` fala em *"os cinco capítulos"*, mas o `AUDITORIAS.md` tem **seis**, e os quatro `AUD-*` anteriores cobrem os caps. **2 a 5** — o **cap. 1, os portões automáticos da L-19, não tem item de auditoria**, possivelmente porque é verificado por automação (`C5`) e não por auditor; (b) **não existe item para criar a tag de versão** que o `H1` e o `H2` declaram como pré-requisito — a dependência é real e saiu da coluna para a descrição deles. | Alta | Nenhum | Média | ✅ Concluído | ✓ |
| 8,0 | B2 | 0 | B | Remover nome real, biografia e link de foto **da pessoa viva usada como análogo** do capstone do roster, de `docs/design/roster-analogos/21-helion-tusk.md` e `docs/design/brainstorm-backlog.md`; o personagem **Helion Tusk permanece intacto** (L-16: homenagem só com aceite confirmado). ⚠️ **Escopo corrigido em 25/08/2026: são CINCO arquivos públicos, não dois.** Somam-se `docs/design/roster-analogos/03-tesla.md` (nota de escopo que desambigua dois personagens, e precisa continuar desambiguando sem o nome), `docs/_processo/lente-fluxo.md` e `docs/_processo/lente-produto.md` (citam este item ao explicar priorização, e ao citá-lo repetem o nome). **Fora de escopo por desenho:** `docs/_processo/mineracao-rodada-2.md` é relatório datado que transcreve texto antigo como registro histórico (L-24, exceção de citação); e `docs/_secret/auditoria-privacidade.md` é cifrado, não é público, e é o próprio registro da auditoria que achou isto. ⚠️ **Este item continha o nome real na própria descrição** até 25/08/2026 — o pedido de remoção reproduzia o que mandava remover. ⚠️ **Exposição histórica medida e ACEITA pelo líder em 25/08/2026:** o nome está em **2 commits já publicados** (`80cf16c`, `8d81293`), somando 19 ocorrências nos diffs; limpar a árvore **não** os alcança, e quem clonar acha por `git log -p`. **Decisão dele: NÃO reescrever o histórico** — a auditoria de privacidade do projeto classificou o caso como MÉDIO, o material são links de fontes públicas sobre figura pública, e a L-16 é regra de projeto sobre homenagear, não proteção de dado exposto; reescrever 83 commits e forçar push quebraria clones existentes para um risco médio. **Não reabrir sem ordem nova dele.** Ponteiro (L-30): `GODS_LAWS.md` L-16. ✅ **FEITO em 25/08/2026.** Os cinco arquivos públicos zerados (medido por `grep -w`, não por relatório de agente); **Helion Tusk intacto** em nome, história, papel e mecânica, 20 ocorrências; a nota de escopo do `03-tesla.md` segue desambiguando os dois personagens **sem** o nome; os quatro links — duas biografias, uma reportagem e a fotografia de referência de sprite — saíram inteiros, sem substituto. ⚠️ **Conserto do orquestrador por cima do implementador:** a nota que explicava a remoção **nomeava as publicações removidas**, o que é roteiro de volta à identidade por descrição (L-24). Reescrita para dizer quantas e de que natureza, declarando que não as identifica de propósito. ⚠️ **DECISÃO DO LÍDER, 25/08/2026, NÃO REABRIR:** a seção "Fontes consultadas" **fica como está**, com os três links que identificam a pessoa pelas empresas. Eu argumentei que um deles — a cadeia Zip2 (venda 1999) a X.com/PayPal (venda à eBay 2002), descrita como "padrão de reinvestimento" — **é biografia pessoal**, com datas e valores da carreira de um indivíduo vivo, e não arquétipo; ele decidiu mantê-los assim mesmo, ciente disso. ⚠️ **O implementador morreu de erro de API antes de commitar**, e o trabalho foi recuperado do disco em vez de refeito. | Alta | Nenhum | Baixa | ✅ Concluído | ✓ |
| 7,0 | A3 | 0 | A | Configurar `git-crypt` com chave simétrica, exportar a chave para FORA da árvore e informar o caminho exato ao líder. Executa ANTES de A2: sem o filtro registrado, o primeiro `add` de arquivo secreto entra em texto puro no objeto git, irreversível. Perda da chave é perda permanente do conteúdo cifrado. **AUDITADO em 25/08/2026** por Narciso (CISO): **PASSA limpo.** Os **19 blobs** de todo o histórico nos quatro caminhos cifrados começam com a magia `\0GITCRYPT\0` — **inclusive no primeiro commit**, o que prova que o filtro existia antes do primeiro `add` e que a janela irreversível de texto puro nunca abriu. Varredura dos 2.129 blobs do repositório atrás da chave: zero. ⚠️ **Backup da chave autorizado pelo líder em 25/08/2026** (item `A13`). | Alta | Nenhum | Média | ✅ Concluído | ✓ |
| 7,0 | G7 | 0 | G | Decisão do líder: triagem do material recuperado do backup e do `gusworld_legacy` — o que continua valendo do ui-kit de 7 componentes, dos 11 mockups e das 14 capturas em `docs/design/` (parados por L-27 até o GlintFx traduzir marcação), e o que mais trazer do legacy (`ROADMAP.md`, `CHANGELOG.md`, `ACKNOWLEDGMENTS.md`, tabelas antigas, seis dossiês de auditoria, resto de `docs/tech/`). Os 20 ADRs já foram processados em `docs/tech/adr/_INDEX.md`. `GusEngine/` segue fora por força da L-01. Origem: INBOX de 21/08/2026 (duas entradas, consolidadas aqui). **DECIDIDO em 28/08/2026: triar AGORA**, verbatim do líder. Despachado a agente na mesma data, e a triagem partiu-se em duas por um fato medido: **não existe clone local do `gusworld_legacy`** — é repositório privado e arquivado no GitHub, e confirmei que responde. **Metade A, local:** `docs/design/ui-kit/` (11 arquivos) e `docs/design/mockups/`, com veredicto por peça sob a **L-27** (nenhuma interface antes de o GlintFx traduzir marcação) e a **L-02** (2D pixel-art). ⚠️ Mockup em HTML **não é implementável** aqui — o jogo é C++ sobre GlintFx —, então a pergunta é se ainda serve como **especificação de intenção**. **Metade B, remota:** parecer sobre o que trazer, com clone **raso e sem blobs** em `/var/tmp`, apagado ao fim — o projeto anterior carregava centenas de MB de livros e modelos 3D (L-15). ⚠️ **É triagem, não migração:** o agente entrega parecer; trazer arquivo é decisão do líder depois de ler. ✅ **TRIAGEM ENTREGUE em 28/08/2026**, relatório em `/var/tmp/builds/claude-1000/triagem-G7.md`. Clone raso do legacy usado e **apagado**, confirmado. **É parecer, não migração:** trazer arquivo continua sendo decisão tua. **Metade A, medido por consumidor vivo:** ui-kit **8 de 9** com consumidor; mockups **6 de 11**; capturas **0 de 14** — nenhuma citada por nome em `.md` nenhum. **Metade B:** recomenda **não trazer nada** dos seis dossiês de auditoria (0 de 6 com consumidor), nem `ROADMAP`, `CHANGELOG`, `ACKNOWLEDGMENTS` ou tabelas antigas — já minerados. **Duas exceções com ressalva forte**, ambas achados que valem decisão tua: ⚠️ **(a) `runtime-hardening-plan.md`** — decisão **já aprovada por ti em 22/06/2026** sobre proteger valores **em memória durante a partida** (HP, créditos, XP). **Nenhuma lei atual cobre isso**: a L-18 e a L-25 só alcançam dado **em repouso**. É lacuna real com aprovação prévia. ⚠️ **(b) `REQ-decorator-image-tint.md`** — o `card-frame-spec.md` vigente declara o recolor por domínio **FECHADO** com `image-tint`/`luminance-multiply` do **glintfx v0.7.0**, que é o GlintFx **descartado e reiniciado do zero**. Canon possivelmente desatualizado (L-13). ✅ **AUDITADO em 28/08/2026** pelo `internal-auditor`, independente de quem implementou, e reconferido por mim. **PASSA COM RESSALVA, e a ressalva é um defeito real da triagem:** a métrica *"0 de 6 dossiês com consumidor vivo"* **está errada — é 1 de 6**. O `docs/narrative/INCOHERENCES.md` citava `docs/auditoria/AUDIT-LORE-2026-07-08/DECISOES-CRIADOR.md`, caminho que **não existe**, e a varredura por consumidor não o pegou. ✅ **A recomendação de fundo continua correta** (não trazer o dossiê), porque a afirmação já estava preservada em `factions.md:128` — conferido. Citação órfã apagada em 28/08/2026 sob a L-24, preservando a afirmação. Amostragem de 3 peças refeita pelo auditor bateu. | Média | Nenhum | Baixa | ✅ Concluído | ⚠ |
| 7,0 | P3 | — | P | Redigir e enviar ao bus, no momento certo, o pedido ao GlintFx derivado de `docs/design/ui-kit/REQUISITOS-UI.md` — a lista do que a interface exige. Não agora, por L-27 (nenhuma interface antes de o GlintFx traduzir marcação); mesmo padrão de disparo do E7, mas a fatia dele (`present/`) não tem data. O momento certo é quando `present/` estiver para nascer. Origem: INBOX de 21/08/2026. ⚠️ **Coluna limpa em 25/08/2026 (`B15`):** ela dizia `Nenhum (bloqueado por present/, GlintFx)`, e a **vírgula dentro do parêntese** fazia o parser ler dois IDs de lixo. **Nenhuma informação se perdeu:** não há pré-requisito de item, e o bloqueio é externo — a camada `present/` não existe e depende do GlintFx (L-06, L-27). Aqui vale também que L-27. | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 6,5 | G24 | 0 | G | Decisão do líder: o trocadilho **Volt/voltar** em `docs/narrative/vozes-party.md:190`. Ele **adiou a escolha de propósito** (*"a escolha final foi adiada pra decidir com calma"*) porque o trocadilho não sobrevive ao inglês — e a premissa era que o inglês só chegaria **depois** do lançamento. Com a alteração do `C-09` em 25/08/2026, o inglês entra **na 1.0** e a premissa caiu: a escolha deixa de poder esperar. É o primeiro caso de uma classe que vai reaparecer: **piada, trocadilho e jogo sonoro que só funcionam em pt-br**. Vale decidir junto a política geral (o inglês recria o efeito com outro trocadilho, ou entrega o sentido e perde a piada?), porque decidir caso a caso produz vozes incoerentes. Ponteiro (L-30): `GODS_LAWS.md` L-29 corte `C-09`. ⚠️ **WSJF recalculado (3,0 → 6,5):** decisão barata que destrava uma **classe inteira** (piada e jogo sonoro que só funcionam em pt-br); decidir caso a caso produz voz incoerente. 3 + 5 + 5 = CoD 13, Job Size 2. ✅ **DECIDIDO em 28/08/2026:** o inglês **recria o efeito com outro trocadilho**, e isto vale como **política para a classe inteira** — piada, trocadilho e jogo sonoro que só funcionam em pt-br. **Motivo dado nas opções e aceito por ele:** a matéria declarada deste jogo é **voz**, e piada achatada mata voz mais que erro de gramática. ⚠️ **Custo que ele aceitou de olhos abertos:** exige tradutor com **ouvido literário**, não conversor — é reescrita, o mesmo custo que ele já assumira ao pôr o inglês na 1.0. **Consequência direta no `D19b`**, que produz a prosa longa: recriar trocadilho não é linha de planilha, e o Job Size 20 daquele item já pressupunha isso. O caso concreto do `Volt/voltar` em `vozes-party.md` resolve-se por esta política, não por decisão avulsa. | Média | Nenhum | Baixa | ✅ Concluído | ✓ |
| 6,0 | A9 | 0 | A | `OFFLINE-NOTICE.md` bilíngue (pt-br e inglês): a cláusula de rede da AGPL não alcança quem só joga. Texto já redigido pelo Cláudio (CLO); gravar e formatar. **AUDITADO em 25/08/2026** por Cláudio (CLO): **PASSA limpo.** Bilíngue de verdade, medido: 1 título e 3 parágrafos em cada idioma, espelhados parágrafo a parágrafo — não é tradução parcial. A ressalva fica **fora** do texto da licença, como a L-08 exige. | Baixa | Nenhum | Baixa | ✅ Concluído | ✓ |
| 6,0 | G12 | 0 | G | Decisão do líder: três decisões antigas que travam conteúdo — efeito do suco de limão e da água com gás (itens de cura, canon do filho); balanceamento dos easter eggs; perguntas Q4 a Q7 de canon do Gus. Travam entradas específicas de conteúdo (catálogo de D3, economia e conhecimento de D8), não a arquitetura: D3 e D8 nascem e são testados sem elas, que entram depois como dado. Origem: INBOX de 21/08/2026. ✅ **Uma das três fechou em 25/08/2026:** o líder decidiu os dois efeitos de cura que faltavam. **Suco de limão** limpa efeito negativo e, refinado, vira **suco puro de limão**, insumo de bateria de baixa qualidade; **água com gás** restaura fôlego ou energia de ação e, refinada, vira **água destilada**, mesmo papel. O desenho que vale é o **trade-off**: cada item tem dois destinos que se excluem, e a lição, verbatim dele, é *"saber economizar e destinar corretamente recursos escassos"*. Escrito em `docs/design/mecanicas/comidas-ingredientes-craft.md` §5.5 e em `cartas-hardware-pirataria-energia.md` §5 (L-30). **Seguem abertas** as outras duas deste item. **DECIDIDO em 28/08/2026 COMO CONDUZIR, não o conteúdo:** o líder mandou trazer as duas restantes **uma por vez**, com opções preparadas — verbatim da opção que ele escolheu: *"eu leio o que o corpus já diz e volto com opções concretas em vez de te pedir para redigir do zero"*. É **mais trabalho meu e menos dele**, e casa com a razão da L-11: pergunta em prosa obriga o líder a redigir, e resposta redigida é lenta e ambígua. **Próximo passo:** ler o corpus sobre o balanceamento dos easter eggs e sobre as perguntas Q4 a Q7, e levar cada uma com alternativas concretas. ✅ **OPÇÕES ENTREGUES em 29/08/2026** (commit `54578e9`, dentro da área cifrada). **Cinco eixos**, cada um com 2 ou 3 alternativas concretas e uma recomendação — o formato que o líder fixou: opções prontas para escolher, não pergunta que o obrigue a redigir. Nada novo inventado. ⚠️ **As outras duas partes deste item já estavam fechadas:** os efeitos de cura em 25/08, e as perguntas `Q4` a `Q7`, que a apuração de 29/08 mostrou **já respondidas pelo líder em 04/08/2026** — o próprio documento declara a seção "Ainda em aberto" vazia. **Aguarda decisão do líder.** ⚠️ **Nota de processo:** o portão 5 barrou a primeira mensagem de commit por ela nomear em texto puro o que o arquivo cifrado protege. O gate estava certo. ✅ **DECIDIDO em 30/08/2026** (`AskUserQuestion`, cinco eixos): **Eixo 1** (Cogumelo-Recuperador) - Opção B modificada (drop de inimigo-mestre sem respawn, taxa por dificuldade Fácil 89% / Médio 55% / Difícil 34% / Hardcore 0%, canon novo do item: chocolate 80% cacau, papel alumínio vira insumo de craft); **Eixo 2** (Limiar Coletor de Bytes) - Opção C, documentação multi-estágio por espécie no Bestiário, limiar mantido em 100, fecha a pergunta 3 de `conquistas.md`; **Eixo 3** (Token-de-Coragem) - Opção A escalada, cura por dificuldade (Fácil 3% / Médio 5% / Difícil 8%), Hardcore sem Token, fecha a pergunta 7; **Eixo 4** (grades do minigame EE-19/EE-20) - guardrail escalado (Fácil 3 / Médio 5 / Difícil 8 grades), limiar de 3 erros seguidos mantido, correção do argumento de raridade/acessibilidade aplicada; **Eixo 5** (Persistência Geométrica) - limiar escalado por dificuldade (Fácil 34 / Médio 55 / Difícil 89 / Hardcore 144), substitui o valor único de 100. Escalar por dificuldade não viola o corte `C-06` (dificuldade fixa por save, nunca ajustada por desempenho). Registrado em `docs/_secret/proposta-balanceamento-easter-eggs.md` (área cifrada), `docs/design/mecanicas/conquistas.md` (perguntas 3 e 7) e `docs/narrative/comic-reliefs.md` (EE-4). **As TRÊS partes deste item agora estão fechadas** (efeitos de cura em 25/08, easter eggs em 30/08, `Q4` a `Q7` já respondidas em 04/08). **Trabalho novo descoberto, não executado aqui:** `D32` (estágios do Bestiário, pré-requisito do Eixo 2) e `D33` (reescrita da dica do EE-4 e craft do papel alumínio). ✅ **PROPAGADO em 05/09/2026.** Os três eixos que só existiam na área cifrada foram ao canon público: o limiar da Persistência Geométrica passa a escalar por dificuldade e revoga o valor único, o Token-de-Coragem perde o efeito fixo em favor da cura percentual escalada, e o guardrail de grades do minigame entra onde a dependência já estava registrada. A varredura achou sete gêmeos além dos dois que a auditoria apontara. Fica pendente só a reescrita da prosa de dois banners que ainda citam o valor antigo, trabalho de escrita narrativa. | Baixa | Nenhum | Baixa | 🔍 Pendente verificação | — |
| 5,5 | B10 | 0 | B | Espelhar em `docs/tech/` o contrato do formato de mapa que o GlintFx fechou e congelou em 22/08/2026 no bus, como spec do lado consumidor (ele é dono do formato; nós somos consumidores, não coautores): lista de volumes com `sensor` e `enabled`; `blocks_path` separado de `blocks_move`; UUID de mapa com destino de teleporte por ele; canal de propriedades nomeadas; camada única na v1 com reserva aditiva; preservação de bloco desconhecido ao regravar; e o selo aberto de integridade (detectar no mapa, proteger no save), que casa com a L-25. A implementação do lado do GlintFx está adiada sem data, mas o contrato não se move: D10 e D11 projetam contra ele hoje. Serve D10, D11, E1 e E3. Convenção de extensão: `.gw.map` nosso, conteúdo no formato dele (`docs/tech/convencao-formatos-gw.md`). Origem: INBOX de 22/08/2026 ("falta virar item nesta tabela na reordenação"). ⚠️ **Distinção que o GlintFx mandou em 25/08/2026 e que muda o desenho:** **mapa canônico e save são problemas diferentes**, e o mesmo envelope os serve em **modos** distintos. **Mapa canônico** é conteúdo estático que nós distribuímos: **detecção pura sai de graça, sem segredo nenhum** — o jogo guarda o resumo dos canônicos e compara; a cifra acrescenta só a ilegibilidade que o líder pediu. **Save** é escrito na máquina do jogador em execução: ali o que importa é **autenticidade**, e confidencialidade é bônus. Tratar os dois como o mesmo problema produz desenho mais pesado que o necessário num deles. ✅ **ENTREGUE em 28/08/2026** em `docs/tech/contrato-mapa-glintfx.md` (255 linhas, 13 seções). Os **sete pontos** do contrato confirmados contra a fonte, **zero divergência**. A correção de 25/08 — mapa canônico é **detecção pura sem segredo**, save é **autenticidade** — entrou como seção própria no topo, ligada ao selo de integridade. ⚠️ **ACHADO DE PROCESSO, e é o mais grave do dia:** a mensagem que congelou o contrato **nunca chegou à nossa inbox**. Ela está endereçada **`para: mapeditor`**, mora em `inbox/mapeditor/archive/20260822-0100-glintfx-decisoes-de-mapa-20-itens.md`, e menciona `gusworld` **13 vezes**, com uma seção inteira *"Ao `gusworld`:"*. **Fomos endereçados dentro da caixa de outro projeto.** O agente só achou porque foi procurar; qualquer sessão futura teria concluído que o contrato não existia. Vira decisão de protocolo — ver `G9`. ✅ **AUDITADO em 28/08/2026 pelo `internal-auditor`: PASSA, zero defeitos em 7 pontos + 1 correção.** Ele leu a fonte inteira e achou o que eu não sabia: o **selo de integridade** não vinha do documento de 20 itens, e sim de uma **segunda mensagem** (`20260822-0200-glintfx-selo-de-integridade-e-ordem-mantida.md`) — corretamente citada na própria spec. Verificou também que a spec **não propõe nem negocia** o formato (0 ocorrências de *sugerimos/propomos/deveria*) e **não contém promessa proibida** pela L-25 (0 de *impossível editar/à prova de trapaça*). | Média | Nenhum | Baixa | ✅ Concluído | ✓ |
| 5,3 | B14 | 0 | B | **Dominó do `C-09`, achado pela auditoria de `B13` em 25/08/2026.** A afirmação antiga *"en-intl pós-release v1.0.0"* sobreviveu em **seis** arquivos além dos que o `B13` cobriu, e todos hoje **contradizem a lei corrigida**: `docs/design/mecanicas/combat-flavor.md:329` (nota de convenção i18n), `docs/design/propostas/copy-stubs-combate.md:168` (instrução a quem aprova texto en-intl), `docs/book/BIBLE-V1-STRUCTURE.md:7` (audiência-alvo do livro), `docs/narrative/deep/_INDEX.md:153` (campo "Idioma" de tabela de metadados canônicos) e `docs/narrative/propostas/disputas-linguagens-farpas-PROPOSTA.md:13` (escopo de idioma do documento). ⚠️ O sexto, `docs/narrative/vozes-party.md:190`, **NÃO entra aqui**: não é data errada, é **decisão criativa que o líder adiou de propósito** (o trocadilho Volt/voltar não sobrevive ao inglês) apoiada na premissa de que o inglês só viria depois do lançamento — premissa que caiu. Reabrir é decisão dele, e está em `G24`. **Fora de escopo por desenho:** `docs/_processo/mineracao-roadmap-todo-antigos.md` cita o texto antigo como transcrição datada de mineração; editar corromperia o registro do que o documento antigo dizia (L-24, exceção de citação histórica). Ponteiro (L-30): `GODS_LAWS.md` L-29 corte `C-09`. ⚠️ **WSJF recalculado (3,0 → 5,3):** canon que contradiz lei vigente é o que produz os laços de revisão — 3 + 5 + 8 = CoD 16, Job Size 3. ✅ **ENTREGUE em 28/08/2026** (commit `5924f1e`), e o status ficou por sincronizar até 28/08 à noite — furo meu de escrituração, não do agente. Foram **8 arquivos**, não 5: ele achou **3 gêmeos** sozinho (`en_intl.md`, o `README.md` das traduções e `cartas-technomagik.md`). ⚠️ Editou dentro do **git-crypt** e o blob commitado **continua cifrado**, verificado. ⚠️ E fez uma distinção que se sustenta, conferida por mim no `ADR-001`: o inglês **do jogo** entra na 1.0, mas a edição **do livro** segue cronograma próprio pós-release — são coisas diferentes, e a única ocorrência que meu `grep` ainda acusa é justamente essa frase correta. ✅ **AUDITADO em 28/08/2026** pelo `internal-auditor`, independente de quem implementou, e reconferido por mim. **PASSA, 0 defeitos em 4 verificações.** A que mais importava: `git show HEAD:docs/book/BIBLE-V1-STRUCTURE.md` começa com `\0GITCRYPT\0` — **o blob commitado continua cifrado**, não houve vazamento de texto puro. | Média | Nenhum | Baixa | ✅ Concluído | ✓ |
| 5,0 | B3 | 0 | B | **Escopo corrigido em 25/08/2026, por decisão do líder, após auditoria do `qa-engineer`.** Este item descreve agora **só o que foi de fato entregue**: os ganchos de sessão de `.claude/settings.json` passaram a usar `$HOME` em vez de caminho absoluto de máquina (commit `0ef3499`, 24/08/2026), preservando portabilidade num arquivo que vai versionado. ⚠️ **Registro honesto do que deu errado:** o `✅` foi gravado em 22/08 (`e80e495`) sobre um diagnóstico que o próprio autor desmentiu dois dias depois, verbatim, *"a causa não era a que eu tinha reportado"*, e o `TODO.md` nunca foi retocado após o conserto real. A varredura ampla de caminho absoluto no corpus público, que a descrição antiga prometia, **nunca aconteceu** e virou o item `B11`. | Média | Nenhum | Média | ✅ Concluído | ✓ |
| 5,0 | G13 | 0 | G | **DECIDIDO em 25/08/2026.** Os três cortes fechados pelo líder, e a L-29 já traz o texto novo. **C-04:** o corte é amplo, **sem voz nenhuma no jogo** — alcança o voice-over assistivo do Diário, e a distinção "UI assistiva, não personagem" não abre exceção; voz volta só como **add-on pós-lançamento**. **C-09 ALTERADO** (lei das leis, com argumentação contra registrada): **o inglês entra no lançamento**, pt-br e en na 1.0. **C-14:** o corte é sobre **pago**; expansão gratuita não atravessa a cerca, e o pin do subterrâneo perde a palavra "DLC". Consequências despachadas em `B13` (reconciliar o corpus) e `D19` (produzir o inglês). Ponteiro (L-30): `GODS_LAWS.md` L-29. | Média | Nenhum | Baixa | ✅ Concluído | ✓ |
| 5,0 | G9 | 0 | G | Decisão do líder, protocolo compartilhado do bus (não tocável por agente): (i) o Ultra-Post entra no `PROTOCOL.md` como canal com formato definido?; (ii) o formato de timestamp do Gus Dragon vale para as respostas de todas as sessões?; (iii) a parte AMPLA do aviso proativo — avisar o Gus de qualquer atualização, além das ideias dele e da alta prioridade que a L-31 já cobre — entra no `PROTOCOL.md` como regra das quatro sessões? Ponteiro: L-31 e o `PROTOCOL.md` do clone do bus. Origem: INBOX de 22/08/2026 (catálogo de bugs, issue 8). ✅ **PARTE (i) DECIDIDA em 28/08/2026: o Ultra-Post ENTRA no `PROTOCOL.md` como canal com formato definido.** **Motivo:** ele **já funciona como canal na prática** — a L-31 obriga avisar ali com timestamp e uma das três classificações que o Gus Dragon fixou (**Bug Consertado**, **Bug Funcional**, **Bug Possível**). Canal que existe de fato e não está no protocolo é canal que a próxima sessão não sabe que existe; escrever o formato só registra o que já se faz. ⚠️ **SEGUEM ABERTAS as partes (ii) e (iii):** o formato de timestamp dele vale para as respostas de todas as sessões?; e a parte **ampla** do aviso proativo — avisá-lo de qualquer atualização, além do que a L-31 já cobre — vira regra das quatro sessões? ⚠️ O `PROTOCOL.md` é **compartilhado**: editá-lo muda o contrato dos quatro projetos.  ⚠️ **Parte (iv), NOVA, achada em 28/08/2026 pelo trabalho do `B10`:** o protocolo **não impede que uma mensagem enderecada a um projeto contenha seção dirigida a outro**, e foi o que aconteceu com o contrato de mapa — `para: mapeditor`, com *"Ao `gusworld`:"* no corpo e 13 menções a nós. Ficamos sem o documento por seis dias e só o achamos por acaso. **Decisão do líder:** o protocolo passa a exigir **uma cópia na inbox de cada destinatário substantivo**, ou basta o remetente listar todos em `para:`? ✅ **FECHADO em 29/08/2026** (commit `7ca4435` no bus). Partes **(i)** e **(iv)** já estavam no `PROTOCOL.md`. **(ii) decidida:** o formato do Gus vale para as quatro sessões — e **não custa nada**, porque a medição mostrou que o carimbo dele é exatamente o `[DD/MM/YY - HH:MM:SS]` já canônico; o que é dele é a numeração e as três classificações, válidas no canal dele. **(iii) decidida: NÃO** vira dever amplo — encher o catálogo de bug com toda mudança faz o sinal sumir, e canal que vira ruído para de ser lido. Válvula: o que não é bug nem ideia dele vai na issue dele. ⚠️ **Ambas são decisão autônoma, a confirmar retroativamente.** | Média | Nenhum | Baixa | ✅ Concluído | — |
| 5,0 | G32 | 0 | G | Fonte de obtenção do zinco e do cobre da bateria caseira: a decisão do líder de 05/09/2026 acrescentou os dois metais à receita sem fonte canônica no corpus. **Fechado em 05/09/2026, decisão do líder, por `AskUserQuestion`: sucata do ferro-velho como fonte principal, loja e negociação como rota alternativa**, no mesmo padrão de fonte dupla (drop de sucata ou compra) do catálogo de ingredientes de `economia.md` §7.8. Ponteiro (L-30): `docs/design/mecanicas/cartas-hardware-pirataria-energia.md` §5, "Fonte de obtenção do zinco e do cobre: sucata do ferro-velho como fonte principal, loja e negociação como rota alternativa". WSJF: Valor 2 + Criticidade 1 + Redução de Risco 2 = CoD 5, Job Size 1. ✅ **CONCLUÍDO em 05/09/2026.** Decisão gravada no §5 do documento de energia, com o ferro-velho como fonte principal e loja e negociação como rota alternativa. A verificação de C-level achou duas sobras, as duas corrigidas: a frase que ainda dava a fonte como lacuna aberta na lista de Pontos ABERTOS, e um ponteiro que mandava procurar a terceira rota comercial acima quando ela está abaixo. | Baixa | Nenhum | Baixa | ✅ Concluído | — |
| 4,5 | D21 | 0 | D | Documento canônico da **dungeon final da Selve Profunda**, promovendo a captura de 29/08/2026. Assume a vaga que o `mundo-topologia.md` já orçava, sem estourar as 13. Conteúdo, decisões e cadeia de missão em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Alta | Nenhum | Média | ⏳ Pendente | — |
| 4,0 | D22 | 0 | D | Entra no `docs/narrative/timeline.md` a **era de pré-história** decidida em 29/08/2026, junto do **sistema de calendário de duas contagens** (a antiga e a do GusWorld, com os sufixos que o líder fixou). ⚠️ Inclui **fixar o deslocamento entre os dois calendários**, sem o qual a inscrição do `D23` não pode ser escrita. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Alta | Nenhum | Média | ⏳ Pendente | — |
| 4,0 | B5 | 0 | B | **FECHADA em 24/08/2026: a pergunta já estava respondida pela L-25**, e o item a fazia de novo sem que ninguém reparasse. A lei diz, verbatim: *"Fonte do conteúdo: texto estruturado dentro do repositório, lido apenas pelo gerador em tempo de build. Nada em texto na distribuição"*, e explica por quê — cumpre a L-18 no que chega à máquina do jogador e preserva revisão de balanceamento, histórico número a número e erro de validação apontando a linha. **Não há tensão L-18 × L-25:** a L-25 é a aplicação da L-18, com a fronteira no texto que o JOGADOR lê, não no que o repositório guarda. O líder reafirmou a mesma regra no mesmo dia, ao decidir o G2 ("tudo binário após compilar"). O formato concreto ficou no G2; a especificação escrita dele é o item B9. | Média | Nenhum | Alta | ✅ Concluído | — |
| 4,0 | D20 | 0 | D | **Contrato de i18n do projeto, ordem do líder de 25/08/2026**, verbatim: *"lembre de colocar as marcacoes i18n nos arquivos para deixar tudo pronto para outras linguas possiveis apos lancamento"*. Escrever o contrato que todo texto do jogo obedece, **antes** de o `B9` desenhar o formato: convenção de **chave** (nomenclatura, granularidade, estabilidade — chave nunca muda de sentido), **locale como dimensão aberta** e não lista fechada, **cadeia de fallback** (locale ausente cai em quê, e o que aparece se a chave faltar), sintaxe de **placeholder** e de **plural/gênero** (ICU MessageFormat), **contexto para quem traduz** (a mesma palavra em botão e em prosa são chaves distintas), e **orçamento de expansão** (alemão estoura caixa que o pt-br cabia). ⚠️ O ponto da ordem é que **acrescentar idioma depois seja acrescentar DADO, nunca mudar formato** — mudar formato depois do lançamento colide com a compatibilidade de save e catálogo da L-25. Trabalho do `i18n-l10n-specialist` (L-10), não inline. Ponteiro (L-30): `GODS_LAWS.md` L-29 corte `C-09`, L-25. ✅ **AUDITADO em 28/08/2026** pelo `internal-auditor`, independente de quem implementou, e reconferido por mim. **PASSA, 0 defeitos.** O critério que decidia o item foi verificado e sustenta-se: **acrescentar idioma é acrescentar DADO, nunca mudar formato** — locale como string não-enum, metadado que nasce hoje para não quebrar depois, e migração de placeholder como reword, não rekey. A única exceção (fonte e glifo para script não-latino) está **declarada** como pendência de `present/`, não escondida. | Alta | Nenhum | Média | ✅ Concluído | ✓ |
| 4,0 | E12 | 0 | E | Refazer a avaliação de proporcionalidade de `E9` e `E10` (fases 2 e 3 da blindagem de save, L-25), depois que o líder revogou o `C-08` hoje (`GODS_LAWS.md` L-29): o jogo TEM conquistas, e ele quer conquistas da Steam quando a distribuição for real. A avaliação anterior (lente de produto) argumentava que `E9`/`E10` pareciam desproporcionais porque "o C-08 já corta placar e conquista", logo não haveria cenário competitivo a proteger; essa premissa caiu, porque agora existe exatamente o cenário que faltava (conquista destravada é reivindicação que um save forjado pode fraudar). Não é para repetir a pergunta antiga: é para reavaliar com a premissa nova, e registrar se `E9`/`E10` continuam no tamanho que estão, crescem, ou mudam de desenho. Ponteiro (L-30): `GODS_LAWS.md` L-29 (revogação do C-08) e TODO.md itens `E9` e `E10`. ✅ **ENTREGUE em 29/08/2026** (commit `edbfe21`, `docs/security/E12-reavaliacao-proporcionalidade-save.md`). ⚠️ **REPROVADO na revisão adversarial de 29/08/2026** (revisor independente do autor). O documento concluiu que `E9` e `E10` mantêm o tamanho, mas a **ponte lógica está quebrada num ponto central e citável**: ele empresta ao `E9` a justificativa que é do `E10`. A fonte que o próprio corpus usa para decidir, `docs/_processo/opcoes-protecao-save.md:107` e `:110`, trata **`A3` (forjar história nunca jogada) e `A4` (restaurar backup legítimo) como colunas DISTINTAS**, e a linha 33 define a defesa de `A4` como *âncora out-of-band + machine-binding*, **sem replay**. Logo: o `E9` (log encadeado com re-execução) resolve `A3`; o `E10` (âncora TPM) resolve `A4`. Um backup restaurado é estado **legítimo**, que o replay do `E9` valida com sucesso — é justamente por isso que a L-25 precisa de uma fase 3 separada. **Conferido na fonte pelo orquestrador**, e o revisor está certo. ⚠️ **Segunda ressalva:** o "custo amortizado" via `D13` é otimista — `D13` garante o motor determinístico, mas não o log dentro do save, o encadeamento por hash, nem o versionamento do verificador entre patches, que segue em aberto no `G20`. **O veredicto final pode até acabar sendo o mesmo, mas por argumento correto.** Refazer separando qual ameaça cada item resolve. ⚠️ **O texto errado esteve nesta linha e foi escrito pelo orquestrador, que repassou o resumo do autor sem conferir a fonte.** | Alta | Nenhum | Baixa | ❌ Reprovado | — |
| 4,0 | F7 | — | F | CRT do boot como fundo do menu inicial, ideia do Gus Dragon (autoria dele), nascida de feedback de PLAYTEST. Origem: commit `485c604` de 22/07/2026. Ele descreveu uma convenção do gênero, não gosto pessoal, verbatim: *"o menu inicial (so ele) em geral tem alguma arte, ou animacao por tras, e nao a tela de onde o jogador estava"*. Decisão do líder à época: reaproveitar o monitor CRT que o jogo já mostra no boot, em vez de encomendar arte nova, casando com o pilar de estética de terminal a custo de asset zero. **O menu de PAUSA continua congelando a cena**, comportamento antigo intacto — só o menu inicial ganha o fundo animado. Item inteiramente da camada que desenha: **bloqueado pelo GlintFx (L-06, L-27)**, sem código de domínio a escrever antes de `present/` existir. Onda `—`, sem data, como os demais bloqueados. Busca em `docs/` não achou spec dedicada de menu-CRT (só ocorrências de CRT em estética Balatro e em overlay de diálogo, assuntos distintos): **sem documento**. ⚠️ **Coluna limpa em 25/08/2026 (`B15`):** ela dizia `Nenhum (bloqueado por present/, GlintFx)`, e a **vírgula dentro do parêntese** fazia o parser ler dois IDs de lixo. **Nenhuma informação se perdeu:** não há pré-requisito de item, e o bloqueio é externo — a camada `present/` não existe e depende do GlintFx (L-06, L-27). | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 4,0 | G10 | 0 | G | Decisão do líder: o jogo passa a cortejar speedrun de propósito, com tech deliberada à moda do Celeste, ou segue indiferente? O ponto técnico já está resolvido em canon (`docs/design/mecanicas/encontros-aleatorios.md`, linha "Determinismo / speedrun"): manipulação por TAS é possível e ACEITA; save verificável por replay (D13) exige determinismo, determinismo permite manipulação, e o projeto escolheu o save verificável. Nenhum corte da L-29 proíbe; nada foi construído para isso. Pergunta do Gus Dragon na issue 6 do bus (autoria dele, L-07). Origem: INBOX de 22/08/2026. ✅ **DECIDIDO em 28/08/2026: INDIFERENTE — não cortejar, não impedir.** O jogo não ganha tech deliberada de speedrun. **Motivo:** a porta **já está aberta de graça** — o canon registra que manipulação por TAS é possível e **aceita**, consequência de o líder ter escolhido o save verificável por replay (`D13`). Quem quiser fazer speedrun faz. Cortejar significaria **construir e manter tech** para isso, escopo novo num jogo de campanha de escopo fechado (`C-15`), e cada mecânica passaria a ter de responder *"isto quebra a rota?"*. ⚠️ **É pergunta do Gus Dragon (issue 6 do bus, autoria dele), então a L-31 obriga avisá-lo sem ele perguntar.** | Baixa | Nenhum | Baixa | ✅ Concluído | ✓ |
| 3,5 | G17 | 0 | G | Decisão do líder: lugar exato onde a carta SUPER ("A Carta Perdida de Tusk") está escondida, o caminho até lá, e o método de chegada (se o encaixe dos 20 nós revela mapa, câmara, coordenada, ou desbloqueia algo que a party ainda decifra). Buraco de lore deixado nomeado, não preenchido por invenção de agente. Ponteiro (L-30): `docs/design/roster-analogos/21-helion-tusk.md` (seção "O clímax"), `docs/design/mecanicas/cartas-technomagik.md` §2.4, `CHARS.md` (entrada de Helion Tusk). ✅ **DECIDIDO em 28/08/2026.** **LUGAR:** um sítio que **não preexiste no mapa** — as 20 ESPECIAIS, encaixadas, apontam para uma coordenada que ninguém conhecia. O líder escolheu isto **ciente do custo de escopo que eu nomeei** (exige entrada nova no `PLACES.md`), e respondeu ao custo com uma frase que vale registrar, verbatim: ⚠️ ***"8h de campanha é muito pouco"***. **MÉTODO:** o encaixe **revela algo que a party ainda decifra**, não uma coordenada pronta. **Motivo:** casa com o **Pilar 4** — *Gus resolve por lógica, não por força*; coletar 20 cartas e receber uma seta é prêmio de colecionador, ler o que elas dizem **juntas** é o clímax do menino que vence pela inteligência, e ecoa a inconsistência de data que ele pega na dungeon. | Média | Nenhum | Baixa | ✅ Concluído | ✓ |
| 3,0 | A10 | 0 | A | `THIRD-PARTY-LICENSES.md` (hoje: só o GlintFx, AGPL-3.0-or-later). **AUDITADO em 25/08/2026** por Cláudio (CLO): **PASSA com ressalva.** ⚠️ **O arquivo está mais correto que este item:** a descrição dizia "hoje: só o GlintFx", mas ele já traz a segunda entrada — Pixel Operator Mono, CC0, com a licença **verificada na fonte, não presumida**. Quem envelheceu foi o item, não o arquivo. | Baixa | Nenhum | Baixa | ✅ Concluído | ⚠ |
| 3,0 | B12 | 0 | B | Reconciliação dos resíduos de vocabulário do modelo de fila CTB que a reconciliação de `D5` (25/08/2026) não cobriu: `docs/design/mecanicas/combat.md` §1, §6, §12 e parte do §18 (efeitos de fila em ambientes, ainda citando "posição-de-fila"); `docs/design/mecanicas/battle-screen.md` linhas 3, 16, 51, 175, 190 e 202 ("fila CTB", "fila de iniciativa"); e o item D10 de `battle-screen.md` ("Abertura: começa parada e espera input"), gêmeo exato do §19.4 de `combat.md`, já corrigido, este não. É trabalho de reconciliação de canon, não decisão: gateia a fatia de `D5` pela L-13 (canon desatualizado bloqueia, atualiza primeiro, trabalha depois), complementar à reconciliação já registrada dentro da própria descrição de `D5` (que cobre só `combat.md` §3, §4 e §4.1, e `battle-screen.md` D4). Ponteiro (L-30): os dois arquivos citados e TODO.md item `D5`. ✅ **ENTREGUE em 28/08/2026** (commit `07995b1`). Além do escopo pedido, a auditoria-dominó achou resíduos em **§17** (o escopo da fatia ainda chamava a primitiva antiga; a nota do statline falava em *"ordem bruta de iniciativa"*) e em **§18** (cabeçalhos de tabela e os deltas de Knockback nos terrenos, traduzidos 1:1 para o campo que Haste e Slow já usam — sem inventar unidade nova). ⚠️ **Relatado e não decidido (L-14):** §18.3 define *"turno do ciclo"* como *"rodada completa de fila"*, e o T2 Talude depende de *"rodada anterior"* — as duas indefinidas no modelo novo. Conversão de unidade é decisão. ⚠️⚠️ **ACHADO MAIS GRAVE, e ele se autoriza sozinho:** os blocos *"Histórico superado"* em `combat.md:148` **citam a L-24 ao contrário** para se justificarem — o texto diz *"(L-24: revogar não é apagar o registro de que a decisão existiu)"*, quando a L-24 diz **verbatim** *"Nada de bloco 'Histórico', 'Superado', 'era antiga', 'mantido para registro' ou seção recolhida com o texto morto dentro"*. São ~40 linhas do modelo de fila que o `ADR-017` substituiu. O agente **não tocou** — estava fora do escopo dele e apagar bloco atribuído a decisão do líder é maior que reconciliação de vocabulário. Decisão do líder. | Média | Nenhum | Média | ✅ Concluído | — |
| 3,0 | B13 | 0 | B | Reconciliação do corpus com os três cortes fechados em 25/08/2026 (`G13`). **(a)** `docs/narrative/diary/ui-spec.md:399` descreve voice-over por entry como recurso opcional de UI; pelo `C-04` o jogo **não tem voz nenhuma**, então a linha passa a marcá-lo como **add-on pós-lançamento**, fora da 1.0, sem sumir do registro (L-24 proíbe apagar o que o líder não revogou, mas o corte revogou a presença na 1.0 — o que fica é a porta que ele abriu). **(b)** o mesmo trecho promete *"G1: pt-br + en"*: com o `C-09` alterado, deixa de ser resíduo e vira canon — conferir se o arquivo diz isso com clareza. **(c)** `docs/narrative/diary/entries-mapas-timeline.md:166` marca o subterrâneo lacrado como *"possível arco DLC pós-jogo"*: tirar a palavra "DLC", que carrega pago, mantendo a possibilidade sem compromisso. Varrer o gêmeo em cada caso (auditoria-dominó): célula de tabela, legenda e título contam como texto. Ponteiro (L-30): `GODS_LAWS.md` L-29, cortes `C-04`, `C-09`, `C-14`. ✅ **AUDITADO em 28/08/2026** pelo `internal-auditor`, independente de quem implementou, e reconferido por mim. **PASSA, 0 defeitos vivos.** O `ui-spec.md` marca o voice-over como add-on fora da 1.0, e o pin do subterrâneo perdeu a palavra "DLC" — os dois conferidos contra o texto atual da L-29. O auditor notou que a correção original usava riscado, o que feria a L-24, **e que isso já fora corrigido 17 minutos depois** no commit `e3f4228`. | Média | Nenhum | Baixa | ✅ Concluído | ✓ |
| 3,0 | G11 | 0 | G | Decisão do líder: os quatro ADRs históricos em `docs/tech/adr/` que citam o ADR-010 apagado ficam como registro da época ou são editados? Nada de código bloqueado; cosmético, sem decaimento. Origem: INBOX de 21/08/2026. **DECIDIDO em 28/08/2026: EDITAR.** O líder mandou apagar a citação e preservar a afirmação — que é o que a **L-24 já prescreve nominalmente** para citação órfã. Os quatro ADRs mandam hoje o leitor a um `ADR-010` que **não existe**. Despachado a agente em 28/08/2026. ⚠️ **Instrução que foi junto, porque muda o risco:** o `ADR-010` adotava o modelo **inverso** ao vigente (o jogo dono da janela). Se algum dos quatro **apoia conclusão** naquele modelo, a conclusão pode estar **errada**, não só órfã — e nesse caso o agente **relata**, não conserta, porque é decisão do líder. ✅ **ENTREGUE em 28/08/2026.** Eram **7 ocorrências em 4 ADRs**, não 4 — `ADR-009`, `ADR-012`, `ADR-013`, `ADR-018`. Em cada uma, o ponteiro morto saiu e a afirmação ficou (L-24). Conferido: `ADR-010` zerou nos quatro. ⚠️ **Achado relatado e NÃO corrigido, por ser decisão tua:** **3 das 7** descrevem substancialmente o **modelo revogado** — *"o host possui o contexto GL/janela, o `UiLayer` se anexa por dentro"* —, que é o **oposto da LEI ZERO**. A conclusão dessas frases pode estar **errada**, não só órfã. O `ADR-018` inteiro (17/07/2026, anterior à LEI ZERO) é candidato a revisão maior. ⚠️ **Segundo achado, fora do escopo dele:** o `docs/tech/adr/_INDEX.md` declara que `ADR-008` e `ADR-009` *"foram apagados"* — **e os dois arquivos existem**, íntegros. Índice canônico afirmando falsidade sobre o próprio diretório. ✅ **AUDITADO em 28/08/2026** pelo `internal-auditor`, independente de quem implementou, e reconferido por mim. **PASSA, 0 defeitos.** Eram **7 ocorrências em 4 ADRs** e zeraram nos quatro, contado linha a linha no diff. ⚠️ O auditor **concorda com o achado que o implementador relatou sem corrigir**: 3 das 7 descreviam o modelo revogado (*"anexa a um contexto GL que o HOST possui"*), oposto da LEI ZERO. **Resolveu-se por outra via** — aqueles ADRs foram apagados por inteiro horas depois, na extirpação. | Baixa | Nenhum | Baixa | ✅ Concluído | ✓ |
| 3,0 | G18 | 0 | G | Decisão do líder: a motivação específica de Helion Tusk para esconder a CARTA em si, distinta da já fechada decisão de apagar o compilador. O corpus não registra por que ele escondeu a própria obra depois de tê-la em mãos, além da ressonância temática com a ética que levou à deleção do compilador. Lacuna nova, já nomeada no doc de design; nenhum agente preenche por conta própria. Ponteiro (L-30): `docs/design/roster-analogos/21-helion-tusk.md` (seção do clímax, parágrafo "O que isto fecha e o que continua aberto"). ✅ **DECIDIDO em 28/08/2026: esconder a carta É o método que torna a engenharia reversa impossível.** Não é ato separado do apagamento do compilador — são **a mesma decisão ética em duas frentes**: apagar o compilador impede fazer cartas novas; esconder a carta impede **refazer o compilador a partir dela**. ⚠️ **Fecha um laço que o próprio canon já tinha aberto:** o líder registrara que as piratas não passam de 20% de fidelidade *"pois RE é impossível, já que a carta não está disponível para ninguém tentar"* — ou seja, **a indisponibilidade nunca foi consequência, é o método**. | Média | Nenhum | Baixa | ✅ Concluído | ✓ |
| 2,7 | D32 | 0 | D | Desenhar e aplicar o conceito de **documentação multi-estágio por espécie** no Bestiário (`docs/narrative/diary/entries-fichas-bestiary.md`), decidido pelo líder em `G12` (Eixo 2 de `docs/_secret/proposta-balanceamento-easter-eggs.md`) como forma de manter o limiar de 100 do `achv_byte_collector` alcançável (~22 espécies × 5 estágios ≈ 110). Os cinco estágios já nomeados pelo líder: avistamento, primeiro combate, análise completa, fraqueza descoberta, item de drop identificado. **Fato medido, não suposição:** o documento hoje não tem o conceito de estágio (grep por "estágio"/"stage" não devolve nada nesse sentido; §5 confirma "Bestiary: Catálogo turn-based (~22 inimigos)" sem estágios); é desenho novo, não ajuste de número. Pré-requisito do Eixo 2 do `G12` funcionar de fato. Ponteiro (L-30): `docs/design/mecanicas/conquistas.md` §2.9 e pergunta 3; `docs/_secret/proposta-balanceamento-easter-eggs.md` Eixo 2. WSJF: Valor 3 + Criticidade 2 + Redução de Risco 3 = CoD 8, Job Size 3. | Média | Nenhum | Média | ⏳ Pendente | — |
| 2,7 | D41 | 0 | D | Proposta numérica do `economy-designer`, ratificada pelo líder, dos dois gastos de cada carta com face passiva, gasto de standby e gasto de disparo, nunca custo zero (decisão do líder de 02/09/2026), para dez cartas: as cinco passivas puras (Ada, Hayek, Mises, Planck, Pythagoras) e a face passiva das cinco Híbridas (Faraday, Maxwell, Newton, von Neumann, John Dee), extensão decidida em 03/09/2026; as três Fora de combate (Euler, Menger, Turing) ficam fora por decisão do mesmo dia. Ponteiro (L-30): `docs/design/mecanicas/cartas-hardware-pirataria-energia.md` §5 ("Passiva tem dois gastos: standby e disparo", e linhas 515-520 para a extensão); `docs/design/mecanicas/cartas/_vocabulario.md` §9. WSJF: Valor 3 + Criticidade 3 + Redução de Risco 2 = CoD 8, Job Size 3. | Média | Nenhum | Média | ⏳ Pendente | — |
| 2,7 | D43 | 0 | D | Proposta numérica do `economy-designer`, ratificada pelo líder, da bateria de baixa qualidade craftada a partir de lanches refinados e metais (§5; mecanismo, herança de risco, escada de três degraus por insumo, exigência dos dois insumos juntos e quem ensina a filtrar já fechados pelo líder, os dois últimos em 05/09/2026): capacidade e usos; preço de compra de cada degrau de refino do suco puro e da água destilada; taxa de sucesso do refino; probabilidade numérica de cada um dos três riscos herdados da bateria pirata; e como as nove combinações dos três degraus por insumo caem nos três resultados nomeados (muito ruim, ruim ou aceitável por sorteio, boa). A fonte do zinco e do cobre é o `G32`, não este item; a origem dos insumos é o `G12`. Ponteiro (L-30): `docs/design/mecanicas/cartas-hardware-pirataria-energia.md` §5, bloco "Não decidido, e não inventado aqui" (linhas 680-690). WSJF: Valor 3 + Criticidade 2 + Redução de Risco 3 = CoD 8, Job Size 3. | Média | Nenhum | Média | ⏳ Pendente | — |
| 2,5 | G20 | — | G | Decisão do líder, a tomar quando a fase 2 da L-25 for desenhada: o validador por re-execução do histórico de comandos roda contra o ruleset vigente quando o save foi gravado (exige versionar o catálogo e o verificador escolher a versão certa), ou o vigente no momento da verificação (quebra save cuja regra dependia de condição afrouxada ou apertada depois)? Não é exclusivo de conquista, vale para qualquer regra sujeita a replay. Ponteiro (L-30): `docs/tech/convencao-formatos-gw.md` (seção da lacuna registrada). ⚠️ **Onda posta em `—` em 28/08/2026, por decisão do líder.** Ele mandou movê-lo para a onda em que será decidido — e essa onda **é `—`**: o alvo dele, a fase 2 da L-25 (`E9`), está ele próprio sem onda, bloqueado sem data. Não é fuga da fila; é a fila honesta deste item. | Média | Nenhum | Baixa | ⏳ Pendente | — |
| 2,5 | P2 | — | P | Retrato reativo em combate — decisão do líder que não atravessou para este repositório. Origem: commit `764da8b` de 25/06/2026: o retrato do personagem troca de expressão ao soltar a ação (atacar vira conjurando, vitória vira feliz). Inteiramente `present/`: o domínio já emite os eventos que bastam (contrato de evento de D5 e D6, L-17); falta só a camada que traduz evento em troca de sprite de retrato, e essa camada **não nasce antes de o GlintFx ter janela, sprite e loop de quadro** (L-06, L-27). Onda `—`, sem data. Busca em `docs/` por "retrato"/"portrait" não retornou nenhum arquivo: **sem documento**. ⚠️ **Coluna limpa em 25/08/2026 (`B15`):** ela dizia `Nenhum (bloqueado por present/, GlintFx)`, e a **vírgula dentro do parêntese** fazia o parser ler dois IDs de lixo. **Nenhuma informação se perdeu:** não há pré-requisito de item, e o bloqueio é externo — a camada `present/` não existe e depende do GlintFx (L-06, L-27). | Baixa | Nenhum | Média | ⏳ Pendente | — |
| 2,5 | D33 | 0 | D | Reescrever a dica/flavor do EE-4 (Cogumelo-Recuperador) em `docs/narrative/comic-reliefs.md` para refletir o canon novo decidido em `G12` (Eixo 1 de `docs/_secret/proposta-balanceamento-easter-eggs.md`): chocolate 80% cacau em formato de cogumelo, papel alumínio colorido no padrão de cogumelo natural. Ligar o papel alumínio ao sistema de crafting como insumo (o item vira ingrediente, não só embalagem descartável). Trabalho de `narrative-writer` (prosa) e de quem cuida do catálogo de craft. Ponteiro (L-30): `docs/narrative/comic-reliefs.md` EE-4, linhas 918-935; `docs/_secret/proposta-balanceamento-easter-eggs.md` Eixo 1. WSJF: Valor 2 + Criticidade 1 + Redução de Risco 2 = CoD 5, Job Size 2. | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 2,3 | B6 | 0 | B | Conferência visual amostral das 1266 imagens antes de irem a público: o método indireto não achou captura de tela, mas nenhum humano olhou. Gateia B8. ✅ **APROVADO em 29/08/2026** pelo `qa-engineer` (commit `67b5b2a`, `docs/qa/B6-conferencia-visual-imagens.md`), com o orquestrador reconferindo o texto do relatório. **1263 de 1263 imagens olhadas**, por enumeração em 13 folhas de contato, zero pulos e zero amostragem por hipótese. **Nenhum vazamento de tela pessoal.** ⚠️ **Achado informativo que ficou para o líder (L-14):** `resources/vfx/boot_pixel_test/REJECTED_pro_hallucination_frame001.png` renderiza texto de marca de uma franquia e **está no remoto público**; o agente não apagou porque agente não declara nada morto. | Alta | Nenhum | Média | ✅ Concluído | — |
| 2,3 | D42 | 0 | D | Proposta numérica do `economy-designer`, ratificada pelo líder, da camada pirata de ESPECIAL e SUPER (§15, canon fechado em 25/08/2026; o mecanismo de entrega da SUPER original, §15.6, fechou em 03/09/2026 e já não é pendência do líder): probabilidade de cada um dos três graus de falha da cópia; custo em crédito e material do craft; preço da cópia comprada pronta no mercado negro. Ponteiro (L-30): `docs/design/mecanicas/cartas-hardware-pirataria-energia.md` §15. WSJF: Valor 3 + Criticidade 2 + Redução de Risco 2 = CoD 7, Job Size 3. | Média | Nenhum | Média | ⏳ Pendente | — |
| 2,0 | D18 | 0 | D | Especificação escrita das sete conquistas listadas como "Cross-ato (conquistas)" em `docs/narrative/comic-reliefs.md` (EE-4 cogumelo, EE-9 100 inimigos, EE-10 placa hospital, EE-12 100 mortes na mesma cena, EE-16 diálogo casca azul, EE-18 encorajador, EE-20 reunião de emergência), agora que a revogação do `C-08` (L-29) tirou a contradição que as travava. A spec formaliza, por conquista, o gatilho de domínio que a destrava, no formato `.gw.achv` (`G2`, nome confirmado em `G19`). A apresentação do banner de conquista é `present/`, bloqueada pelo GlintFx, fora de escopo aqui. Derivar dos sete gatilhos já descritos nas cenas, por narrative-designer; nomear o que não está coberto em vez de inventar. Ponteiro (L-30): `docs/narrative/comic-reliefs.md` (tabela "Distribuição expandida pelos atos") e `docs/tech/convencao-formatos-gw.md` (definição de `.gw.achv`). ✅ **ENTREGUE em 29/08/2026** (commit `889f04f`, `docs/design/mecanicas/conquistas.md`, 179 linhas). Das sete, **três tinham gatilho de verdade** (EE-9, EE-12, EE-20), **uma é parcial** (EE-18, que o corpus chama de sistema oculto, não de conquista) e **três são lacuna genuína** (EE-4, EE-10, EE-16 são descrição de item, placa de flavor e cena cômica, sem condição de destravar em lugar nenhum) — nomeadas como lacuna, não inventadas. Regra derivada do próprio `.gw.achv`: como o save guarda um **conjunto** de ids, nenhuma é destravável duas vezes. Fecha com 7 perguntas ao líder. ✅ **CONFIRMADO COM RESSALVA** na revisão adversarial de 29/08/2026, contra o blob `889f04f`. As três lacunas **são lacuna mesmo** (a varredura ampla achou só três referências a `EE-4`/`EE-10`/`EE-16`, e as três são traço de personagem, não gatilho); nenhum gatilho foi inventado; a regra do conjunto confere verbatim com `convencao-formatos-gw.md`; sem violação da L-27 nem do `C-08`. ⚠️ **Ressalva:** na pergunta 3, o documento parafraseia o gatilho em vez de citar a frase do corpus — *"100 entradas completas no Bestiário"* (`comic-reliefs.md:963`) —, e a palavra **"completas"** favorece a leitura que é impossível com ~22 espécies. Ao levar a pergunta ao líder, citar verbatim. | Média | Nenhum | Média | ✅ Concluído | — |
| 2,0 | G19 | 0 | G | Decisão do líder: confirmar o nome da extensão `.gw.achv` para o arquivo de definição de conquista (nome, dica, condição de destravar, oculta ou não), escolhida pelo arquiteto por consistência com `.gw.batt`, `.gw.foe` e `.gw.stat`. Ponteiro (L-30): `docs/tech/convencao-formatos-gw.md` (tabela de tipos). **DECIDIDO em 25/08/2026:** confirmado **`.gw.achv`**, pela consistência com os outros doze tipos; a spec das sete conquistas (`D18`) fica destravada. | Baixa | Nenhum | Baixa | ✅ Concluído | ✓ |
| 2,0 | D40 | 0 | D | Reconciliação nome a nome DENTRO de cada uma das três cadeias genealógicas de `era-1`, que o líder confirmou em 01/09/2026 serem objetos distintos que não precisam bater entre si: a cadeia institucional (dez nomes), a descendência de sangue da narradora (sete gerações) e a linhagem matrilinear canônica de `CHARS.md`; ninguém cruzou os nomes de cada conjunto sozinho para achar incoerência interna. Relatório de achados; correção é item próprio. Ponteiro (L-30): `CHARS.md` (bloco "Linhagem matrilinear cronistas Atelaiá" e linha 230) e `docs/narrative/deep/eras/era-1-pre-codigo.md` capítulo 10 (hoje linhas 2747, 2811 e 3087; o `D35` re-aponta para seção). WSJF: Valor 2 + Criticidade 1 + Redução de Risco 3 = CoD 6, Job Size 3. | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 1,5 | B1a | 0 | B | **Fase 1, compilar:** levantar a lista completa das ocorrências de `__DEP_REMOVIDA__` nos 45 arquivos, separando o nome de biblioteca raspado do **dano colateral em texto inocente**, e levá-la ao líder em `G1`. É varredura, não edição: nada é alterado nesta fase. **Split aprovado pelo líder em 25/08/2026** para desfazer o ciclo `G1` ↔ `B1`, em que cada um era pré-requisito do outro. **Cinco ocorrências já foram consertadas em 25/08** por decisão dele, como amostra da categoria colateral (`core::fmt` e três links `pcgamer.com`): a lista deve refletir o que sobrou. ✅ **ENTREGUE em 28/08/2026**, em `docs/_processo/lista-dep-removida.md`. ⚠️ **Eram 173 ocorrências, não 28** — a estimativa desta tabela errava por **seis vezes**. Medido em 116 linhas e 45 arquivos, por dois caminhos independentes, e reconferido por mim. **Cinco categorias, cada uma com recomendação própria** — não é aprovar tudo de uma vez: `CAT-A` stack vigente (11 linhas), `CAT-B` identificador técnico em ADR (65), `CAT-C` crédito de pessoa real (6), `CAT-D` dano colateral (2), `CAT-E` citação do marcador como tópico (31, **recomenda NÃO editar** — é o próprio rastro da auditoria). Mais **1 não classificada**, cifrada e não inspecionada (L-25). ⚠️ **`CAT-D` é dano de verdade, não citação órfã:** a substituição original comeu o `rml` de **"firmly"** num prompt de imagem — prova de que foi feita **sem fronteira de palavra**. ⚠️ **E uma classe inteira que busca por string NÃO alcança: a corrupção atingiu NOMES DE ARQUIVO**, onde o nome colapsou para `_` em vez do marcador. Nenhum `grep` pelo marcador os acha. **Dois achados laterais:** `ADR-005:117` perdeu o nome real e por isso o `THIRD-PARTY-LICENSES.md` não consegue atribuir a licença daquela dependência; e o `ADR-008` segue `Accepted` quando o resto do corpus já trata aquela stack como passada. ⛔ **ORDEM DO LÍDER, 28/08/2026: nenhum nome de dependência de terceiro volta ao corpus, em documento nenhum.** **Nenhum nome de dependência morta volta ao corpus.** Isto **revoga** a leitura anterior desta mesma data, em que eu recomendara restaurar e ele escolhera *"o nome volta"* — a ordem posterior vence, e nada chegou a ser executado: o `B1b`, que é a fase que edita, **nunca foi despachado**. ⚠️ **Não reabrir, não propor restauração, nem que pareça conserto óbvio.** Vale para toda dependência de terceiro já aposentada e seus derivados; os nomes não se escrevem aqui. | Alta | Nenhum | Baixa | ✅ Concluído | ✓ |
| 1,5 | G21 | 0 | G | Decisão do líder: os cinco ADRs que carregam decisão viva sem marcador de superação no próprio documento (`ADR-004`, `ADR-006`, `ADR-007`, `ADR-015`, `ADR-021`) ficam como registro histórico ou recebem o marcador? Nada de código bloqueado, cosmético e sem decaimento, mas listado como achado em `docs/tech/adr/_INDEX.md`. Distinto de `G11`, que trata dos ADRs que citam o `ADR-010` apagado. Ponteiro (L-30): `docs/tech/adr/_INDEX.md`. ⚠️ **Premissa mudou antes da decisão: eram CINCO, são QUATRO** — o `ADR-015` foi apagado em 28/08/2026 na extirpação. **DECIDIDO em 28/08/2026**, e o líder propôs opção melhor que as três minhas, verbatim: *"edite e deixe apenas as partes vivas. O que não tiver mais nada vivo, apague"*. Trata cada ADR **pelo que ele tem**, em vez de carimbar os quatro igual. Despachado a agente na mesma data. ⚠️ O risco que isto fecha **já se materializou hoje**: um agente leu no `ADR-015` a frase *"Vendorizar uma biblioteca de criptografia de terceiro, ESCOLHIDA"* como decisão vigente. ✅ **EXECUTADO em 28/08/2026**, cada ADR julgado pelo que tinha, como o líder mandou. **`ADR-004`:** ficou a fórmula de combate, confirmada verbatim no `combat.md`; saiu tabela de implementação de stack morta. **`ADR-006`:** saiu a primitiva criptográfica escrita em casa (LEI ZERO, L-25); ficou o formato de envelope, o timestamp por save e a semente de RNG, vivos por 6 citações ativas. **`ADR-007`:** saiu o desenho inteiro do arquivo de controles em texto (L-18); ficaram as três camadas anti-adulteração que não dependiam dele. **`ADR-021`: apagado inteiro** — nada sobrava fora do que a L-08 já cobre. Contagem corrigida para **13 ADRs vivos** nos dois índices. ⚠️ **Relatado e não decidido (L-14):** o mecanismo de *diff e continuar ou restaurar* do `ADR-007` **tensiona** com a resolução mais simples que a L-25 já dá para configuração selada (*cai em padrão de fábrica com aviso*). O agente não reconciliou por conta própria, e faz bem: é decisão. ⚠️ **AUDITADO em 28/08/2026: PASSA COM RESSALVA, e duas claims que eu repassei ao líder caíram.** **(1)** *"fórmula confirmada verbatim"* era **impreciso**: o `ADR-004` diz `multDisrupt`, o `combat.md` diz `multSynergy` (7 ocorrências). O **contrato estrutural** está vivo e correto — `multAmbiente` como último fator, default 1.0, teto `[0.44, 2.25]` —, só a string literal diverge, e a divergência é **anterior** ao `G21`. **(2)** As *"6 citações ativas"* do `ADR-006` **não se reproduzem**: o auditor contou 16 em 8 arquivos, ou 8 em 4 filtrando só formato de envelope. A conclusão de fundo se sustenta; o número era invenção. ⚠️ **Achado novo, IMPORTANTE:** duas citações vivas ficaram stale — `ADR-019:35` ainda chama o save de **JSON** (contra a L-18) e `mapas-formato-legado.md:46` ainda cita a primitiva criptográfica que **saiu** do `ADR-006` (contra a L-25). ✅ **RESÍDUO FECHADO em 29/08/2026** (commit `bad2a9a`): `ADR-019` e `mapas-formato-legado.md` pararam de contradizer a L-18/L-25, e a auditoria-dominó achou **mais três gêmeos** na mesma violação (`diary/ui-spec.md` §9.2, `diary/_INDEX.md`, `diary/knowledge-gates.md`), todos corrigidos no mesmo commit. Três achados de natureza diferente ficaram **relatados e não tocados** (L-14). | Baixa | Nenhum | Baixa | ✅ Concluído | ⚠ |
| 1,3 | B4 | 0 | B | Fechar as sete pontas soltas do canon: vocabulário .NET e contradição de plataforma em `plano_vs.md` (mesmo arquivo, sequenciar internamente), bloco histórico em `docs/specs/_INDEX.md`, títulos "Especificação Técnica de Asset 3D" nas oito specs, linha órfã em `style-guide.md`, citações órfãs em `resources/translations/en_intl.md`, ponta em `raid-log.md`. ✅ **ENTREGUE em 29/08/2026** (commit `ae37244`, 15 arquivos). **Duas das sete pontas estavam mortas** (viviam no `plano_vs.md`, apagado em 28/08). Das cinco vivas: bloco histórico do `_INDEX.md` apagado sob a L-24 — **e a alegação de que as 8 specs guardavam bloco de histórico interno era FALSA**, conferida arquivo a arquivo; os 8 títulos de spec 3D renomeados sem tocar conteúdo 3D (L-02); linha órfã do `style-guide.md` reconstruída com o idioma **já consolidado** em quatro outros documentos, não com formulação inventada; citação a arquivo `.cs` de stack morta removida do `en_intl.md`; e o `raid-log.md` teve as citações órfãs removidas, com o buraco **sinalizado em vez de preenchido**. **Mais três gêmeos** achados e corrigidos. **Três achados maiores ficaram relatados e não tocados** (L-14), entre eles uma convenção de caminho inexistente citada em **30+ arquivos**, muitos deles canon protegido. ✅ **CONCLUÍDO em 05/09/2026.** A verificação de C-level conferiu as sete pontas contra a árvore: cinco já estavam fechadas, duas morreram com o arquivo de plano de produção apagado em 28/08/2026. Sobrava um gêmeo que uma limpeza anterior deixou de fora, três citações ao arquivo apagado no documento de playtest, agora removidas preservando a afirmação de cada frase. A decisão sobre o RAID, que o item devolvia ao líder, foi tomada em 05/09/2026: revogado, documento removido. | Baixa | Nenhum | Média | ✅ Concluído | — |
| 1,2 | D38 | 0 | D | Reconferência, um a um contra a árvore, dos 71 achados de fadiga de prosa de `docs/_processo/AUDIT-T4-VOZ-V2.md` e dos achados de palavras e pontuação proibida de `docs/_processo/AUDIT-T6-PALAVRAS-V2.md`, com contagem `encontrados/reconferidos/vivos` impressa mesmo quando zero (L-36 global); refator só do que sobreviver, em item próprio aberto depois, nunca aqui (L-33). Nasce da drenagem de 05/09/2026 por decisão do líder, no padrão que a reconferência das quatro auditorias resgatadas sugere (17 de 17 críticos já estavam vencidos): a maior parte da dívida antiga foi paga sem rastreio. Ponteiro (L-30): `docs/narrative/INCOHERENCES.md:188` e os dois AUDIT. WSJF: Valor 2 + Criticidade 1 + Redução de Risco 3 = CoD 6, Job Size 5. | Baixa | Nenhum | Média | ⏳ Pendente | — |
| 1,0 | P4 | — | P | Redimensionamento de sprite em tempo de execução, pelo motor gráfico do GlintFx — decisão do líder de 30/08/2026, por `AskUserQuestion`, verbatim: *"todos 180, mas deixa para mudar o tamanho no glintfx, com o motor grafico, nao precisa regenerar"*. O tamanho CANÔNICO do sprite de personagem passa a ser **180×180 para todo o elenco**; os assets que hoje estão em **256×256** no disco (o Gus e o `bento_requiem`, confirmados por `identify`) **NÃO são regerados** — o ajuste de 256 para 180 é responsabilidade da camada de apresentação, em tempo de execução, ligando direto no motor gráfico do GlintFx (L-17, L-27). Isto é uma **EXIGÊNCIA NOVA** ao framework: capacidade de redimensionar sprite na apresentação. Registrada aqui como requisito, não como pedido — **nenhum pedido é aberto no bus agora**; ele só vai quando `present/` nascer e esbarrar de verdade na falta (L-07), mesmo padrão de disparo do E7 e do P3. Grupo `P` (decisão do líder fora do bus do Gus, camada `present/`; reúne P1, P2, P3 e este). Ponteiro (L-30): `docs/art/style-guide.md` §8, `docs/art/sprites-inventory.md`. | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 1,0 | P5 | — | P | Relógio de escolha visível na interface, exigência nova gerada pela especificação fechada da carta do Volta (Volt-Leech) — decisão do líder de 31/08/2026, por `AskUserQuestion`: ao usar a carta, o jogador tem um tempo cronometrado para escolher se o retorno do dreno vira mana ou vida; estourado o tempo, a energia dissipa e ele perde os dois. Valores por dificuldade: **Fácil 13s, Médio 8s, Difícil 5s, Hardcore 3s** (nomes de dificuldade de `docs/design/mecanicas/modos-morte.md`). Registrada aqui como requisito, não como pedido — **nenhum pedido é aberto no bus agora** (L-07: só quando o jogo esbarrar de verdade na falta); ele só vai quando `present/` nascer, mesmo padrão de disparo do E7, P1, P2, P3 e P4 (L-06, L-27). Grupo `P` (decisão do líder fora do bus do Gus, camada `present/`; reúne P1, P2, P3, P4 e este). **Duas consequências conscientes registradas junto da decisão, NÃO resolvidas aqui:** o combate por turnos ganha um elemento em tempo real (decisão de desenho, não descuido), e a escolha cronometrada é barreira de acessibilidade conhecida (Fácil, com 13s, é válvula parcial, não solução) — cabe ao trabalho de acessibilidade, quando vier. Ponteiro (L-30): `docs/design/roster-analogos/_EFEITOS-ESCOLHIDOS.md` linha 14 (carta do Volta). | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 1,0 | P1 | — | P | GPU dedicada, opt-in nas Configurações — decisão do líder que não atravessou para este repositório. Origem: commit `70c4a491` de 10/07/2026, decisão dele por `AskUserQuestion`: opção de usar a GPU dedicada, **sempre opt-in, nunca default**, para não punir máquina fraca; mais um "modo caprichado" de qualidade para PC forte, aplicado só no próximo boot, atrás de aviso de três botões. Conferido contra a **LEI ZERO** e a **L-05**: o GusWorld não cria contexto gráfico nem escolhe adaptador — isso é do GlintFx. A tela de Configurações é `present/`, inteiramente bloqueada (L-06, L-27), e a própria capacidade de escolher adaptador ou qualidade **ainda não existe no GlintFx** — dependência adicional, registrada aqui; **nenhum pedido é aberto no bus agora** (L-07: só quando o jogo esbarrar de verdade na falta, e hoje não há sequer `present/`). Grupo `P` (novo: decisão do líder fora do bus do Gus, camada `present/`; reúne este item e P2). Busca em `docs/` não achou spec dedicada: **sem documento**. ⚠️ **Coluna limpa em 25/08/2026 (`B15`):** ela dizia `Nenhum (bloqueado por present/, GlintFx)`, e a **vírgula dentro do parêntese** fazia o parser ler dois IDs de lixo. **Nenhuma informação se perdeu:** não há pré-requisito de item, e o bloqueio é externo — a camada `present/` não existe e depende do GlintFx (L-06, L-27). Aqui vale também que a capacidade de adaptador ainda não existe. | Baixa | Nenhum | Alta | ⏳ Pendente | — |
| 1,0 | ART-1 | 0 | ART | Ferramenta que regenera a PARTE MEDIDA de `docs/art/sprites-inventory.md` a partir do disco (contagem de pastas, arquivos, dimensão por `identify`, presença de `walk/`/`anims/`), para a deriva entre texto e disco não voltar — motivo da reforma de 31/08/2026, que achou oito divergências entre o documento e o disco real. ⚠️ **Ferramenta de apoio que nunca é ligada ao binário do jogo** (roda fora do build, só lê `resources/sprites/` e escreve markdown): pela reforma da LEI ZERO de 28/08/2026, isso **não é dependência** — registrado aqui para ninguém barrar o item por engano lendo a letra antiga da lei. Ponteiro (L-30): `docs/art/sprites-inventory.md`. | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 1,0 | ART-2 | 0 | ART | Regerar `north`, `east` e `west` de `personagens_inspirados/gus/anims/breathing_idle/` em cor plena, alinhados à técnica do `south`. Medido em 31/08/2026 por `identify`: `south/f0.png` é TrueColorAlpha, 12.056 cores (23/06/2026); `north/f0.png`, `east/f0.png` e `west/f0.png` são PaletteAlpha, respectivamente 52, 62 e 57 cores (23/07/2026) — técnica diferente da do sul, apesar de já estarem na pasta final. **DECISÃO DO LÍDER, 31/08/2026, por `AskUserQuestion`:** as três direções novas devem ser regeradas em cor plena; a hierarquia de dois regimes visuais (protagonista × elenco, decisão de 30/08/2026) vale entre o protagonista e o elenco, não dentro dele — o Gus não pode trocar de técnica conforme a direção pra qual olha, porque isso aparece quando ele gira. ⚠️ **A execução é do líder (L-02) — o pipeline de arte não é tocado por agente nenhum; nenhum agente deve executar este item.** Ponteiro (L-30): `docs/art/sprites-inventory.md` (seção "Gus — `anims/breathing_idle/`" e bloco ⭐ do Gus protagonista). Substitui o identificador `ARTE-RESP-4DIR`, que nunca existiu nesta tabela — só era citado dentro do próprio `docs/art/sprites-inventory.md`. | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 1,0 | G26 | 0 | G | Decisão do líder, 31/08/2026, por `AskUserQuestion`, registrada em `cartas-hardware-pirataria-energia.md` §5 ("Mana e bateria são o mesmo recurso" + "Múltiplas baterias e a barra da tela" + "Estoque e vazão"): mana e carga de bateria são o MESMO recurso, dois nomes intercambiáveis no texto do jogo, confusão inicial deliberada desfeita por fala de personagem; o jogador carrega várias baterias, existe uma ATIVA cuja carga a barra da tela mostra (padrão a mais cheia, seleção do jogador possível), posição de interface registrada como fato sem desenho de tela (L-27). ✅ **AMBIGUIDADE RESOLVIDA no mesmo dia (a pergunta original deste item):** a bateria é o ESTOQUE (carga, persiste entre batalhas, degrada) e a rampa do ator em `combat.md` §5 é a VAZÃO (taxa máxima de saque por turno, `2 + turnos`, com teto que passou a depender da saúde da bateria em uso, decisão do líder de 02/09/2026); o limite real do turno é o MENOR dos dois. Refletido em `combat.md` §5 (subseção "Mana / Compilação", e nota no clamp de `RestoreMana`) e em `cartas-spec-logica.md` §3.1 (subtração dupla apagada por L-24, agora uma subtração só). **Duas consequências da resposta — CORRIGIDO 31/08/2026, já estavam no canon vizinho, não eram pendência nova:** (a) ficar sem bateria em combate já tem mecanismo especificado — a carta vira `DEPLETED` (inerte), e o jogador troca via `SwapBattery`, ação de emergência de **2 AP fixo** (`cartas-spec-logica.md` §3.2-3.4, `cartas-numeros-proposta.md` §1c), com precedente do líder no mesmo padrão em `F1`/`glitch` ("bateria em batalha: consome, sem exceção", 24/08/2026) — **ainda em aberto, e só isto:** se ficar sem carga da bateria ativa em combate tem consequência ALÉM de não poder jogar a carta até trocar; (b) a curva de degradação já tem número — **13 pontos percentuais de SoH por recarga**, bateria morta abaixo de **21% SoH**, ~6 recargas de vida útil (`cartas-numeros-proposta.md` §1b). **Acréscimo novo do líder no mesmo dia, verbatim:** *"Bateria usada vai pra o slot de baterias usadas da mochila do jogador, ou do bolso. Para levar pro lixo de baterias ou recarregar. Nunca deve ser jogada fora."* — não é mecânica nova (o `BatteryItem{charge:0}` já existia), é um compartimento próprio para ele. **Ainda em aberto, marcado:** se o slot tem capacidade limitada e o que acontece se encher. ✅ **Segunda ambiguidade também RESOLVIDA no mesmo dia, acréscimo posterior do líder:** `battle-screen.md` linha 50 não estava errada nem desatualizada em mostrar Mana como pips — o líder decidiu que os DOIS elementos existem: **barra contínua = estoque** (carga da bateria ativa), **pips discretos = vazão** (o que ainda se pode sacar no turno); AP continua só pips, sem mudança. Posição "abaixo do quadro do personagem" confirmada como batendo com o cockpit da spec (medidor já fica sob retrato e nome) — não havia conflito de layout, só de forma. **Correção de fato registrada no doc-fonte:** `battle_scene.cpp`, citado antes como já "embarcado", **não existe** — este repositório não tem código nenhum; a spec de tela cita nomes de consumidor planejado, não implementação real. ✅ **Terceira e ÚLTIMA ambiguidade do sistema RESOLVIDA no mesmo dia:** cada carta gasta a PRÓPRIA bateria (modelo por-exemplar de `cartas-spec-dados.md`, sem mudança); baterias avulsas do inventário são peça de troca, encaixadas numa carta descarregada, nunca um cinto comum. "Selecionar outra bateria" é selecionar outra CARTA como ativa. A alternativa do cinto comum foi considerada e DESCARTADA pelo líder — apagaria a diferença entre duas cópias da mesma carta e enfraqueceria o sistema de pirataria. Refletido em `combat.md` §5 e `cartas-spec-logica.md` §3.1. **Sistema fechado, sem ambiguidade estrutural restante.** Ponteiro (L-30): `docs/design/mecanicas/cartas-hardware-pirataria-energia.md` §5, "Cada carta gasta a própria bateria: o fecho do sistema". Trabalho de propagação textual medido (250 "mana" / 289 "bateria" em `.md`) segue em `G27`, item novo, para não deixar este aberto para sempre. | Baixa | Nenhum | Baixa | ✅ Concluído | — |
| 1,0 | P6 | — | P | Sombra de sprite em tempo de execução, vinda do motor: o líder manteve em 01/09/2026 a proibição de sombra pintada dentro do sprite (`docs/art/style-guide.md`), o pedido foi ao bus no mesmo dia (`inbox/glintfx/20260901-1650-gusworld-sombra-de-sprite.md`, repositório `petrinhu/gusworld_ia_autocomm`) e foi ACEITO pelo líder no GlintFx em 02/09/2026 como item `R2D-SPRITE-SHADE` (onda W8 do `TODO.md` de lá), com o maior dos quatro escopos escolhido em 03/09/2026; continua sem data, e o trabalho de arte que depende de sombra fica parado até lá. Grupo `P` (camada `present/`, decisão do líder fora do bus do Gus), mesmo padrão de bloqueio de P1 a P5 (L-06, L-27). Ponteiro (L-30): `docs/art/style-guide.md`; `../GlintFx/TODO.md` item `R2D-SPRITE-SHADE`. WSJF 1,0, alinhado a P4 e P5 (mesma natureza, mesmo bloqueio). | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 0,9 | G28 | 0 | G | Efeito de interrupção que quebra o loop de reanimação dos zumbis do Pântano de Markov: decisão do líder de 31/08/2026 fixou a regra (dano normal derruba, só um efeito que quebra o loop mantém o zumbi caído; a carta do Gödel, "Sentença Indecidível", é o trunfo pontual para um caso difícil, não a regra geral), mas não desenhou o efeito em si. Falta nome, custo, alcance e qual carta o traz. ⚠️ **Corrigido em 31/08/2026:** dizia "Pântano da Atelaiá"; o líder apontou que não há sub-local novo, os zumbis corrompem o Pântano de Markov já canônico. Trabalho de design de mecânica (`lead-game-designer`), não decidido nem desenhado por este item. Ponteiro (L-30): `docs/narrative/environments/02-selve-sombria.md` §11 + `docs/design/brainstorm-backlog.md` §11. **Reclassificado em 05/09/2026 por decisão do líder:** entregável de design a delegar ao `lead-game-designer`, não decisão dele; a regra geral já foi decidida em 31/08/2026 e a ratificação do desenho (nome, custo, alcance, carta) é do líder. | Média | Nenhum | Média | ⏳ Pendente | — |
| 0,9 | D37 | 0 | D | Fatiar `docs/narrative/comic-reliefs.md` (2.133 linhas) em átomos por cena: medido em 05/09/2026, 19 cenas numeradas (1-10 e 15-19 como `##`, 11-14 como `###` dentro de `## Cenas de guerra de plataforma`) mais 11 subseções internas, uma por jogo homenageado, dentro dessa seção, e 149 referências em 51 arquivos a re-apontar, inclusive os ponteiros de `D18` e `D33` nesta tabela; a INBOX dizia 22 cenas, número que não bate com a árvore. Primeiro entregável: proposta de corte que diga, subseção a subseção, se esse bloco de subseções é agregação legítima da L-33 (partes que só têm sentido juntas) ou átomos presos, ratificada pelo líder antes do split. Ponteiro (L-30): `GODS_LAWS.md` L-33; o arquivo-alvo. WSJF: Valor 3 + Criticidade 1 + Redução de Risco 3 = CoD 7, Job Size 8. | Média | Nenhum | Alta | ⏳ Pendente | — |
| 0,8 | D35 | 0 | D | Fatiar `docs/narrative/deep/eras/era-1-pre-codigo.md` (4.595 linhas, dez capítulos `## N.`) em dez arquivos, um por capítulo, sem alterar uma linha de texto, e re-apontar no mesmo commit as 281 referências em 22 arquivos `.md` (quase todas por âncora de seção; as por linha crua vivem na INBOX do `TODO.md`, em `CHARS.md:230` e em `docs/_processo/AUDIT-T1-NOMES.md:502`), cumprindo a L-33 por decisão do líder de 05/09/2026 de virar item. Ponteiro (L-30): `GODS_LAWS.md` L-33; o arquivo-alvo. WSJF: Valor 3 + Criticidade 3 + Redução de Risco 5 = CoD 11, Job Size 13. | Média | Nenhum | Alta | ⏳ Pendente | — |
| 0,8 | D34 | 0 | D | **Desenhar e produzir as 32 áreas secretas dentro das dungeons**, decisão do líder de 31/08/2026 (`AskUserQuestion`), escalada pelo **tipo** de dungeon: labirinto 5, puzzle puro 3, mista 2, só batalhas 1, tutorial 1. Aritmética conferida contra a contagem de tipos do `mundo-topologia.md` §4: labirinto 2×5=10, puzzle puro 4×3=12, mista 3×2=6, só batalhas 3×1=3, tutorial 1×1=1, **total 32**. Somadas às 21 do §5, o jogo passa a ter **53 lugares secretos ao todo**. **Custo declarado, distinto da camada do §5:** são salas feitas à mão, não tiles reusados — camada CARA, e o líder superou deliberadamente o filtro de produção da linha 5 daquele documento só para esta categoria (registrado lá com a data, o filtro em si não foi alterado). ⚠️ **O que NÃO está decidido:** posição, conteúdo e recompensa de cada área secreta ficam para a fase de PRODUÇÃO, junto do layout fino de cada dungeon, com o `level-designer` e a engine de mapa (`docs/design/mundo-topologia.md` §10, item 1) — este item só fixa a quantidade. WSJF: Valor 5 + Criticidade 2 + Redução de Risco 3 = CoD 10, Job Size 13. Ponteiro (L-30): `docs/design/mundo-topologia.md` §4.1. | Média | Nenhum | Alta | ⏳ Pendente | — |
| 0,7 | G29 | — | G | Lista concreta do loot dos zumbis do Pântano de Markov ("o que nunca foi liberado"): decisão do líder de 31/08/2026 fixou o princípio (cada zumbi carrega algo que a rotina de decomposição travada devia ter devolvido ao mundo), mas não a lista de itens. ⚠️ **Corrigido em 31/08/2026:** dizia "Pântano da Atelaiá"; o líder apontou que não há sub-local novo, os zumbis corrompem o Pântano de Markov já canônico. Decisão do líder, não inventada por este item. Ponteiro (L-30): `docs/narrative/environments/02-selve-sombria.md` §11. ⚠️ **Onda 0→— em 05/09/2026, decisão do líder, por `AskUserQuestion`: adiado, verbatim: "deixe pra discutir quando for fazer essa área e esses inimigos".** Não é cancelamento (Status segue `⏳ Pendente`): é adiamento até a área do Pântano de Markov entrar em produção; medido nesta data, não existe hoje na tabela nenhum item de construção daquela área (só `G28` e o `G30` cancelado a citam), então não há onda numérica de destino a apontar. | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 0,5 | G30 | 0 | G | Nome próprio do sub-local do pântano com zumbis, designação descritiva de trabalho "Pântano da Atelaiá" (`PLACES.md`, `docs/narrative/environments/02-selve-sombria.md` §11). 🚫 **CANCELADO em 31/08/2026, decisão do líder: item vence sem objeto.** O erro era do orquestrador, não do líder: o pântano com zumbis não é um sub-local novo, é o Pântano de Markov, já canônico e já nomeado (`docs/narrative/environments/02-selve-sombria.md` §2, landmark 3). A designação "Pântano da Atelaiá" foi descartada por inteiro, removida de `PLACES.md` e de `docs/narrative/environments/02-selve-sombria.md` §11. Não há nome próprio pendente. | Baixa | Nenhum | Baixa | 🚫 Cancelado | — |
| 0,5 | G31 | 0 | G | O logo/ícone oficial do jogo passou a existir — `resources/images/vance_dragon_glyph-alfa.png`, decisão do líder de 01/09/2026, verbatim: *"esse arquivo será o logo/icone de gusworld"* — e isso destrava uma decisão que antes era impossível: classes de registro de marca a proteger, que `ASSETS-LICENSE.md` (última linha do arquivo) já deixa expressamente com o titular, verbatim: *"a validação jurídica formal cabe ao titular, inclusive: classes de registro de marca a proteger e se o carve-out precisa de menção nominal aos nomes de personagem"*. Sem arquivo de logo a pergunta não tinha objeto; agora tem. A decisão em si é do titular, não deste item. **Fato do ecossistema, não trabalho a executar agora:** este é o primeiro asset que atravessa os quatro projetos do ecossistema (o jogo, o site que conta o progresso, o editor de mapas, o GlintFx) — ícone de janela, favicon e identidade visual do editor nascem todos dele, quando cada um desses projetos chegar à própria fatia disso. Ponteiro (L-30): `ASSETS-LICENSE.md`, seção "Marca (carve-out)". | Baixa | Nenhum | Baixa | ⏳ Pendente | — |
| 7,0 | G1 | 1 | G | Decisão do líder: aprovar em bloco a lista de reparo das 28 ocorrências de `__DEP_REMOVIDA__` (a lista é compilada em `B1a` e vai a ele antes de qualquer edição). Destrava `B1b`, a fase de edição. ⚠️ **O bloqueio deste item era o NOME REAL da dependência, e em 28/08/2026 ele foi ACHADO no próprio repositório, não adivinhado.** O `docs/tech/adr/_INDEX.md` nomeava o repivô e citava o arquivo original do `ADR-008`. As evidências internas convergem: `__DEP_REMOVIDA__Renderer`, *"a `__DEP_REMOVIDA__` GPU API"*, *"a camada anterior removeu o suporte a gamepad; a seguinte tem gamepad nativo"`. **O marcador cobre UM nome só.** Uma segunda dependência NÃO foi raspada e aparecia em texto claro 78 vezes. ⚠️ **E o fato que decide:** a raspagem foi **incompleta** — o nome sobrevivia **em texto claro** no `docs/tech/adr/_INDEX.md` e no `docs/specs/_INDEX.md`, num repositório **público**. Manter o marcador nos outros 173 lugares **não protege nada**; só torna o corpus ilegível. Aguarda confirmação do líder antes de qualquer substituição. ⛔ **ORDEM DO LÍDER, 28/08/2026: nenhum nome de dependência de terceiro volta ao corpus, em documento nenhum.** **Nenhum nome de dependência morta volta ao corpus.** Isto **revoga** a leitura anterior desta mesma data, em que eu recomendara restaurar e ele escolhera *"o nome volta"* — a ordem posterior vence, e nada chegou a ser executado: o `B1b`, que é a fase que edita, **nunca foi despachado**. ⚠️ **Não reabrir, não propor restauração, nem que pareça conserto óbvio.** Vale para toda dependência de terceiro já aposentada e seus derivados; os nomes não se escrevem aqui. ✅ **FECHADO em 30/08/2026, decisão do líder: o item vence sem objeto.** Este item pedia aprovação para RESTAURAR o nome real. A ordem do líder de 28/08/2026 foi a OPOSTA — extirpar, não restaurar — e por ser oposta nunca precisou passar pela pergunta que este item fazia: ela a substituiu por inteiro, no mesmo movimento em que foi dada. É por isso que `B1b` (dependente) rodou e fechou antes de `G1` (seu pré-requisito na tabela): o pré-requisito real da extirpação nunca foi este item, foi a ordem direta do líder, e o commit `e2fcc6e` (28/08/2026) a executou. O grafo não estava quebrado — faltava só este registro, que fecha agora. **Estado medido nesta auditoria (30/08/2026), não herdado do relato de `B1b`:** restam **118 ocorrências do marcador em 12 arquivos** (contagem por `git grep`, comando `-o` para ocorrências e `-c` para linhas por arquivo), contra as 173 em 45 antes da extirpação. Dos 12, dez são exatamente a categoria **CAT-E** de `docs/_processo/lista-dep-removida.md` §7 (rastro da própria auditoria — leis, tabela, relatórios de processo — que o documento recomenda **não editar**), e os dois restantes (`docs/book/BIBLE-V1-APENDICE-G.md`, `docs/book/BIBLE-V1-PREFACIO.md`) são cifrados e tratados à parte, na fatia 2 desta mesma auditoria de 30/08/2026. **Dos três achados laterais do §9 do mesmo documento:** o de **compliance** está **VENCIDO** — verificado hoje, `THIRD-PARTY-LICENSES.md` lista só `GlintFx` e a fonte `Pixel Operator Mono`, ambos corretamente atribuídos, sem dependência órfã (o `ADR-005`, fonte da citação original, foi reescrito na própria extirpação de `B1b`). **Os outros dois (status do `ADR-008` e os dois nomes de arquivo corrompidos) NÃO foram resolvidos por mim nesta auditoria — rastreados em `G25`, sem fechar aqui por conta própria.** ⚠️ **Reaferido em 05/09/2026 pelo líder: a ordem de 28/08/2026 acima vale sem exceção nova, e a lei já responde sem precisar perguntar.** Medido nesta data por área (`grep -ro "__DEP_REMOVIDA__"`): zero ocorrências em `docs/tech`, `docs/design`, `docs/art`, `docs/narrative`, `docs/book`, `assets` e `resources`, e zero em `AI-DISCLOSURE.md`: as quatro categorias que exigiam decisão (`CAT-A` stack vigente, `CAT-B` identificador de ADR, `CAT-C` crédito de pessoa real, `CAT-D` dano colateral) já não têm ocorrência viva no corpus. Resta só `CAT-E`, a categoria que a própria lista recomenda **não editar**: cerca de 110 ocorrências em `docs/_processo` (relatórios que discutem o marcador como assunto), uma no `GODS_LAWS.md` e sete nesta própria `TODO.md`. **O nome não voltou a lugar nenhum:** conferido em `docs/art/characters/gus.md:70`, `docs/art/style-guide.md`, `docs/design/gdd.md`, onde a stack vigente diz "C++23 sobre GlintFx", nunca o nome antigo; e `AI-DISCLOSURE.md` preserva o crédito a El Iagows sem nomear a ferramenta ("guided me on architecture, rendering and spritesheets" / "me orientou no uso do pipeline de renderização e de spritesheets"). Não marco `✅`: esta é medição minha, não auditoria de C-level `fable`. | Alta | B1a | Baixa | 🔍 Pendente verificação | — |
| 7,0 | A2 | 1 | A | `.gitattributes`: Git LFS para sprite, VFX, imagem e áudio (cerca de 280 MB) e marcação git-crypt de `docs/_secret/**`. Depende do git-crypt já configurado (A3), resolução de conflito a favor da lente de arquitetura. **AUDITADO em 25/08/2026** por Narciso (CISO): **PASSA.** 1.317 arquivos sob LFS, e nenhum binário cru nos padrões prometidos em 1.302 blobs históricos. **Decisão do líder em 25/08/2026:** as duas fontes `.ttf` (17 KB cada) ficam **fora do LFS** — LFS é para arquivo pesado, os blobs já estão gravados de qualquer forma, e o item entregou exatamente o que prometia. | Alta | A3 | Baixa | ✅ Concluído | ✓ |
| 6,0 | A6 | 1 | A | Diretório `LICENSES/` no padrão REUSE: `AGPL-3.0-or-later.txt` e o texto da reserva de asset (L-08). **AUDITADO em 25/08/2026** por Cláudio (CLO): **PASSA limpo.** `AGPL-3.0-or-later.txt` e `LicenseRef-GusWorld-Assets-Reserved.txt` presentes, este último com o regime completo da L-08: reserva total, carve-out de marca, permissão de conteúdo de fã não comercial e os livros à parte. | Média | A5 | Baixa | ✅ Concluído | ✓ |
| 4,3 | B9 | 1 | B | Especificação escrita do formato próprio `.gw.text` e do pacote binário selado em que ele é compilado — os dois lados do gerador, que se definem em conjunto (decisão do líder, 24/08/2026). Hoje a spec **não existe**: vive implícita nos quatro arquivos de `resources/dialogues/` e no exemplo comentado em `docs/narrative/propostas/cena15-dlgtxt-PILOTO-PROPOSTA.md`, e o cabeçalho que dizia onde ela estava apontava para um ADR e um caminho de código do projeto anterior, ambos apagados. Sem spec escrita, o leitor de D9 e o gerador de E8 nascem por adivinhação a partir de exemplo. Derivar dos arquivos existentes, por agente especialista; nomear o que o exemplo NÃO cobre em vez de inventar. Segue o padrão `.gw.<tipo>` fixado pelo líder, documentado em `docs/tech/convencao-formatos-gw.md`. ⚠️ **Escopo ampliado em 25/08/2026, por decisão do líder:** o formato **nasce multilíngue**, não bilíngue. Ordem dele, verbatim: *"lembre de colocar as marcacoes i18n nos arquivos para deixar tudo pronto para outras linguas possiveis apos lancamento"*. Logo o envelope selado carrega **locale como dimensão aberta** (N idiomas), não dois campos chumbados: pt-br e en **preenchidos** na 1.0, e acrescentar um terceiro idioma depois é **acrescentar dado, nunca mudar o formato** — o que exigiria requebrar compatibilidade de save e catálogo (L-25). Implementa o contrato de `D20`. ⚠️ **WSJF recalculado em 25/08/2026 (2,2 → 4,3):** virou **one-way-door** quando o líder o fez multilíngue — mudar o formato depois do lançamento colide com a compatibilidade de save e catálogo da L-25. Valor 8 + Criticidade 13 + Risco 13 = CoD 34, Job Size 8. | Alta | G2, D20 | Média | ⏳ Pendente | — |
| 3,5 | D30 | 1 | D | Propagação ao canon das consequências já decididas desta linha, em `CHARS.md`, `sinopse.md`, `docs/design/technomagik.md` e `docs/narrative/arco-principal.md`. O `PLACES.md` já foi atualizado em 29/08. Gateia trabalho por L-13. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Alta | D21, D22 | Média | ⏳ Pendente | — |
| 3,0 | D28 | 1 | D | Especificação do **artefato de fim de jogo** (22 × 18,4 cm) como peça de hardware, do pareamento por radiofrequência com a carta `OCU-02-R_v1.0`, e da camada nova de tradução que ele habilita, com o ganho de eficiência que ela traz. ⚠️ Usar a formulação vigente **magia é sistema formal computável**, não a descartada. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Alta | D21 | Alta | ⏳ Pendente | — |
| 3,0 | D25 | 1 | D | Especificação do **traje de descida** criado por **Helion Tusk** com **Brunus Vetorial** a pedido da party — peça de hardware, não carta. Resolve duas das três impossibilidades físicas medidas; a terceira é resolvida pela geografia da caverna. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável), e a base factual em `docs/_secret/dungeons/pesquisa-profundidade-temperatura.md` | Média | D21 | Baixa | ⏳ Pendente | — |
| 2,7 | D39 | 1 | D | Medir as 36 âncoras autobiográficas em primeira pessoa ("minha mãe", "meu nascimento", "eu tinha doze anos", "minha herdeira") dos capítulos 9 e 10 da era-1, escritas para a vida de Verônica Atelaiá, contra a cronista contemporânea de Era 3 sem nome (commit `60c24cf`, decisão do líder de 01/09/2026), e relatar quais quebram e quais sobrevivem; a correção de prosa é item próprio depois (L-33). Depende do `D35` para o relatório apontar para os arquivos de capítulo, não para linha crua de um arquivo que vai ser fatiado. Ponteiro (L-30): `docs/narrative/deep/eras/era-1-pre-codigo.md` capítulos 9 e 10 (arquivos próprios depois do `D35`); `CHARS.md`. WSJF: Valor 3 + Criticidade 2 + Redução de Risco 3 = CoD 8, Job Size 3. | Média | D35 | Baixa | ⏳ Pendente | — |
| 2,5 | D27 | 1 | D | A **dungeon de 8 níveis** no subsolo do Edifício Vance e o objeto guardado no fundo dela. ⚠️ O acesso fica na **base do prédio**, não dentro do apartamento: o canon põe a bancada na **sala**, no **6º andar**, e ela é da **mãe Gargi** (`sinopse.md:80`). Inclui a parada obrigatória na **Biblioteca Cintilante**. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Média | D21 | Média | ⏳ Pendente | — |
| 2,5 | A8 | 1 | A | `README.md` com seção de licença, carve-out de marca e link para a nota offline (A9). **AUDITADO em 25/08/2026** por Cláudio (CLO): **PASSA limpo.** Seção de licença, carve-out de marca e link para a nota offline presentes. **Ponteiros mortos: 0 de 12 verificados. Promessas proibidas pela L-25: 0.** | Média | A5, A9 | Baixa | ✅ Concluído | ✓ |
| 2,0 | D23 | 1 | D | Expansão **mínima** do Sylvarin e a **inscrição da porta**: a língua tem hoje 13 raízes-âncora e nenhuma gramática, numeral ou sistema de escrita (`docs/narrative/lingua/02-lexico-semente.md`). Cresce só o bastante para a frase ser escrita e decifrada — decisão do líder de não abrir projeto de conlang como pré-requisito. Texto da inscrição em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Média | D22 | Alta | ⏳ Pendente | — |
| 1,0 | G27 | 1 | G | Propagação textual da equivalência mana=bateria e do modelo estoque×vazão×por-carta (`G26`, fechado por inteiro), medida e maior que uma fatia: varredura de 31/08/2026 (`/usr/bin/grep -rniw`) achou 250 ocorrências de "mana" e 289 de "bateria" em `.md` no corpus, espalhadas por `combat.md`, `cartas-technomagik.md`, `cartas-spec-dados.md`, `cartas-spec-logica.md`, `cartas-statlines-rascunho.md`, `onboarding-vs.md`, `economia.md`, `proposta-economia-comedimento.md`, `resources/translations/pt_br.md` e outros; buscas por "medidor único"/"mana global"/"mana do personagem"/"barra única"/"barra de mana"/"quadro do personagem" deram zero. Cobre: (1) revisar cada ocorrência à luz do modelo fechado em `G26` (estoque×vazão, bateria por-carta), corrigindo redação que ainda descreva dois medidores separados ou bateria compartilhada; (2) atualizar `battle-screen.md` linha 50 para descrever os dois elementos (barra de estoque + pips de vazão) em vez de só pips de Mana, quando a fatia de UI nascer com `present/` (L-27; não editado nesta fatia, é desenho de interface); (3) escrever, quando o líder e o `ux-writer`/`narrative-writer` decidirem, a fala de personagem que ensina a equivalência mana=bateria (`cartas-hardware-pirataria-energia.md` §5, "Mana e bateria são o mesmo recurso" já registra a necessidade, não escreve o texto); (4) resolver com o líder, quando a onda de combate/inventário pedir, as duas perguntas genuinamente abertas que sobraram do sistema de energia: se o slot de baterias usadas (mochila/bolso) tem capacidade limitada e o que acontece se encher; e se ficar sem carga da bateria ativa em combate tem consequência além de não poder jogar a carta até trocar. Execução de fala/texto é de `ux-writer`/`narrative-writer`, nunca inline; qualquer desenho de tela espera `present/` (L-27). Ponteiro (L-30): `docs/design/mecanicas/cartas-hardware-pirataria-energia.md` §5. | Baixa | G26 | Média | ⏳ Pendente | — |
| 1,0 | D36 | 1 | D | Segundo corte da era-1: fatiar o capítulo 9 (`## 9. Vestígios que sobreviveram`, 1.208 linhas) pelas suas dez subseções existentes, com re-apontamento das referências; o capítulo 10 (1.869 linhas, zero subseção) NÃO entra aqui, porque exige leitura editorial para achar ponto de quebra e volta à INBOX sem porte (L-08). Ponteiro (L-30): `GODS_LAWS.md` L-33; o arquivo do capítulo 9 criado pelo `D35`. WSJF: Valor 2 + Criticidade 1 + Redução de Risco 2 = CoD 5, Job Size 5. | Baixa | D35 | Média | ⏳ Pendente | — |
| 9,0 | A11a | 2 | A | **Fase 1, o gabarito:** cabeçalho SPDX canônico (`SPDX-FileCopyrightText: Petrus Alves da Silva Costa` + `SPDX-License-Identifier`) e as anotações do `REUSE.toml` que declaram o regime por tipo de arquivo (L-08). **Fecha uma vez** e é pré-requisito real do primeiro commit `A12`. Nasceu do fatiamento do antigo `A11` em 25/08/2026, por decisão do líder sob a L-33: o item misturava duas coisas de tempos de vida opostos, e por isso travava o grafo — o `A12` esperava por algo que, por construção, nunca fechava. Ponteiro (L-30): `GODS_LAWS.md` L-08. **AUDITADO em 25/08/2026** por Cláudio (CLO): **PASSA com ressalva cosmética.** O gabarito é **um só, sem variação de grafia**, em 60+ arquivos. Ressalvas: ele não está escrito como spec em manual nenhum (vive nas instâncias), e o portão de CI que verificará o `A11b` vai precisar de fonte canônica; e as anotações de código do `REUSE.toml` não cobrem `.py` nem `.sh`, que já existem no repo. | Alta | A5, A6 | Baixa | ✅ Concluído | ⚠ |
| 5,8 | D19a | 2 | D | **Fatia 1, a camada de interface:** produzir em inglês as **244 chaves vazias** de `resources/translations/en_intl.md` (medido em 25/08/2026: 361 nomes de chave, só 117 com valor). ⚠️ **É o PRIMEIRO CONSUMIDOR REAL do contrato de i18n (`D20`)** — se a convenção de chave, o fallback ou o placeholder estiverem errados, é **aqui** que aparece, e ainda com tempo de consertar antes da prosa longa e antes do terceiro idioma. Nasceu do fatiamento do `D19` em 25/08/2026, na reordenação: o item inteiro tinha Job Size 20 e WSJF 1,5, então ficava adiado para sempre **apesar de ser condição de lançamento** — a armadilha clássica do WSJF com trabalho grande. Valor 8 + Criticidade 13 + Risco 8 = CoD 29, Job Size 5. Ponteiro (L-30): `docs/tech/contrato-i18n.md`, `GODS_LAWS.md` L-29 corte `C-09`. | Alta | B9, D20 | Média | ⏳ Pendente | — |
| 4,0 | A4 | 2 | A | Corrigir a frase nos dois arquivos de `docs/_secret/` que afirma proteção por `.gitignore` inexistente, descrevendo o mecanismo real (git-crypt via A2 e A3). Sobe com o corpus em B7. | Média | A2, A3 | Baixa | ⏳ Pendente | — |
| 2,0 | D26 | 2 | D | A cena do encontro **Brunus Vetorial × Helion Tusk**. ⚠️ Os dois **não se conhecem** no canon: as ocorrências que cruzam os nomes os ligam por tema, sem biografia nem cronologia compartilhada (`docs/narrative/arco-principal.md`). ⚠️ Respeitar a exceção única de vida inteira do Brunus a nunca entrar em luta, e não confundir o item com o segredo de longevidade dele. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Média | D25 | Média | ⏳ Pendente | — |
| 2,0 | A7 | 2 | A | `REUSE.toml` com regras por diretório: código AGPL, asset reservado, fronteira jurídica do catálogo conforme L-25. Precisa passar o lint do REUSE. **AUDITADO em 25/08/2026** por Cláudio (CLO) e **REPROVADO**, com os três achados confirmados por mim no shell que ele não tinha. **(1)** `tools/security/termos_proibidos.txt` está *tracked* sem anotação, sem `.license` e sem cabeçalho possível (é cifrado) — `reuse lint` reprova. **(2) O grave:** `REUSE.toml` declara `resources/**` como copyright de Petrus, todos os direitos reservados, e isso **alcança as duas fontes `PixelOperatorMono`, que são de Jayvee Enaguas sob CC0** — o repositório afirma titularidade sobre obra alheia em metadado legível por máquina, e **contradiz o próprio `THIRD-PARTY-LICENSES.md`**. CC0 permite redistribuir sob qualquer termo, então não há ilícito; há misdeclaração de proveniência, que é exatamente o que o REUSE existe para impedir. **(3)** `.bigtech-porte` também está *tracked* e descoberto. Conserto em `A14`. ⚠️ A claim "precisa passar o lint do REUSE" **não se sustenta**, e a ferramenta `reuse` **não está instalada** nesta máquina. ⚠️ **Defeitos consertados em `A14` na mesma data**, com lint verde e SPDX por arquivo conferido. **O item segue `❌ Reprovado` de propósito:** quem promove é auditoria, e o implementador do conserto não pode auditar o próprio trabalho. Reauditar junto de `A14`. ✅ **PROMOVIDO em 25/08/2026, sem reauditoria, por decisão do líder.** Ele perguntou *"por que faria reauditoria?"* e a pergunta estava certa: a regra "implementador ≠ revisor" existe para impedir **autocertificação de julgamento**, e o que houve aqui foi **verificação mecânica por terceiro** — um agente editou, o orquestrador rodou a ferramenta, e `reuse lint`/`reuse spdx` são determinísticos: dão a mesma resposta para quem os rode. Reauditar repetiria os mesmos comandos pelos mesmos números. **Evidência que sustenta a promoção:** `1957 / 1957` com copyright e licença, `Licenças irregulares: 0`, nenhum arquivo descoberto; as duas fontes resolvendo para `CC0-1.0` com crédito a Enaguas; `resumo.pdf` em reservado; `.gw.card` seguindo AGPL como contraprova. | Média | A2, A6 | Média | ✅ Concluído | ✓ |
| 1,8 | D29 | 2 | D | O trecho **jogável** entre o achado do `D28` e o confronto final, com a mudança de mundo que ele provoca. ⚠️ Derruba só a **barreira técnica**, e **não** a escassez material: bateria, ingrediente e componente seguem escassos, e a lição de destinar recurso escasso fica intacta. ⚠️ **O vilão NÃO está vencido nesse ponto**, ordem expressa do líder. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Média | D28 | Média | ⏳ Pendente | — |
| 1,5 | D24 | 2 | D | **Dicionário de Sylvarin em PDF** em `docs/`, do que a língua tiver depois do `D23`, para o jogador traduzir a inscrição fora do jogo. Alternativa ao site, já prevista pelo líder. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) | Baixa | D23 | Baixa | ⏳ Pendente | — |
| 1,0 | G25 | 2 | G | Fecha os dois achados laterais do §9 de `docs/_processo/lista-dep-removida.md` que a decisão de `G1` (30/08/2026) explicitamente NÃO resolveu: **(1)** o `ADR-008` seguia marcado `Accepted` quando deveria ser `Superseded`; **(2)** dois nomes de arquivo corrompidos pelo marcador `__DEP_REMOVIDA__` que a busca por string não pega — o arquivo do `ADR-008`, com o nome corrompido e `difficulty_menu_____.hpp/.cpp` (citado dentro de `docs/design/mecanicas/modos-morte.md:166`). ⚠️ **Medido na mesma auditoria de 30/08/2026 que fechou `G1`, e registrado aqui sem fechar o item por conta própria (L-11 do projeto, L-01 global):** os dois PARECEM já resolvidos, como efeito colateral da extirpação em massa de `B1b` (commit `e2fcc6e`, 28/08/2026), não por trabalho deste item. `find` por `ADR-008` na árvore não devolve nada — o arquivo (e a variante de nome corrompido) foi **apagado inteiro**, não só renomeado, e já está listado entre os "Apagados" da própria linha de `B1b` nesta tabela; não sobra arquivo nenhum para carregar o status errado. E `docs/design/mecanicas/modos-morte.md:166` hoje lê limpo — "documento de estilo dedicado (`difficulty_menu.hpp/.cpp`)" — confirmado por `git log -p` no mesmo commit `e2fcc6e`, que já trocou o nome corrompido pelo correto. **Este item fica pendente para CONFIRMAÇÃO de fechamento, não para trabalho de correção**, a menos que uma varredura mais ampla ache outra ocorrência que esta auditoria não pegou. ✅ **CONFIRMADO em 30/08/2026, decisão do líder (`AskUserQuestion`), sem trabalho novo, os dois achados seguem vencidos:** verificação independente 1, `ADR-008`, comando `find docs/tech/adr -iname "ADR-008*"` devolve vazio, confirmando que o arquivo foi apagado inteiro pelo commit `e2fcc6e`, sem sobrar arquivo nenhum para carregar o status `Accepted` errado. Verificação independente 2, nomes corrompidos, comando `grep -n "difficulty_menu" docs/design/mecanicas/modos-morte.md` devolve a linha 166 já limpa, citando `gus/app/screens/difficulty_menu.hpp`, sem o marcador `__DEP_REMOVIDA__`. A resolução veio como efeito do commit `e2fcc6e` (28/08/2026), não de trabalho novo deste item. `Estado Auditado` fica em `—`: a verificação foi feita pelo agente que criou o item e reconferida pelo orquestrador, não por auditoria de C-level `fable` (convenção da Legenda deste arquivo). Ponteiro (L-30): `docs/_processo/lista-dep-removida.md` §9. | Baixa | G1 | Baixa | ✅ Concluído | — |
| 0,6 | B1b | 2 | B | **Fase 2, editar:** reparar caso a caso as ocorrências da lista, **só depois da aprovação em bloco de `G1`**. Fatiar por categoria, não por contagem. ⚠️ **Restaurar sem saber a palavra original é pior que deixar quebrado**: ocorrência sem origem provável volta ao líder em vez de virar chute. **Escopo cresceu de 28 para 45 arquivos** depois que os 20 ADRs foram recuperados do backup em 21/08/2026; a contagem real medida em 25/08/2026 é de **173 ocorrências**, não 28. ⛔ **ORDEM DO LÍDER, 28/08/2026: nenhum nome de dependência de terceiro volta ao corpus, em documento nenhum.** **Nenhum nome de dependência morta volta ao corpus.** Isto **revoga** a leitura anterior desta mesma data, em que eu recomendara restaurar e ele escolhera *"o nome volta"* — a ordem posterior vence, e nada chegou a ser executado: o `B1b`, que é a fase que edita, **nunca foi despachado**. ⚠️ **Não reabrir, não propor restauração, nem que pareça conserto óbvio.** Vale para toda dependência de terceiro já aposentada e seus derivados; os nomes não se escrevem aqui. ✅ **EXECUTADO em 28/08/2026, com escopo que cresceu duas vezes.** Começou em três dependências de terceiro e o líder ampliou para **toda dependência de terceiro**, verbatim: *"extirpe essas pragas daqui do projeto!"*, *"GusWorld APENAS PODE LIGAR COM GLINTFX e o SO! VEJA AS LEIS"*. **Placar medido, não relatado: 216 violações em 37 arquivos → 0, `LIMPO`**, em 603 arquivos varridos. **Apagados**: `ADR-002`, `ADR-003`, `ADR-008`, `ADR-009`, `ADR-011`, `ADR-012`, `ADR-013`, `ADR-014`, `ADR-015`, `ADR-018`, `PORTING-RCSS.md`, `art-spike-protocol.md`, `plano_vs.md`. ⚠️ **O `ADR-015` caiu inteiro por decisão do líder:** ele decidia *"Vendorizar uma biblioteca de criptografia de terceiro, ESCOLHIDA"* — decisão **viva** contra a LEI ZERO, já superada pelo bus (a cripto entra no escopo do GlintFx). Verifiquei antes que **a L-25 já carrega** gravação atômica, cadeia de backups e validador semântico: some o ADR, a lei fica. ⚠️ **`ADR-005` e `ADR-021` foram reescritos, e não isentados, porque o líder derrubou a premissa que eu havia aceitado sem conferir:** eu repetira que releases publicadas travariam o texto por efeito jurídico. **Medi depois dele apontar: zero tags locais, zero no remoto.** Nenhuma release existe. ⚠️ **Achado do próprio agente contra a minha ferramenta:** `RCSS` e `RML` **não são praga** — são a linguagem de estilo e a marcação **nativas do GlintFx**, confirmado no repositório dele. Meu guard os acusava, e ele se recusou a apagar documentação correta sobre o único framework que a lei autoriza. Guard corrigido. **Órfãos das deleções despachados à parte**: `ADR-015` em 7 arquivos, `ADR-014` em 5, `ADR-002` em 4, mais `003`, `008`, `011` e `012` (L-24, mesma regra do `G11`). ⚠️ **Reconferido em 05/09/2026, mesma medição de `G1`:** as quatro categorias que exigiam decisão (`CAT-A`, `CAT-B`, `CAT-C`, `CAT-D`) já não têm ocorrência viva no corpus fora de `docs/_processo` (rastro de auditoria, `CAT-E`, ~110 ocorrências), `GODS_LAWS.md` (1) e desta `TODO.md` (7); o nome da dependência extirpada não voltou a nenhum arquivo de stack, ADR ou crédito. Não marco `✅`: falta auditoria de C-level `fable`. | Alta | G1 | Média | 🔍 Pendente verificação | — |
| 8,5 | A12 | 3 | A | `git remote add origin` e PRIMEIRO COMMIT MÍNIMO, por ordem do líder: só licença e configuração de repositório (`A1` a `A10` mais o gabarito `A11a`; a prática contínua `A11b` não bloqueia e nunca bloqueou). O corpus (`docs/`, `resources/`) NÃO entra aqui; sobe em B7 e B8 depois da limpeza correspondente. **Faixa trocada por lista explícita em 25/08/2026, por decisão do líder:** o `A4` estava dentro da faixa `"A1 a A11"` mas a descrição dele diz que sobe com o corpus em `B7`, ou seja nunca bloqueou este item. Exceção implícita em faixa é a mesma armadilha que escondeu o ciclo `G1`/`B1`. **AUDITADO em 25/08/2026** por Narciso (CISO): **PASSA.** Remoto correto, e o primeiro commit `81a8ef1` levou 16 arquivos de licença e configuração; o corpus entrou depois, nos commits planejados. ⚠️ **Texto emendado em 25/08/2026, por decisão do líder:** o commit inicial inclui também **dois arquivos de `docs/_secret/`**, e isso é **correto, não desvio** — pôr a área cifrada no primeiro commit é justamente o que provou que o filtro do git-crypt já estava de pé antes do primeiro `add`. A frase "o corpus `docs/` NÃO entra" alcança o corpus **legível**; a área cifrada entra junto por desenho. | Alta | A1, A2, A3, A5, A6, A7, A8, A9, A10, A11a | Baixa | ✅ Concluído | ✓ |
| 2,7 | B7 | 3 | B | Commit e push do corpus TEXTUAL limpo (`docs/`, `resources/translations/` e afins), segundo momento de publicação depois do primeiro commit mínimo. Verificar por clone fresco que o diff não expõe o que a limpeza removeu (histórico é público). | Média | A4, B1b, B2, B3, B4 | Baixa | ⏳ Pendente | — |
| 2,0 | A11b | — | A | **Fase 2, a prática:** aplicar o gabarito de `A11a` em **todo arquivo novo, desde o primeiro** (L-08). **Não fecha sozinha por desenho** — atravessa os grupos C, D, E e F e só encerra quando o último arquivo do projeto existir, o que é a razão de ela não ter onda: item sem onda é item que não entra em fila de execução. ⚠️ **Verificada por portão de CI, nunca por marcação manual na tabela** — marcar à mão uma prática contínua é como o antigo `A11` mentia. Ponteiro (L-30): `GODS_LAWS.md` L-08, L-19 (portões). | Média | A11a | Baixa | 🔄 Em andamento | — |
| 1,5 | D19b | 3 | D | **Fatia 2, a prosa longa:** produzir em inglês o texto corrido que o jogador lê — Diário (**54.772 palavras** em 8 arquivos, medido), cartas, diálogos e texto de sabor. ⚠️ **Não é conversão, é REESCRITA:** a matéria do jogo é voz — um menino de 11 anos, o léxico élfico da casa — e inglês ruim danifica a obra mais que inglês ausente. O líder foi avisado disso e confirmou o inglês na 1.0 mesmo assim. ⚠️ **O WSJF baixo (1,5) é correto e não deve ser 'consertado' inflando o número:** Valor 13 + Criticidade 13 + Risco 3 = CoD 29, mas Job Size **20**. Ele é urgente E enorme; o que o método manda é fatiar mais, por sistema, quando esta fatia for encarada. Depende de `D19a` porque os defeitos do contrato aparecem lá primeiro, mais barato. Ponteiro (L-30): `GODS_LAWS.md` L-29 corte `C-09`; política de trocadilho em `G24`. | Alta | D19a, G24 | Alta | ⏳ Pendente | — |
| 1,3 | B11 | 3 | B | Varredura ampla de **caminho absoluto de máquina** no corpus que vai a público: nenhum arquivo rastreado e não cifrado pode conter o caminho do diretório pessoal do líder. Escopo que o `B3` prometia e não entregou (auditoria do `qa-engineer`, 25/08/2026). **Roda depois de `B1b` e `B4` para não colidir nas mesmas linhas.** ⚠️ **Caso já medido, que prova a necessidade:** o `docs/_processo/AUDIT-T1-NOMES.md` entrou com **12 ocorrências** no commit `bb052d0` de 25/08/2026, três horas depois de o `B3` estar marcado como concluído, e foi consertado no mesmo dia. Aceite: a checagem roda sobre arquivo **rastreado**, e declara explicitamente o que ignora (o `docs/_secret/` cifrado é exceção legítima, L-25, e a fixture do teste do portão de commit também). | Média | B1b, B4 | Baixa | ⏳ Pendente | — |
| 4,4 | C1 | 4 | C | `CMakeLists.txt` raiz, C++23, estrutura das cinco camadas `core`, `content`, `domain`, `app`, `present` (L-17, L-20). Fundação de build, legitimamente sem TDD (L-20); nasce certo para as cinco plataformas. | Alta | A12, G6 | Alta | ⏳ Pendente | — |
| 2,0 | B8 | 4 | B | Commit e push dos assets binários (`resources/` restante: 1266 imagens, sprites, áudio) via Git LFS, depois da conferência visual amostral. | Média | A2, B6, B7 | Baixa | ⏳ Pendente | — |
| 3,0 | C2 | 5 | C | Gate de CI de camadas: qualquer `#include <glintfx/` fora de `present/` derruba o build (L-17). Se dividir o mesmo arquivo de workflow com C4 e C5, sequenciar C2, C4, C5 ou separar workflows desde já. | Média | C1 | Média | ⏳ Pendente | — |
| 3,0 | C6 | 5 | C | `clang-format` base LLVM, indentação 4, colunas 100. | Baixa | C1 | Baixa | ⏳ Pendente | — |
| 2,0 | AUD-4 | 5 | AUD | Auditoria de licença (cap. 5): REUSE/SPDX em todo arquivo, regime de asset reservado com carve-out de marca e permissão de fã, fronteira jurídica do catálogo, livros fora do regime do jogo. Executor: C-level `fable`. ⚠️ **`A11b` deixou de ser pré-requisito em 05/09/2026, por decisão do líder:** prática contínua que por desenho nunca fecha, e portão que espera fechamento nunca dispara. A aderência ao gabarito SPDX é conferida DENTRO desta auditoria, no instante em que ela roda: varredura REUSE sobre a árvore inteira, com contagem `encontrados/analisados/falharam` impressa mesmo quando zero (L-36 global). | Média | A7, B7, B8 | Média | ⏳ Pendente | — |
| 1,7 | C3 | 5 | C | Harness de teste próprio, sem framework de terceiro (LEI ZERO): motor de asserção e execução, descoberta e registro, integração CMake e relatório legível por humano e CI. Pré-requisito literal de todo o grupo D (L-19); maior risco de bloqueio em cadeia, fatiar nas três partes. ⚠️ **O `.claude/tdd-guard.json` foi criado em 28/08/2026**, por ordem do líder, fechando o aviso que a reordenação de 25/08 emitiu (o T1 não tinha hook que o forçasse). Ele está **armado e inerte**: os 1962 arquivos rastreados classificam **zero** como `production`, porque ainda não há C++ — o guard ativa sozinho quando este item e o `C1` criarem o primeiro `.cpp`. Nada a ligar depois. **Semântica que este harness precisa honrar:** estado ausente **bloqueia**; teste vermelho **permite** (é a fase red); tudo verde **bloqueia** (escreva o teste que falha antes); comando quebrado faz **fail-open com aviso**. ⚠️ **O ponto que este item tem de resolver, e que é armadilha real:** o guard chama `ctest --test-dir build`. Enquanto esse diretório não existir com testes registrados, o `ctest` falha, e falha vira **fail-open** — ou seja, **o guard fica silenciosamente inútil**, parecendo armado. O harness só está de pé de verdade quando um `ctest` nesse caminho retornar vermelho e verde de propósito, e isso precisa ser **verificado**, não presumido. Ponteiro (L-30): `.claude/tdd-guard.json`, `docs/tech/adr/ADR-025-harness-teste-proprio-minimo.md`, `GODS_LAWS.md` L-19. | Alta | C1 | Alta | ⏳ Pendente | — |
| 2,0 | C8 | 6 | C | Hook de TDD do projeto (`.claude/tdd-guard.json`, hoje inexistente), encaixado no ciclo vermelho/verde da L-19 contra o harness C3. | Média | C3 | Média | ⏳ Pendente | — |
| 1,1 | C4 | 6 | C | Matriz de CI das cinco plataformas: Fedora 44 pinado (nunca `:latest`), Ubuntu, Arch, CachyOS com job próprio (não é Arch renomeado), Windows. Nenhuma não bloqueante (L-09, L-20). Fatiar por plataforma, Fedora primeiro. | Alta | C1, C2, G6 | Alta | ⏳ Pendente | — |
| 4,5 | TST-3 | 7 | TST | **T12 do `TESTES.md`: busca de CVE nas dependências** (`trivy` + `grype` + consulta a NVD/OSV), acrescentado em 25/08/2026 por decisão do líder na reordenação — era a **única** categoria do catálogo sem item que a exercesse. ⚠️ **Ressalva registrada junto com a decisão, para ninguém superdimensionar:** a única dependência declarada do jogo é o **GlintFx**, que é nosso e tem lei de dependência zero. O valor real não está nele: está na **toolchain e nas bibliotecas de sistema** que as cinco plataformas de CI arrastam, e é aí que `trivy` e `grype` pagam. Roda no CI, depois de a matriz das cinco plataformas existir. Ponteiro (L-30): `TESTES.md` T12. ⚠️ **WSJF recalculado (3,0 → 4,5):** 2 + 2 + 5 = CoD 9, Job Size 2. | Média | C4 | Baixa | ⏳ Pendente | — |
| 0,8 | C5 | 7 | C | Os **cinco** portões da L-19 no CI (a linha dizia "quatro" e ficou para trás quando a lei ganhou o quinto em 22/08; corrigido em 24/08/2026). **Quatro rodam como etapa de build:** zero aviso com `-Werror`; ASan e UBSan em build separado; `clang-tidy` e `cppcheck`; `gitleaks`. **O quinto é de natureza diferente e já está entregue:** o gate de mensagem de commit (`tools/security/commit_gate.py`) é gancho `commit-msg` **local**, instalado por `tools/git-hooks/install.py`, e existe porque o `git-crypt` cifra conteúdo de arquivo mas **não** cifra mensagem de commit, nome de arquivo nem de diretório. O que falta dele aqui não é reimplementar: é (a) rodar o teste próprio dele (`tools/security/test_commit_gate.py`, verde hoje) como etapa do CI, para gate quebrado não passar despercebido, e (b) uma etapa que **falhe** se o gancho não estiver instalado no clone — gancho vive na árvore de trabalho e não se instala sozinho. Integrar ferramenta a ferramenta, cada uma verde antes da próxima, nas cinco plataformas. | Alta | C4 | Alta | ⏳ Pendente | — |
| 7,0 | AUD-6 | 8 | AUD | **Auditoria dos cinco portões de qualidade da L-19** (`AUDITORIAS.md` **cap. 1**), criada em 25/08/2026 por decisão do líder: era o **único capítulo do manual sem item de auditoria**, e a lacuna foi achada ao higienizar os pré-requisitos (`B15`). Confere que os cinco portões **de fato rodam**: zero aviso de compilação com `-Werror`, ASan e UBSan em build separado, `clang-tidy` e `cppcheck`, scan de segredo por `gitleaks`, e o **gate de mensagem de commit**. ⚠️ **O portão 5 exige método próprio, e é a razão de este item existir:** o próprio `AUDITORIAS.md` diz que *"os quatro primeiros deixam rastro no CI, então basta ler o log"* — o quinto é gancho `commit-msg` **local**, e **nenhum log de CI o prova**. Um clone sem o gancho instalado passa pelos outros quatro e mente sobre o quinto. **Automação não se autocertifica**, e é por isso que o `C5` implementá-los não substitui auditá-los. ⚠️ Para dado sensível, a verificação correta varre o **histórico** (`git log --all -p` filtrado por `grep -i`), **nunca** `git grep`, que só olha a árvore — e o `-i` não é opcional. Ponteiro (L-30): `AUDITORIAS.md` cap. 1, `GODS_LAWS.md` L-19. ⚠️ **WSJF recalculado (6,0 → 7,0):** o portão 5 não deixa rastro no CI, então sem esta auditoria **ninguém sabe se ele roda**. 5 + 3 + 13 = CoD 21, Job Size 3. | Alta | C5 | Média | ⏳ Pendente | — |
| 5,0 | D1 | 8 | D | `core`: tipos básicos, identificadores, resultado e erro; cada conceito é átomo em arquivo próprio (L-04). TDD estrito começa aqui (L-19): saída real do vermelho antes do verde em toda fatia. | Alta | C2, C3, C5 | Média | ⏳ Pendente | — |
| 2,0 | AUD-2 | 8 | AUD | Auditoria de plataforma (cap. 3): matriz das cinco entradas verde, Fedora pinado, nenhum suporte declarado por tabela emprestada (CachyOS não é coberto pelo job de Arch). Executor: C-level `fable`. | Média | C4, C5 | Média | ⏳ Pendente | — |
| 1,0 | C7 | 8 | C | Script local que espelha o CI, rodado antes do push (L-19): reproduz os **cinco** portões (a linha dizia "quatro", mesmo defeito de C5, corrigido em 24/08/2026) e a lógica multiplataforma, com variante POSIX e Windows. Do quinto portão, o que ele reproduz é o **teste** do gate de mensagem de commit e a checagem de que o gancho está instalado — o gate em si já roda a cada `git commit`, não precisa ser invocado aqui. | Média | C2, C4, C5, C6 | Alta | ⏳ Pendente | — |
| 7,7 | D2 | 9 | D | `core`: sorteio determinístico CONTADO, como parte do estado (semente mais contador), nunca global (L-17). Se nascer global por engano, D5, D13, D14 e F2 precisam ser reescritos e o replay fica inválido retroativamente. | Alta | D1 | Média | ⏳ Pendente | — |
| 4,2 | D3 | 9 | D | POCOs de carta: carta de catálogo em `content/`, instância e estado físico (bateria, origem ROM/EPROM/pirata, infecção) em `domain/`, com zero-value seguro; separar fisicamente as duas camadas do L-17. Aceite: contrato comando/evento (ex-D12). | Alta | D1 | Média | ⏳ Pendente | — |
| 3,0 | D9 | 9 | D | `domain`: diálogo como dado. A forma de armazenamento **já está decidida** e não depende mais de B5: fonte em `.gw.text` no repositório, compilada em pacote binário selado (G2, L-25); o jogo lê só o binário. Consome a especificação escrita em B9. Aceite: ex-D12. | Média | D1, B9 | Média | ⏳ Pendente | — |
| 2,6 | D10 | 9 | D | `domain`: mapa como dado, grade quadrada, colisão por célula e marcação de bloco atravessável (L-26). Gateia F1. Aceite: ex-D12. Regra de save por local que depende do mapa: `docs/design/mecanicas/save-por-local.md`. **Reordenação de 25/08/2026:** o contrato do formato de mapa que o GlintFx congelou em 22/08/2026 (volumes com `sensor` e `enabled`, `blocks_path` separado de `blocks_move`, UUID com destino de teleporte, propriedades nomeadas, camada única na v1, preservação de bloco desconhecido) passa a ser consumido via B10, pré-requisito acrescentado; convenção de extensão em `docs/tech/convencao-formatos-gw.md`. | Média | D1, B10 | Média | ⏳ Pendente | — |
| 3,0 | G14 | 10 | G | Decisão do líder, a resolver NA ONDA DE COMBATE, não antes: ativação de ator no relógio de ação consome uma ação por vez, ou até três AP num loop de multi-ação? Os dois modelos coexistem, incompatíveis, em quatro pontos de `docs/design/mecanicas/combat.md` (§3 linha 35, §5 linha 187, §4 "Por-ator" linha 221) e na própria lacuna já registrada em `D5` do TODO.md. Gateia o fechamento fino de `D5`. NÃO DESPACHAR antes da onda de combate. Ponteiro (L-30): `docs/design/mecanicas/combat.md` e TODO.md item `D5`. ⚠️ **Onda fixada À MÃO em 28/08/2026, por decisão do líder — NÃO 'consertar' na próxima reordenação.** Este item não tem pré-requisito, então o cálculo topológico o joga na onda 0 e ele passa a impedir que a onda 0 feche, esperando uma decisão que o próprio líder adiou. A onda aqui declara **quando ele acorda**, não de que ele depende. Alvo: decidir **antes** de o `D5` (máquina de estados de combate, onda 11) ser implementado. A dependência foi fiada de verdade: o `D5` passa a listar `G14` como pré-requisito, então isto não é só número. | Média | Nenhum | Baixa | ⏳ Pendente | — |
| 2,5 | G16 | 10 | G | Decisão do líder, a resolver NA ONDA DE COMBATE: as cinco sinalizações abertas do §5 de `docs/design/mecanicas/modos-morte.md` (item 1, unlock do Hardcore via âncora, ainda não 100% confirmado; item 3, locais reais de respawn do Difícil, dependem do level-designer; item 4, resto do gatilho "Dormir", parcialmente fechado em 25/08/2026 porque dormir virou mecânica própria e custa tempo de relógio, o resto aguarda resposta do Gus Dragon pelo bus na issue 8; item 5, magnitude dos percentuais do Difícil, para a onda de balanceamento; item 7, detalhe do "sobrevive" do kernel-panic puzzle, mesma onda de balanceamento). Paradas até a onda de combate, mesma nota já registrada em `G3`. NÃO DESPACHAR antes. Ponteiro (L-30): `docs/design/mecanicas/modos-morte.md` §5. ⚠️ **Onda fixada À MÃO em 28/08/2026, por decisão do líder — NÃO 'consertar' na próxima reordenação.** Este item não tem pré-requisito, então o cálculo topológico o joga na onda 0 e ele passa a impedir que a onda 0 feche, esperando uma decisão que o próprio líder adiou. A onda aqui declara **quando ele acorda**, não de que ele depende. Alvo: a onda do combate, junto do `G14`. ⚠️ **Sem dependência fiada, e de propósito:** as cinco sinalizações do §5 de `modos-morte.md` não têm um consumidor único e óbvio na tabela, e inventar um pré-requisito falso seria pior que o número manual. | Média | Nenhum | Baixa | ⏳ Pendente | — |
| 2,3 | D4 | 10 | D | `domain`: deck, mão e coleção com os invariantes anti-exploit do canon (instância única com id, mão como lista de ids, descarte só num sentido, venda atômica e idempotente, especial protegida); um ciclo vermelho/verde por invariante. Aceite: ex-D12. | Alta | D3 | Alta | ⏳ Pendente | — |
| 1,1 | D5 | 11 | D | `domain`: máquina de estados de combate, fila de iniciativa, recursos, fórmula de dano, com a ordem de consumo do sorteio fixada; fatiar por subsistema (fórmula e fila antes da máquina que as consome). Primeiro da onda por ter a maior cauda de dependentes (D6, D13, D14, F3, F4). Aceite: ex-D12. **Rota A confirmada pelo líder em 25/08/2026** (parecer do CTO, mesma data): vale o `docs/tech/adr/ADR-017-action-clock-combat-unificado.md`, **relógio de ação por ator**, e não a fila de iniciativa do modelo 1B, que a decisão dele de 16/07/2026 já substituíra. **Contrato do relógio:** instantes inteiros; fórmula de reset em **aritmética inteira**, com regra única de arredondamento declarada; ordem total de desempate **terminando em identificador único de ator**; resolução agendada no mesmo relógio; trava anti-inanição determinística; o relógio **não consome sorteio**. Nada de tempo real, de quadro ou de ordem de contêiner (L-17); o detector permanente é o replay de `D13`. ⚠️ **Pré-condição da L-13, bloqueia a fatia:** reconciliar `combat.md` §3, §4 e §4.1 e `battle-screen.md` D4, que ainda descrevem a fila CTB superada. ⚠️ **Lacuna de spec registrada por decisão do líder, a resolver NA ONDA DE COMBATE:** ativação de ator é **uma ação** (modelo do ADR) ou **três AP com multi-ação** (modelo do `combat.md`)? Os dois são incompatíveis e o item não escolhe sozinho. ⚠️ **Ganhou `G14` como pré-requisito em 28/08/2026:** aquela decisão do líder precede esta implementação, e antes disso a precedência existia só na prosa — máquina nenhuma a lia. | Alta | D2, D3, D4, G14 | Alta | ⏳ Pendente | — |
| 3,0 | F5 | 11 | F | Carta de aposta com odds visíveis, ideia do Gus Dragon (autoria dele). Origem: `ideias1.txt`, issue #1 do bus, commit `93440a5e` de 17/07/2026, verbatim: *"carta RNG de odds VISIVEIS (risco calculado, preserva o pillar 'vence por lógica')"*. O jogador vê a probabilidade ANTES de apostar, então continua vencendo por cálculo, não por sorte. Reaproveita o sorteio determinístico contado de D2 (o mesmo mecanismo de F1 e F2); a carta e a resolução da aposta são domínio, buildable nesta onda. **A revelação da probabilidade NA TELA é `present/`**, bloqueada pelo GlintFx (L-06, L-27) — o domínio só expõe o número calculado como dado, a tradução em UI vem depois. Irmã de F1 (glitch) e F2 (urandom). Busca em `docs/design/mecanicas` não achou spec própria desta carta: **sem documento** (pontua-se contra o contrato já canônico de D2). | Média | D2, D3, D4 | Média | ⏳ Pendente | — |
| 2,7 | F1 | 11 | F | Carta `glitch`, ideia do Gus Dragon (autoria dele): atravessa só blocos marcados pelo designer (D10); duração derivada de tipo de carta, tipo de bateria e carga. **Refinado por ele na issue 3 do bus em 21/08/2026:** só em salas específicas de dungeon; a bateria dela é "de internet": só recarrega em área com cobertura de sinal dentro da dungeon (decisão do Gus Dragon, ratificada pelo líder em 03/09/2026, `cartas-hardware-pirataria-energia.md` §5), e a cobertura de sinal vira insumo de level design; e a regra que governa o desenho, verbatim dele, é *"mais como puzzle, não como arma"*. Inclui a regra do que acontece quando a bateria acaba com o personagem dentro da parede (segunda ideia dele, mesma carta, não duplicada; fechada em G4 em 24/08/2026, ver (a) abaixo). **Comportamento em batalha definido por ele em 22/08/2026 (issue 3):** a carta não causa dano (ou muito pouco) e tem 50/50 de chance de bloquear o golpe do oponente. Nunca ataca, nos dois contextos: fora da batalha atravessa parede, dentro dela interfere. O sorteio usa o contador determinístico de D2, então recarregar o save não re-rola o resultado. **Três decisões do líder, FECHADAS em 24/08/2026 (G4):** **(a) Preso na parede:** a bateria acabando com o personagem dentro da parede **cospe ele de volta**, com preço em dano. ⚠️ **Ordem literal do líder, e é regra de correção, não de sabor: "muito cuidado para não cuspir para o outro lado da parede. Deve cuspir de volta de onde veio"** — sair do outro lado transformaria a falha em atravessador garantido de parede, furando o portão de level design que a carta existe para respeitar (só blocos marcados, D10). O ponto de retorno é o lado por onde ele **entrou**, e o teste de aceite verifica isso explicitamente. Não é recarregar save: é reposicionar a entidade, sem gravar nem carregar, então não colide com a regra de save por local. **Refinado por dificuldade em 03/09/2026, por `AskUserQuestion` (escolha do líder: "Voltar ao começo da sala; a dungeon só reseta no Hardcore"):** no Fácil, no Médio e no Difícil o jogador é cuspido para o começo da sala; só no Hardcore a dungeon inteira reseta. A autoria das duas saídas é do Gus Dragon, que as propôs juntas na issue 3 do bus, 21/08/2026, verbatim: *"risco ficar preso na parede (pode resetar dungeon como morte em links awakening ou voltar a sala para nao punir muito com variacao de dificuldade)"*; o líder escolheu as duas, distribuídas por dificuldade, exatamente como ele propôs. O gatilho de ficar preso também se expande: sem carga OU sem sinal (a bateria desta carta é "de internet", recarrega só com cobertura de sinal na dungeon, `cartas-hardware-pirataria-energia.md` §5). **(b) Bateria em batalha:** consome, **sem exceção** — segue o padrão canônico do líder de 08/08 ("TODAS as cartas passam por originalidade E degradação da bateria; exceção nunca por omissão"). Ela se limita sozinha, que é o que o enquadramento do Gus pede. **(c) Bloqueio:** fecha em **50/50**, o número do Gus vira canon; só muda se a simulação em massa provar que quebra. A travessia NA TELA fica para `present/`, bloqueada pelo GlintFx. | Média | D3, D4, D10 | Média | ⏳ Pendente | — |
| 2,2 | D17 | 11 | D | `domain`: coleta e loot idempotentes — o baú guarda o estado de já aberto e a segunda coleta é ACEITA e nada faz (idempotência), espelhando na direção contrária o invariante 5 (venda idempotente) de `docs/design/mecanicas/deck-mao-sistema.md` §7. Fecha o bug possível (2) do Gus Dragon (autoria dele, L-07; discussion 7 do bus, 22/08/2026): abrir baú duas vezes, à moda de um caso do Wind Waker, para duplicar item ou dessincronizar hitbox. Os nove invariantes protegem a carta DEPOIS que ela existe; a coleta é o instante em que ela NASCE, e não existia item nenhum de coleta ou loot nesta tabela — achado dele. Distinção canonizada em D16, que não pode ser perdida: carregar quer máquina de estados (recusa o segundo comando), coleta quer idempotência (aceita e nada faz); escolher errado entre as duas é como o bug volta. Hitbox: o volume `sensor`/`enabled` do contrato de mapa (B10) é DERIVADO do estado do baú, nunca comandado. `docs/tech/convencao-formatos-gw.md` já cita este item pelo ID. Aceite: teste de dupla-abertura com estado final idêntico ao de uma coleta só; ex-D12. | Alta | D3, D4, D10 | Média | ⏳ Pendente | — |
| 1,4 | D7 | 11 | D | `domain`: economia (fontes e sumidouros), com a régua do comedimento como propriedade executável. Entra na vaga liberada: cap de WIP 3 nesta onda, D7 é a fila. Aceite: ex-D12. | Média | D3, D4 | Alta | ⏳ Pendente | — |
| 3,0 | D8 | 12 | D | `domain`: progressão de conhecimento e mestria por uso (diferencial anti-grind do canon). Aceite: ex-D12. **Reordenação de 25/08/2026:** ponteiro (L-30) `docs/design/mecanicas/knowledge-progression.md` — o §3 conta mastery por derrota em combate ("Derrotar o inimigo em combate: +1"), por isso D5 entra como pré-requisito (achado da lente de arquitetura). | Média | D1, D3, D4, D5 | Média | ⏳ Pendente | — |
| 2,0 | D6 | 12 | D | `domain`: efeitos de status e o contrato de evento, acoplado à fronteira do combate (interface com D5 coordenada, arquivos vizinhos). Aceite: ex-D12. | Média | D5 | Média | ⏳ Pendente | — |
| 3,0 | F2 | 13 | F | Carta `urandom`, ideia do Gus Dragon (autoria dele): versão pirata com 1 em 3 de dar ruim (número definido por ele), versão original como prêmio da RunaDex; usa o sorteio contado (D2). Sequencial com F1 por tocarem o mesmo catálogo. | Média | D2, D3, D8 | Média | ⏳ Pendente | — |
| 3,0 | F3 | 13 | F | Vírus zip-bomb, ideia do Gus Dragon (autoria dele): a ficha técnica existe no design, falta o efeito de estourar na batalha (regra de dano e status via D5 e D6). O VFX da explosão fica para `present/`, bloqueado pelo GlintFx. | Média | D5, D6 | Média | ⏳ Pendente | — |
| 2,3 | F6 | 13 | F | Ataque duplo cooperativo, ideia do Gus Dragon (autoria dele). Mesma leva de F5: `ideias1.txt`, issue #1 do bus, commit `93440a5e` de 17/07/2026, verbatim: *"acao cooperativa de 2 atores, consome o relogio dos 2"*, classificada por ele textualmente como **"a maior das 4"**. Dois membros da party combinam uma ação só, que consome o relógio de ação dos DOIS (D5). `docs/tech/adr/ADR-017-action-clock-combat-unificado.md` foi lido: ele especifica o `ActionClock` que esta ação consumiria (escalonador por-ator, `next_action_at`, ticks inteiros), mas **não** especifica uma ação cooperativa de dois atores — essa peça fica **sem documento**; o ADR-017 entra só como o contrato de relógio que ela tem de respeitar (L-30, precedência 3, parcial). Aceite: ex-D12. **Contrato de encaixe no relógio (parecer do CTO, 25/08/2026, com a rota A confirmada pelo líder):** a ação declara **dois executores**; a pré-condição é ambos prontos, e o mecanismo de segurar do ADR-017 já sincroniza; resolve **uma vez** (um consumo de sorteio, uma entrada de log) e o reset do relógio se aplica a **cada** executor pela própria fórmula. É extensão **aditiva**: não muda chave, desempate nem fórmula. Abertas para a onda de combate, decisão do líder: estilo único ou por executor; de quem sai o custo; reset simétrico ou por papel. | Média | D5, D6 | Alta | ⏳ Pendente | — |
| 2,5 | G15 | 14 | G | Decisão do líder, a resolver NA ONDA DE COMBATE: fatiar `D15` entregando a cena de combate sem save/load encurtaria o caminho até regra jogável demonstrável de 19 para cerca de 7 itens (lente de arquitetura), mas as duas metades podem só fazer sentido juntas, e o corte cria o acoplamento sem fronteira que a L-33 proíbe. Decidir com o código à vista, não antes. Ponteiro (L-30): TODO.md item `D15`. ⚠️ **Onda fixada À MÃO em 28/08/2026, por decisão do líder — NÃO 'consertar' na próxima reordenação.** Este item não tem pré-requisito, então o cálculo topológico o joga na onda 0 e ele passa a impedir que a onda 0 feche, esperando uma decisão que o próprio líder adiou. A onda aqui declara **quando ele acorda**, não de que ele depende. Alvo: decidir **antes** de o `D15` (camada `app/`, onda 15) ser implementado, já que a decisão é sobre fatiar o próprio `D15`. A dependência foi fiada: o `D15` passa a listar `G15` como pré-requisito. | Média | Nenhum | Baixa | ⏳ Pendente | — |
| 1,9 | D11 | 14 | D | `domain`: modelo de save como estado canônico serializável, agregando D3 a D10 e as cartas F1 a F3; o esquema antigo do corpus (a engine anterior/a linguagem de script da engine anterior) não é base, re-derivar da forma real dos sistemas. Aceite: ex-D12. Política de save por local (cidade livre, dungeon restrita): `docs/design/mecanicas/save-por-local.md`. ⚠️ **Faixa expandida em 25/08/2026 (`B15`):** a coluna dizia `D3aD10`, que não é ID e nenhuma checagem mecânica lia. A expansão é literal, não presumida — a própria descrição diz *"agregando D3 a D10"*, e os oito existem. | Alta | D3, D4, D5, D6, D7, D8, D9, D10, F1, F2, F3 | Alta | ⏳ Pendente | — |
| 2,6 | E1 | 15 | E | Envelope binário do projeto (magia, versão de formato, tipo, nonce, dado, selo), servindo save, configuração, mapa e pacote de conteúdo (L-25). A estrutura é redigível hoje; o SELO depende de criptografia do GlintFx, inexistente e sem data: nessa parte o item fica bloqueado por terceiro, esperando a resposta do bus (E7), sem dublê (L-05). Porta de mão única depois que existir save de jogador. | Alta | D11, E7 | Alta | ⏳ Pendente | — |
| 2,0 | D15 | 15 | D | Camada `app/` exigida pela L-17: fluxo de cena, avanço de turno, coordenação entre domínios, save e load como caso de uso. Despachante FINO: cada cena recebe só os sistemas que usa; proibido contexto universal dono de tudo (armadilha 1 da L-17). F4 e TST-2 dependem dela. **Pergunta registrada por decisão do líder em 25/08/2026, a resolver NA ONDA DE COMBATE, não antes:** fatiar este item, entregando a cena de combate sem `save`/`load`, encurtaria o caminho até regra jogável demonstrável de 19 para cerca de 7 itens (medição da lente de arquitetura). Contra-argumento a pesar na hora: as duas metades podem só fazer sentido juntas, e aí o corte cria o acoplamento sem fronteira que a L-33 proíbe. Decidir com o código à vista. ⚠️ **Faixa expandida em 25/08/2026 (`B15`):** a coluna dizia `D5aD11`. Os sete existem. Nota: como o `D11` já depende de `D3` a `D10`, o fecho transitivo desta lista alcança `D3` e `D4` sem os declarar aqui. ⚠️ **Ganhou `G15` como pré-requisito em 28/08/2026:** aquela decisão do líder precede esta implementação, e antes disso a precedência existia só na prosa — máquina nenhuma a lia. | Alta | D5, D6, D7, D8, D9, D10, D11, G15 | Alta | ⏳ Pendente | — |
| 3,0 | D16 | 16 | D | `app`: **carregar é ESTADO, não bandeira** — a máquina de carregamento recusa por construção um segundo comando de carregar enquanto o primeiro está em voo, e a recusa é transição de `aplica(estado, comando)`, não `if` espalhado no chamador (L-17). Fecha a família de reentrância: dois saves abertos ao mesmo tempo, configuração de um aplicada sobre o outro, duas telas empilhadas. **Bug possível (1) do Gus Dragon, autoria dele** (discussion 7 do bus, 22/08/2026); a metade da biblioteca chegou marcada com o nosso nome pelo GlintFx em 24/08/2026 (`inbox/gusworld/archive/20260824-1945-glintfx-metade-do-bug-1-que-e-de-voces.md`), e a L-35 deles garante entrega de evento sem duplicar nem reordenar — logo o guarda assume **um comando é um evento** e não precisa desconfiar da camada de baixo. Aceite: teste que dispara o segundo comando no meio do primeiro e exige estado final idêntico ao de um comando só; `D13` pega regressão como divergência de replay. **Distinção que não pode ser perdida:** carregar quer **máquina de estados** (recusa o segundo), coleta quer **idempotência** (aceita e nada faz) — escolher errado é como o bug volta. | Alta | D15 | Média | ⏳ Pendente | — |
| 3,0 | F4 | 16 | F | Missão com relógio correndo de verdade, ideia do Gus Dragon (autoria dele), inclusive durante batalhas, premiando escolha de rota. Núcleo como contador de tick em `app/` e `domain/` (regra pura); o relógio VISÍVEL fica para `present/`, bloqueado pelo GlintFx. ✅ **A decisão G5 fechou em 24/08/2026** e o aceite final está livre: o relógio **congela na batalha no Fácil e no Médio**, e **corre visível no Difícil e no Hardcore**, com prazo mais apertado neste último; fora da batalha corre nos quatro modos. Custo assumido e escrito em `missoes-cronometradas.md`: nos dois modos inferiores, "encarar inimigo custa tempo" deixa de valer. | Média | D5, D10, D15 | Média | ⏳ Pendente | — |
| 2,7 | E5 | 16 | E | Configuração selada que NUNCA impede o jogo de abrir: selo inválido cai em padrão de fábrica com aviso, jamais recusa de boot (L-25). O caminho de fallback é escrevível já; a checagem do selo real herda o bloqueio do GlintFx. | Média | E1 | Média | ⏳ Pendente | — |
| 2,0 | D13 | 16 | D | Teste de replay determinístico permanente: semente mais lista de comandos reproduz o estado final byte a byte (L-17); detector das armadilhas de contêiner não ordenado e ponto flutuante em fórmula. | Alta | D2, D5, D11, D15 | Alta | ⏳ Pendente | — |
| 2,0 | E6 | 16 | E | Ferramenta de inspeção e re-selo para desenvolvimento: nasce JUNTO com o formato, na mesma fatia de E1, não depois (L-25). O re-selo real herda o bloqueio de criptografia do GlintFx, sem data. ⚠️ **Coluna limpa em 25/08/2026 (`B15`):** dizia `E1 (mesma fatia)`; o ID é real, o parêntese é que a tornava ilegível por máquina. **A informação não se perdeu:** esta ferramenta nasce na MESMA fatia do `E1`, junto com o formato, e é isso que o resto desta descrição afirma. | Média | E1 | Alta | ⏳ Pendente | — |
| 2,0 | E11 | — | E | Amarra de máquina só no slot Hardcore, com aviso ao jogador; save normal viaja com o dono (L-25). ✅ **A decisão G3 fechou em 24/08/2026** e deixou de bloquear: o desenho dos modos de morte sobrevive inteiro (§1 a §5 de `modos-morte.md`), e o Hardcore mantém o quebra-cabeça de última chance. **Bloqueio restante: só a criptografia do GlintFx.** | Baixa | E1 | Média | ⏳ Pendente | — |
| 1,0 | E2 | 16 | E | Serialização binária versionada dos POCOs de domínio, escrita em casa, com round-trip byte-exato por átomo. ⚠️ **Faixa expandida em 25/08/2026 (`B15`):** a coluna dizia `D3aD11`. Serializar os POCOs de domínio alcança os nove. | Média | D3, D4, D5, D6, D7, D8, D9, D10, D11, E1 | Alta | ⏳ Pendente | — |
| 2,0 | AUD-1 | 17 | AUD | Auditoria de arquitetura e camadas (AUDITORIAS.md cap. 2): dependência só para baixo, as cinco armadilhas da L-17, determinismo do replay como evidência. Executor: C-level `fable` (L-10). | Média | D13, D15 | Média | ⏳ Pendente | — |
| 1,6 | E4 | 17 | E | Gravação atômica e cadeia de backups (escrever em temporário, mover, rotacionar), com atenção à divergência de rename atômico entre os cinco SOs (L-09). | Média | E1, E2 | Alta | ⏳ Pendente | — |
| 1,6 | E8a | 17 | E | Metade escrevível do gerador de build: compila a fonte de conteúdo em texto do repositório (tabelas de número e regra em `.gw.table`, mais o leitor de `.gw.text` do `D9`) em tabelas embutidas no executável, lado AGPL da fronteira; nasce do fatiamento do `E8` decidido pelo líder em 05/09/2026, mesmo tratamento de `E1`, `E5` e `E6`. Ponteiro (L-30): `docs/tech/convencao-formatos-gw.md`; `G2`; `B9`. WSJF herdado do `E8` (1,6), porque a tabela só guarda o quociente dele e nenhuma lente mediu as metades. | Média | D3, D9, E1, E2 | Média | ⏳ Pendente | — |
| 1,6 | E8b | — | E | Metade selada do gerador de build: empacota texto de sabor e asset reservado no pacote binário selado (fronteira jurídica da L-25) sobre as tabelas do `E8a` e o envelope do `E1`; bloqueado por terceiro, só a criptografia do GlintFx (pedido `E7` no bus), sem data, dublê proibido (L-05). Ponteiro (L-30): L-25; `E1`. WSJF herdado do `E8` (1,6). | Média | E8a, E1 | Alta | ⏳ Pendente | — |
| 1,4 | E3 | 17 | E | Validador semântico: o estado carregado é alcançável pelas regras; único nível de defesa que sobrevive à chave vazada (L-25). Não depende de criptografia. ⚠️ **Faixa expandida em 25/08/2026 (`B15`):** a coluna dizia `D3aD11`. O validador confere se o estado é alcançável **pelas regras**, logo depende de todas as nove. | Média | E2, D3, D4, D5, D6, D7, D8, D9, D10, D11 | Alta | ⏳ Pendente | — |
| 1,0 | TST-2 | 17 | TST | Teste de integração do núcleo via `app/`: turno completo atravessando combate, economia, progressão e save/load, sem GlintFx (regra pura, permitido pela L-05). Derivado de TESTES.md T14, podado para este stack. | Média | D13, D15 | Média | ⏳ Pendente | — |
| 0,5 | D14 | 17 | D | Auto-resolve de combate para balanceamento em massa: harness de simulação em lote sobre o determinismo de D13. Critério de estudo por simulação pré-registrado antes do dado existir. | Baixa | D5, D13 | Alta | ⏳ Pendente | — |
| 1,2 | TST-1 | 18 | TST | Fuzzing dos parsers binários (envelope, serialização, mapa, save): entrada corrompida ou hostil nunca derruba o jogo nem passa pelo validador semântico. Derivado de TESTES.md T3; T5, T6, T9, T10 e T11 PODADOS para este stack (sem SQL, sem rede, sem API, dependência só GlintFx). | Média | E2, E3 | Alta | ⏳ Pendente | — |
| 1,2 | D31 | 18 | D | **Calibração por simulação** do ato final, ordem expressa do líder: *"Precisaremos calibrar bem e simular isso ainda"*. Mede-se o **conjunto**, nunca peça isolada, porque três efeitos empilham poder no mesmo ato. Critério **pré-registrado antes de o dado existir** (piso de relevância, faixa-alvo e vetos), e o **relatório** é o entregável, não só o motor. Detalhe em `docs/_secret/dungeons/` (cifrado, L-25 — só o caminho é citável) **Onda 17→18 em 05/09/2026:** dependia de `D14`, que está na onda 17. | Alta | D14, D28, D29 | Alta | ⏳ Pendente | — |
| 0,6 | E9 | — | E | Fase 2 da L-25: save híbrido de estado mais registro de comandos com encadeamento, verificável por re-execução (a única peça que não evapora quando o adversário lê o fonte). BLOQUEADO pela fase 1 completa (selo do GlintFx) e pela maturidade de D13. Sem data. ⚠️ **Faixa expandida em 25/08/2026 (`B15`):** a coluna dizia `E1aE6`. | Baixa | E1, E2, E3, E4, E5, E6, D13 | Alta | ⏳ Pendente | — |
| 2,0 | AUD-3 | 19 | AUD | Auditoria de proteção de dado (cap. 4): nada em texto na distribuição, envelope e validador conferidos, fallback de configuração, comunicação pública sem promessa de "impossível editar". Executor: C-level `fable`. ⚠️ **Faixa expandida em 25/08/2026 (`B15`):** a coluna dizia `E1aE6`. Os seis existem. | Média | E1, E2, E3, E4, E5, E6, TST-1 | Média | ⏳ Pendente | — |
| 0,6 | E10 | — | E | Fase 3 da L-25, opcional: âncora anti-rollback por TPM, com retorno à âncora em arquivo onde não houver; NUNCA requisito para jogar. BLOQUEADO por E9 e pela criptografia do GlintFx. Sem data. | Baixa | E9 | Alta | ⏳ Pendente | — |
| 3,0 | AUD-5 | 20 | AUD | Dossiê formal consolidado pelo `internal-auditor`: os **seis** capítulos num livro único com achado, severidade e remediação; PORTÃO DE RELEASE antes da 1.0 (L-19). ⚠️ **Faixa expandida em 25/08/2026 (`B15`):** a coluna dizia `AUD-1aAUD-4`, e este é **portão de release** — a dependência dele dos quatro capítulos não era verificável por máquina nenhuma. ⚠️ **Discrepância achada na mesma passagem, NÃO consertada por ser decisão:** este item fala em *"os cinco capítulos"*, mas o `AUDITORIAS.md` tem **seis**, e os quatro `AUD-*` anteriores cobrem os caps. **2 a 5**. O **cap. 1, os portões automáticos de qualidade da L-19, não tem item de auditoria nenhum** — possivelmente porque é verificado por automação (`C5`) e não por auditor. Decisão do líder. ⚠️ **Corrigido em 25/08/2026:** dizia "os cinco capítulos" para um manual que tem **seis**, porque o `cap. 1` não tinha item; agora tem (`AUD-6`), e ele entra nos pré-requisitos. | Alta | AUD-1, AUD-2, AUD-3, AUD-4, AUD-6 | Média | ⏳ Pendente | — |
| 2,5 | REL-1 | — | REL | **Publicar a release `1.0`**, criada em 25/08/2026 por decisão do líder: o `H1` e o `H2` declaravam "tag de versão" como pré-requisito, e **a tabela não rastreava isso** — dois itens apontando para algo que não existia. Operação **irreversível**, e por isso tem roteiro próprio: as **sete fases do `DEPLOY_CHECKLIST.md`**, a tag no formato **`vA.B.C.D`** da L-23 (o mesmo do GlintFx), e o `AUD-5` como **portão** — o dossiê consolidado tem de estar fechado antes. ⚠️ **Criar tag e fazer merge em `main` por PR exigem aval explícito do líder no contexto** (L-32): aprovação dada antes não vale para a próxima, e nenhum modo autônomo dispensa isto. ⚠️ **Este item não é o fim do trabalho, é o começo do que depende dele:** `H1` (Wiki) e `H2` (documentação para iniciante) só nascem depois. Ponteiro (L-30): `DEPLOY_CHECKLIST.md`, `GODS_LAWS.md` L-23, L-32. ⚠️ **WSJF recalculado (5,0 → 2,5):** o número anterior superestimava — a release é valiosa (Valor 20) mas está a 23 níveis de distância e é grande. 20 + 8 + 5 = CoD 33, Job Size 13. ⚠️ **Onda `—` desde 05/09/2026, por decisão do líder:** publicar a 1.0 pressupõe jogo jogável, que pressupõe `present/`, que espera o GlintFx; herda o bloqueio sem data junto de `H1` e `H2`. O pré-requisito `AUD-5` permanece. | Alta | AUD-5 | Alta | ⏳ Pendente | — |
| 0,8 | H1 | — | H | Wiki do repositório no GitHub, derivada de `docs/`, linkando em vez de duplicar. Herda, via tag e `present/`, o bloqueio sem data do GlintFx. Execução via technical-writer/ux-writer, nunca inline. **PLANTA RECUPERADA em 25/08/2026 do README do projeto anterior** (backup em `/var/tmp/gw_backup_20260821_004052/git_completo`): a wiki antiga tinha **dez páginas, divididas por público**. Em **pt-br, escrita para iniciante**: `Home`, `Sobre o Jogo`, `Como Rodar e Compilar`, `Arquitetura em Visão Geral`, `Roadmap`, `Créditos`. Em **inglês, para contribuidor técnico**: `Home-English`, `Building and Running`, `Tech Stack and Credits`, `Contributing`. ⚠️ **O conteúdo dessas dez páginas está PERDIDO** — a wiki é repositório git separado, foi zerada junto com o repositório em 21/08/2026, e hoje tem só a página automática de boas-vindas; procurado no GitHub, nos objetos inalcançáveis da wiki e no backup, sem resultado. **Causa raiz, e a lição que a wiki nova tem de corrigir: ela nunca teve fonte versionada no repositório.** A wiki nova nasce com fonte em `docs/`, publicada a partir dela. ⚠️ **Coluna limpa em 25/08/2026 (`B15`):** dizia `B7, tag de versão`. ⚠️ **"Tag de versão" é dependência REAL de algo que a tabela não rastreia** — não existe item para criar a tag. Ela sai da coluna e fica dita aqui: este item **não começa antes da tag de versão existir** (L-23), e herda o bloqueio sem data do GlintFx. **Criar item para a tag é decisão do líder.** ⚠️ **Atualizado em 25/08/2026:** a "tag de versão" que esta descrição citava **agora é item** (`REL-1`) e voltou para a coluna, onde máquina a lê. | Baixa | B7, REL-1 | Média | ⏳ Pendente | — |
| 0,5 | H2 | — | H | Documentação `.md` extensa em registro didático para INICIANTE em computação, explicando todo jargão, passo a passo, derivada de `docs/`. Pré-requisito: tag de versão (sem data hoje). Último item por regra permanente da casa; execução via technical-writer/ux-writer. **A trilha de iniciante já tinha forma no projeto anterior** (recuperada em 25/08/2026, ver `H1`): eram as **seis páginas em pt-br** da wiki, e o README as descrevia literalmente como *"escrita para iniciante"* — `Sobre o Jogo`, `Como Rodar e Compilar`, `Arquitetura em Visão Geral`, `Roadmap`, `Créditos`, mais a `Home`. O conteúdo se perdeu; **a divisão por público sobrevive e é o esqueleto a seguir**: pt-br para quem nunca programou, inglês para quem vai contribuir. Não repetir o erro de escrever direto na wiki: fonte em `docs/`, wiki publicada a partir dela. ⚠️ **Coluna limpa em 25/08/2026 (`B15`):** dizia `H1, tag de versão`. A dependência da tag continua valendo e está dita aqui, não na coluna — ver `H1`. ⚠️ **Atualizado em 25/08/2026:** a dependência da tag chega por `H1`, que agora depende de `REL-1`. | Baixa | H1 | Alta | ⏳ Pendente | — |

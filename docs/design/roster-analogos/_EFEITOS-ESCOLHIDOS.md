# Efeitos ESCOLHIDOS das 20 cartas dos mestres (ROSTER-EFEITOS-CARTAS)

> Registro das escolhas do criador (feat ROSTER-EFEITOS-CARTAS, iniciada 2026-07-12). Substitui o rascunho provisório (`cartas-statlines-rascunho.md`) conforme cada mestre é decidido. Quando os 20 estiverem escolhidos, o statline final por-carta fecha (com a baseline de `cartas-technomagik.md` §2.2) e a redação do diálogo do encontro (molde de 2 movimentos, `techmagic.md`) vira handoff do `narrative-writer`.
>
> **Diretriz do encontro (criador 2026-07-12):** no encontro (Morlhin/Tavus-Eco), o mestre (1) conta seus feitos reais em linguagem de leigo, depois (2) deriva: "portanto, o efeito desta carta é [X]". O efeito nunca é arbitrário.

## Lote 1 (mestres 1-4) — decidido 2026-07-12

| # | Mestre | Efeito ESCOLHIDO | Sub-categoria | Observação |
|---|---|---|---|---|
| 1 | **Faraday** | **A: passiva anti-PEM** — anula o PEM das dungeons (habilita save onde há PEM) + imunidade a debuff elétrico/EMP | fora-de-combate | confirmado |
| 2 | **Maxwell** | **Híbrido novo:** PASSIVA = **iluminar áreas escuras** (áreas que só se percorre com esta carta) + ATIVA = "Onda Unificada" (dano elétrico/luminoso em ÁREA, = opção A) | passiva (exploração) + ativa mana~6 | **abre brainstorm novo: MAXWELL-AREAS-ESCURAS** (o que são, onde, como gate contornável não-hard) |
| 3 | **Tesla** | **A: "Descarga em Cadeia"** — raio que salta até 3 inimigos, decaindo a cada salto | ativa mana~6, Elétrico | confirmado |
| 4 | **Volta** | **Leech termodinâmico (novo):** dreno que absorve energia do alvo (%/absoluto a definir) e devolve à party como mana+HP, mas só x% do absoluto drenado (o resto "perdido como calor", 2ª lei da termodinâmica) | ativa (dreno), Utilitário/Elétrico | **NÃO fura carry-over** (é leech, não retenção de mana). **Brainstorm pendente:** o x% de conversão + se absorve % ou absoluto (argumentos de termodinâmica) |

## Lote 2 (mestres 5-8) — decidido 2026-07-12

| # | Mestre | Efeito ESCOLHIDO | Sub-categoria | Observação |
|---|---|---|---|---|
| 5 | **Einstein** | **A: "Dilatação Temporal"** — desacelera 1 inimigo (empurra a próxima ação dele pra depois do próximo turno da party) | ativa mana~6, Cinético | VFX desejado: "imagem ondulada varrendo a tela" (ver pergunta glintfx) |
| 6 | **Newton** | **B: "Poço Gravitacional" + "Ação e Reação"** — ativa puxa/imobiliza todos 1 turno + passiva reflete fração do dano físico | ativa mana~6 + passiva, Cinético | as DUAS leis reais do Newton |
| 7 | **Planck** | **A: "Quantização do Dano"** (passiva) — o dano do Gus cai sempre em degraus fixos/discretos com chance conhecida, elimina quase-acertos | passiva mana-0 | plug técnico na variância `v` (§11) = decisão de engenharia |
| 8 | **Mandelbrot** | **A: "Eco Fractal"** (ativa) — repete a última ação da party nesta rodada a 50% | ativa mana~6 | confirmado (redação ajustada "neste turno" → "nesta rodada", ver AMB-01) |

## Lote 3 (mestres 9-12) — decidido 2026-07-12

| # | Mestre | Efeito ESCOLHIDO | Sub-categoria | Observação |
|---|---|---|---|---|
| 9 | **Euler** | **B: "Ponte de Euler" (passiva atalho) + "Traçar Rota" (ativa, revela o grafo de uma dungeon)** | fora-de-combate (exploração/puzzle) | amarra na topologia de atalhos (seed #7) |
| 10 | **Gödel** | **A: "Sentença Indecidível"** — 1×/batalha, ignora o próximo efeito de imunidade/resistência/bloqueio do inimigo | passiva mana-0, **trunfo fora-da-roda** (`IgnoresWeaknessWheel`) | o trunfo canônico |
| 11 | **Ada Lovelace** | **A: "Repetir o Algoritmo"** — no fim do turno de um aliado, chance de repetir a última ação sem AP (loop) | passiva mana-0 | confirmado |
| 12 | **Turing** | **A: "Cripto-Chave Universal"** — revela cripto-glifos/textos ocultos + destrava cadeados tipo-cifra + acelera puzzles | passiva mana-0 (exploração) | chave-mestra, não específica |

## Lote 4 (mestres 13-16) — decidido 2026-07-12

| # | Mestre | Efeito ESCOLHIDO | Sub-categoria | Observação |
|---|---|---|---|---|
| 13 | **von Neumann** | **B: "Construtor Universal" (passiva: Token consumido volta 1×/combate) + "Molde Fiel" (ativa: clona 1 aliado por N turnos, stats reduzidos, repete a última ação dele)** | passiva + ativa mana~6 | as 2 descobertas reais; nicho unidade-extra |
| 14 | **Giordano Bruno** | **B: eco do próprio Gus** (ativa: invoca um "Gus de outro ramo" temporário, repete a última ação, stats reduzidos) | ativa mana~6 | evita colisão com o clone do von Neumann; imagem forte (dois Gus) |
| 15 | **John Dee** | **B: "Espelho Negro" (passiva: revela ocultos no scan) + "Scrying" (ativa: revela a próxima ação de todos os inimigos por N turnos)** | passiva + ativa mana~6 | foresight, nicho novo |
| 16 | **Pythagoras** | **A: "Hipotenusa"** (passiva) — quando 2 aliados atacam o mesmo alvo, golpe-bônus de potência = √(a²+b²) dos dois (maior que cada, menor que a soma). É o teorema literal | passiva mana-0 (combo) | **VFX definido pelo criador:** traça uma linha vertical + uma horizontal até o inimigo, e o poder sai pela **hipotenusa** (diagonal). Barato (linhas geométricas). |

## Lote 5 (mestres 17-20, os economistas) — decidido 2026-07-12 — **20/20 COMPLETO**

| # | Mestre | Efeito ESCOLHIDO | Sub-categoria | Observação |
|---|---|---|---|---|
| 17 | **Hayek** | **A: passiva de diversidade** — se os membros da party fazem ações DIFERENTES no mesmo turno, bônus escalonado de precisão/dano (ordem espontânea emergente, nunca pune, só premia) | passiva mana-0 | — |
| 18 | **Mises** | **A: passiva dupla** — (1) party aloca melhor (ações custam menos AP / rendem mais) + (2) debuff em inimigos com tag "comando central" (agem com atraso/erro de mira) | passiva mana-0 | precisa da tag nova "comando central" no motor |
| 19 | **Menger** | **B: "Valor Marginal" (passiva: mostra o valor real de cada consumível) + "Escambo Espontâneo" (ativa 1×/encontro: loot excedente → Crédito na hora)** | fora-de-combate (economia) | amarra no Mercado da Sucata Honesta |
| 20 | **Bastiat** | **A: lente do "não-visto"** — revela o CUSTO/CONSEQUÊNCIA oculta: efeitos colaterais ADIADOS de buffs/ataques inimigos, "free" com pegadinha, trade-off invisível | passiva mana-0 (lente causal) | **DIFERENCIAR dos outros reveladores** (criador): John Dee = objetos/passagens ocultas; Turing = cifras/cadeados; **Bastiat = consequência/custo causal oculto**, NÃO objetos físicos |

> **20/20 EFEITOS ESCOLHIDOS.** Próximos passos: (a) statline numérico por-carta (com a baseline de `cartas-technomagik.md` §2.2, agora destravável); (b) os sub-brainstorms abaixo; (c) redação dos diálogos de encontro (`narrative-writer`, molde de 2 movimentos + frase pedagógica).

## Nomes dos efeitos (CARTAS-NOMES-EFEITOS) — em andamento

Estilo: curto, techno-científico, evoca o feito real (o criador deu "Grav-Wave" como MODELO DE ESTILO; note que o nome escolhido do Einstein acabou sendo **"Time-Dilate"**, ver tabela abaixo — "Grav-Wave" ficou só como exemplo de tom, não é o nome final de ninguém). **Princípio do criador:** a explicação/frase da carta deve levar o jogador a ENTENDER o nome do efeito (nome ↔ frase pedagógica amarrados).

| # | Mestre | Nome do efeito |
|---|---|---|
| 1 | Faraday | **EM-Shield** |
| 2 | Maxwell | **Spectra-Wave** |
| 3 | Tesla | **Coil-Arc** |
| 4 | Volta | **Volt-Leech** |
| 5 | Einstein | **Time-Dilate** |
| 6 | Newton | **Force-Law** |
| 7 | Planck | **Quantum-Lock** |
| 8 | Mandelbrot | **Fractal-Echo** |
| 9 | Euler | **Bridge-Walk** |
| 10 | Gödel | **Null-Proof** |
| 11 | Ada Lovelace | **Re-Run** |
| 12 | Turing | **Decrypt-All** |
| 13 | von Neumann | **Fork** |
| 14 | Giordano Bruno | **Echo-Self** |
| 15 | John Dee | **Black-Mirror** |
| 16 | Pythagoras | **Hypotenuse** |
| 17 | Hayek | **Free-Order** |
| 18 | Mises | **Calc-Edge** |
| 19 | Menger | **Barter** |
| 20 | Bastiat | **Hidden-Cost** |

**20/20 NOMES ESCOLHIDOS.** (Nome completo da carta = `cardExec-[figura]`; o nome do efeito/poder é a alcunha acima, ex.: `cardExec-Einstein` = "Time-Dilate".)

## Frases pedagógicas (CARTAS-FRASES-PEDAGOGICAS) — 20/20 REVISADAS pelo criador 2026-07-13

A linha didática que cada carta mostra ao ser usada (desperta curiosidade sobre a figura/lei + ajuda a entender o nome do efeito). Tom aprovado pelo criador (revisão frase-a-frase); **polimento final de prosa = `narrative-writer`** no handoff dos diálogos de encontro. Zero em-dash (texto user-facing).

| Efeito | Frase |
|---|---|
| **EM-Shield** (Faraday) | "Dentro de uma gaiola de metal, nenhum pulso entra nem sai. Faraday provou em 1836: você está blindado." |
| **Spectra-Wave** (Maxwell) | "Maxwell mostrou que luz, eletricidade e magnetismo são a MESMA onda. Por isso a luz também ataca." |
| **Coil-Arc** (Tesla) | "O arco de Tesla salta de inimigo em inimigo, mais fraco a cada salto. Ele sonhava em enviar energia sem fios." |
| **Volt-Leech** (Volta) | "Você drenou energia, mas só parte virou mana e HP. O resto escapou como calor: nenhuma pilha é perfeita (2ª lei da termodinâmica)." |
| **Time-Dilate** (Einstein) | "Einstein descobriu que o tempo é relativo. Do seu referencial, o inimigo passou a se mover em câmera lenta." |
| **Force-Law** (Newton) | "Toda ação tem uma reação igual e oposta (Newton). O golpe que acerta você também volta pra quem o deu." |
| **Quantum-Lock** (Planck) | "A energia não vem em qualquer quantidade, só em pacotes fixos. Planck descobriu isso, e por isso seu dano agora cai em degraus certos." |
| **Fractal-Echo** (Mandelbrot) | "Um fractal repete a mesma forma cada vez menor, sem fim. Sua ação ecoou pela metade." |
| **Bridge-Walk** (Euler) | "As 7 pontes de Königsberg: dava pra cruzar todas sem repetir? Euler resolveu e criou a teoria dos grafos. Eis o seu mapa." |
| **Null-Proof** (Gödel) | "Gödel mostrou que toda lógica tem verdades que ela mesma não alcança. A defesa do inimigo é uma lógica, e sua jogada está fora dela." |
| **Re-Run** (Ada) | "Ada Lovelace escreveu o primeiro programa da história: uma sequência que se repete sozinha. Sua ação rodou de novo." |
| **Decrypt-All** (Turing) | "A máquina de Turing resolve QUALQUER problema com o método certo. Nenhuma cifra resiste." |
| **Fork** (von Neumann) | "von Neumann imaginou uma máquina que se copia sozinha. Sua jogada virou dado, e pôde ser lida outra vez." |
| **Echo-Self** (Bruno) | "Giordano Bruno foi queimado por dizer que existem infinitos mundos. Em um deles vive outro você, e ele veio lutar ao seu lado." |
| **Black-Mirror** (John Dee) | "John Dee consultava um espelho negro para ver o que ainda não tinha acontecido. Agora você vê o próximo golpe do inimigo." |
| **Hypotenuse** (Pythagoras) | "a² + b² = c². Dois golpes viraram um só, maior: a hipotenusa do triângulo que eles formaram." |
| **Free-Order** (Hayek) | "Hayek mostrou que a melhor ordem nasce sozinha, sem um chefe mandando. Sua party rendeu mais porque cada um fez algo diferente." |
| **Calc-Edge** (Mises) | "Sem preço real, ninguém calcula o que vale a pena (Mises). Quem obedece um comando central erra a conta. Você, não." |
| **Barter** (Menger) | "Menger explicou como o dinheiro nasceu da troca direta. Você trocou o que sobrava por Crédito na hora, sem loja." |
| **Hidden-Cost** (Bastiat) | "Bastiat ensinou a ver o que NÃO se vê: todo efeito tem uma consequência escondida. Agora você a enxerga antes que ela te pegue." |

## Log de ambiguidades

**AMB-01 (2026-07-14, implementação TECHMAGIC-EXECUTOR/RepeatLastAction — Mandelbrot+Ada): a janela de memória do eco é "neste turno" ou "nesta rodada"?**

A redação original da linha do Mandelbrot (Lote 2) dizia "repete a última ação da party **neste turno**". Isso é ambíguo em combate por-turno-de-ator (cada ator tem seu próprio turno dentro de uma rodada): "neste turno" poderia significar (a) só a ação do turno IMEDIATAMENTE anterior ao Mandelbrot ser conjurado (janela estreitíssima, quase sempre vazia se o Mandelbrot for o 1º a agir), ou (b) qualquer ação de dano de QUALQUER aliado desde que a rodada corrente começou (janela larga, do tamanho de uma rodada inteira).

**Decisão do criador:** opção (b) — **"nesta rodada"**. Motivo: (1) casa com o precedente já implementado do Hipotenuse/Pythagoras (`round_hits_`, ledger que também vive por-rodada, zerado na mesma fronteira); (2) uma janela do tamanho de "1 turno de 1 ator" tornaria o Mandelbrot quase sempre um no-op (given que ele mesmo precisa gastar seu próprio turno pra conjurar, "o turno anterior" seria o de outro ator, não necessariamente o dele); (3) "rodada" é a unidade de memória mecânica que o motor já usa consistentemente pro sistema de combo cross-ator. A carta do Mandelbrot foi reescrita (linha do Lote 2) de "neste turno" para "nesta rodada"; nenhuma outra redação do roster foi afetada (Ada já dizia "no fim do turno de um aliado", que descreve o GATILHO dela — quando ela dispara — não a largura da memória que ela lê, que é a MESMA janela de rodada do Mandelbrot).

Implementação: `LastActionRecord` (memória de 1 slot, a ÚLTIMA ação de dano de QUALQUER aliado) é zerada em `CombatStateMachine::process_round_end_hooks`, na mesma fronteira de rodada que já zera o `round_hits_` do Hipotenuse. Ver `docs/tech/adr/ADR-016-techmagic-effect-engine-data-driven.md` (addendum MVP step 5).

**AMB-02 (2026-07-15, implementação TECHMAGIC-EXECUTOR/DelayAction — Einstein/Time-Dilate): "empurra a próxima ação dele pra depois do próximo turno da party" traduzido como "fim da fila da rodada"?**

A redação original da linha do Einstein (Lote 2) dizia: desacelera 1 inimigo (empurra a próxima ação dele **pra depois do próximo turno da party**). No motor de combate por-turno-de-ator (fila de iniciativa única, sem "turno da party" como bloco separado do turno de cada inimigo — ver `combat.md` §4/§4.1), essa frase é ambígua entre pelo menos duas traduções mecânicas: (a) empurrar o alvo pro **FIM da fila da rodada corrente** (ele age depois de todos os outros, inclusive os outros inimigos), ou (b) empurrar exatamente **N posições fixas** (um valor arbitrário, desacoplado do tamanho real da fila).

**Decisão do criador (2026-07-15):** opção (a) — **"fim da fila da rodada"**. Motivo: (1) é a leitura mais fiel à intenção narrativa ("a party toda age antes dele" = ele é o último a agir); (2) não depende de um número mágico de posições que precisaria escalar com o tamanho da fila (3 vs 5 combatentes); (3) reusa a primitiva já existente `InitiativeQueue::reorder_actor` sem precisar de uma segunda primitiva "mover pro fim". Decisão complementar: um alvo que **JÁ AGIU nesta rodada** quando a carta é conjurada faz a carta **dissipar** (no-op, não fica "guardada" pra empurrar o alvo na rodada seguinte) — evita um efeito-fantasma que sobrevive além da janela natural da rodada (mesmo racional de "a memória não atravessa fronteira de rodada" já usado em `RepeatLastAction`/AMB-01 e no ledger do Hipotenuse).

O `EffectSpec.magnitude` do motor permanece genérico (`0` = fim da fila; `>0` = N posições fixas, pra uso futuro de outra carta que precise do modo N-posições), mas a Einstein hoje usa exclusivamente `magnitude = 0`.

Implementação: `handle_delay_action` (novo `EffectKind::DelayAction`, ordinal 7) via `InitiativeQueue::reorder_actor` (mesma primitiva do Gambito-Reordenar). Ver `docs/tech/adr/ADR-016-techmagic-effect-engine-data-driven.md` (addendum MVP step 7).

**Addendum AMB-02 (2026-07-15, modo-aliado assimétrico):** decisão complementar do criador — a redação canônica ("desacelera 1 inimigo") descreve o alvo INIMIGO, mas o motor não impedia mirar um aliado (ver PIN de comportamento que existia em `techmagic_delay_test.cpp`, caso 15, antes desta decisão). O criador decidiu que mirar um ALIADO vira o **espelho benéfico**: em vez de atrasar, o aliado **avança** — age no primeiro slot ainda-não-agido logo após o ator atual ("dilata o tempo A FAVOR dele"). Mesmos guards de dissipação dos dois lados (já agiu / é o `current()` / morto / fora da fila). Implementação: `handle_delay_action` ramifica por `is_player_side()` (ver ADR-016, "Addendum ao addendum"). **Pendente (conteúdo, não código):** a frase pedagógica canônica do Time-Dilate ("Einstein descobriu que o tempo é relativo...") narra o caso INIMIGO; falta uma variante pedagógica pro caso ALIADO — handoff futuro pro `narrative-writer` (mesmo racional de CARTAS-FRASES-PEDAGOGICAS abaixo), não escrita aqui.

**AMB-03 (2026-07-15, implementação TECHMAGIC-EXECUTOR/Balde B — Faraday/EM-Shield): a redação "imunidade a debuff elétrico" cobre buffs elétricos e debuffs de fora do Elétrico? E o que fazer com um cast em alvo inimigo?**

A redação canônica do Faraday (posse-only, anti-PEM + "imunidade a debuff elétrico") não especificava mecanicamente: (1) se o filtro é por *família de origem do status* (`Eletrico`) ou por *família da carta que causou o dano junto*; (2) se um BUFF elétrico (ex. um Shield concedido por uma carta Elétrica) também seria bloqueado; (3) se a blindagem só *previne* debuffs futuros ou também *limpa* os já ativos no momento em que é conjurada; (4) o que a carta faz mecanicamente hoje que ela ganhou uma face de combate castável (F-3) além da posse-only original — inclusive quando conjurada num alvo INIMIGO.

**Decisões do criador (2026-07-15, F-1..F-4):**
- **F-1:** o portão bloqueia SÓ debuffs — `!CombatActor::is_buff(status.id)` — de `family_origin == CardFamily::Eletrico`. Buffs elétricos (ex. um Shield concedido por carta Elétrica) e debuffs de qualquer outra família passam direto. `family_origin` é o campo já existente em `StatusEffect` (a família da CARTA que originou o status, não da carta que eventualmente causou dano junto — mesmo campo que `handle_apply_status` já preenchia com `card.family`).
- **F-2 "previne + limpa":** aplicar `BlindagemEM` também remove os debuffs elétricos JÁ ativos no alvo no momento do cast, não só previne os futuros.
- **F-3:** Faraday vira **Híbrida** — ganha uma face de combate castável (`OnCast` aplica `BlindagemEM`, mana `kActiveManaCost`, `side_filter=AllyOnly` incluindo self, duração 3, `Refresh`). Sujeita ao mesmo gate 1×/batalha das outras Ativa/Híbrida. A face fora-de-combate original (anti-PEM, posse-only) **NÃO** foi tocada — segue sem programa, feat futura (nome mecânico ainda pendente de wiring com `project_save_dungeon_pem_faraday`).
- **F-4:** cast em alvo INIMIGO dissipa (a carta não "vaza" benefício pro lado oposto), nunca lança — mesmo racional de dissipação-como-estado-normal já usado em `DelayAction`/AMB-02.

Decisão técnica derivada (não fórmula/curva, escolha de implementação já registrada no ADR): o filtro de lado (F-4) virou a primitiva genérica `SideFilter` (`Any`/`EnemyOnly`/`AllyOnly`) em `EffectSpec`, reusável por qualquer carta futura que precise restringir o alvo por lado de forma declarativa — o PR2 (Newton) já herda essa primitiva pronta.

Implementação: `StatusId::BlindagemEM` (ordinal 16), `SideFilter` (`combat_enums.hpp`), `CombatActor::try_add_status` (portão de imunidade, choke point único de status ofensivo — `add_status` legado vira wrapper). Ver `docs/tech/adr/ADR-016-techmagic-effect-engine-data-driven.md` (addendum "Balde B, PR1").

**AMB-04 (2026-07-15, implementação TECHMAGIC-EXECUTOR/Balde B, PR2 — Newton/Force-Law + regra geral "fogo amigo"): o Poço Gravitacional atinge 1 alvo ou o grupo? O que a passiva "Ação e Reação" faz quando conjurada num aliado? E o achado colateral — cartas de dano miradas num aliado machucavam o próprio time?**

A redação canônica do Newton ("ativa puxa/imobiliza todos 1 turno + passiva reflete fração do dano físico") já sugeria AoE ("todos"), mas a implementação MVP anterior tinha o Poço como single-target (herdado do template genérico de carta especial) e a passiva só existia na face `OnDamageReceived` (equipada) — sem face `OnCast` conjurável. Faltava decidir: (1) o Poço é 1 alvo ou o grupo inteiro de inimigos; (2) o dano-de-ataque que acompanha o Poço também é AoE; (3) o que Newton faz quando o jogador mira um ALIADO (mesmo padrão de modo-aliado já resolvido pro Faraday/AMB-03 e pro Einstein/AMB-02); (4) se o dono da passiva-própria TAMBÉM ganhar o Reflect-status (ex. auto-cast), as duas fontes empilham ou uma sobrepõe a outra.

Durante a auditoria de implementação, apareceu um achado colateral mais sério: `resolve_use_card` somava o ATK do conjurador no dano-base **mesmo quando o alvo era do PRÓPRIO time** (a fórmula não checava lado) — isso já quebrava silenciosamente os modos-aliado existentes (Einstein/Faraday), que são pensados como BENEFÍCIO: mirar um aliado com essas cartas também o machucava pelo dano-de-ataque, um bug pré-existente exposto agora que o Newton ia introduzir um TERCEIRO modo-aliado. Corrigido como regra GERAL do motor (não só do Newton).

**Decisões do criador (2026-07-15, N-1..N-4 + regra geral):**
- **Regra geral — fogo amigo DESLIGADO:** nenhuma carta causa dano-base num alvo do PRÓPRIO time (o dano vira 0 nesse alvo, sem sortear RNG); só o efeito declarativo (status/OnCast) roda. Corrige retroativamente Einstein e Faraday em modo-aliado (nenhum dos dois deveria nunca ter causado dano no beneficiário).
- **N-1:** o Poço Gravitacional é **AoE** — imobiliza (Stun 1 turno) TODOS os inimigos vivos do grupo, `side_filter=EnemyOnly`. O dano-de-ataque que sai da fórmula-base (o `power` da carta segue 0, só o ATK do conjurador conta) acompanha o mesmo grupo.
- **N-3 (modo-aliado):** mirar um ALIADO com Newton NÃO ataca ninguém — concede **Reflect 30%/3 turnos** ao aliado alvo (`side_filter=AllyOnly`, `ApplyStatus` com `StatusId::Reflect`). Ramo assimétrico do alvo Grupo: se o alvo declarado é do mesmo lado do conjurador, o efeito vira single (só aquele 1 aliado), não o grupo inteiro.
- **N-4:** se o dono da passiva-própria (`OnDamageReceived` → `Reflect`, equipada) TAMBÉM tiver o Reflect-status (ex. um auto-cast, ou outro Newton concedendo o status nele), **as duas fontes SOMAM** (30%+30% = 60% no exemplo de referência) — sem lógica de dedup, cada fonte dispara no seu próprio ponto do código.

Decisão técnica derivada (escolha de implementação, não fórmula/curva nova): a regra geral de fogo amigo e o N-4 não precisaram de `EffectKind` nem `StatusId` novos — reusam `SideFilter` (AMB-03) e o `StatusId::Reflect` (ordinal 15) já existente. A implementação também consertou um achado-dominó independente: `resolve_targets(TargetShape::Grupo)` tinha o lado-alvo HARDCODED (`!is_player_side()`, sempre "os inimigos da party") — se um INIMIGO castasse uma carta Grupo, o alvo virava o PRÓPRIO time dele (o mesmo guard "lado OPOSTO ao ator" que o `ChainDamage`/Tesla já usava corretamente estava faltando aqui). Corrigido pra usar o mesmo racional em todo lugar.

Implementação: `apply_damage_with_hooks` (Reflect-por-status, `take_damage` puro, mesma anti-recursão do Reflect equipado); `resolve_use_card` (guard de fogo amigo no início do loop de alvos); `resolve_targets` (Grupo: lado oposto ao ator + ramo assimétrico ally-single); `master_cards.cpp::newton` (3 `EffectSpec`: Stun EnemyOnly + Reflect-status AllyOnly + Reflect-passiva OnDamageReceived intocada). Ver `docs/tech/adr/ADR-016-techmagic-effect-engine-data-driven.md` (addendum "Balde B, PR2").

**AMB-05 (2026-07-15, implementação TECHMAGIC-EXECUTOR/Balde B, PR3 — Gödel/Null-Proof): o trunfo "ignora a roda de fraqueza" era só de Gödel-carta (`ignores_weakness_wheel`), sempre-ligado. Vira um STATUS que o portador guarda e gasta num hit específico — como conceder, quando consome, e o que fura?**

A redação canônica de Gödel ("teorema da incompletude: existe algo que a lógica normal não alcança") sugeria um trunfo pessoal do MESTRE, não um efeito de carta conjurável — a implementação MVP anterior já capturava isso como a flag `ignores_weakness_wheel=true` na própria carta Gödel (Passiva, sem programa, sem custo de mana), mas a flag nunca tinha WIRING real no resolvedor (era só um campo record-only, decisão adiada pro "resolvedor liga depois", ver `combat_records.hpp`). Faltava decidir: (1) Gödel vira uma carta castável de verdade, ou continua posse-only; (2) se castável, o benefício é permanente (a flag da própria carta) ou um recurso limitado que se gasta; (3) contra o quê o trunfo fura — só o tier Imune (0.0), ou também Resistente (0.66); (4) o trunfo consome sempre que ativo, ou só quando "há algo a furar".

**Decisões do criador (2026-07-15, G-1..G-3):**
- **G-1:** Gödel vira **castável mana-0** (Ativa): aplica um novo status, `StatusId::NullProof`, no PORTADOR — o próprio Gödel ou um ALIADO (`side_filter=AllyOnly`, que inclui self, mesma primitiva de AMB-03/AMB-04). 1×/batalha (mesmo gate `specials_cast_` das outras Ativa/Híbrida).
- **G-2:** o fura-defesa **só queima quando há algo a furar** — no cálculo de dano, se o ATACANTE tem `NullProof` E o multiplicador de fraqueza calculado é `< 1.0` (Resistente OU Imune), o resolvedor força mult `1.0` e CONSOME (remove) o status. Contra Neutro/Fraco (mult `>= 1.0`) o status fica intacto — não é um "consome sempre que ativo".
- **G-3:** fura **os dois tiers abaixo de neutro** — Resistente (0.66) E Imune (0.0), sem distinção. O código não precisa checar QUAL tier: basta `mult < 1.0`.
- **Persistência do `NullProof` (decisão do orquestrador, referendada):** duração-sentinela alta (`dur=99`, `//PLAYTEST`) em vez de um timer curto — a saída real do status é por CONSUMO (o hit que fura remove ele), não por expiração de turnos. Sem tick especial: reusa `remove_status` já existente.

Decisão técnica derivada (escolha de implementação, não fórmula/curva nova): o trunfo ORIGINAL de Gödel (`ignores_weakness_wheel`) ganhou wiring GENÉRICO ao mesmo tempo (qualquer carta futura com a flag sempre resolve mult 1.0) — via INDEPENDENTE do pierce por-status, sem interferir uma na outra. **Limite de cobertura honesto:** o tier Imune (mult 0.0) não é alcançável hoje por nenhuma combinação real de família de carta (achado do TDD, não uma regressão — ver nota já existente em `combat_formula_test.cpp`); os testes cobrem "vs Resistente" (a única combinação real com mult `< 1.0` hoje), que exercita a MESMA branch de código que cobriria Imune quando a flag de imunidade for plugada no futuro.

Implementação: `StatusId::NullProof` (ordinal 17, `combat_enums.hpp`); `master_cards.cpp::godel` (Ativa, `OnCast -> ApplyStatus NullProof AllyOnly`); `resolve_use_card` + `estimate_card_damage` (`combat_state_machine.cpp`, wiring do pierce, gêmeo preview↔real). Ver `docs/tech/adr/ADR-016-techmagic-effect-engine-data-driven.md` (addendum "Balde B, PR3").

**AMB-06 (2026-07-15, implementação TECHMAGIC-EXECUTOR/manifesto item 5 — Planck/Quantum-Lock): "o dano do Gus cai sempre em degraus fixos/discretos com chance conhecida" — quantos degraus, quais chances, e como isso pluga na variância `v` da §11 (decisão de engenharia adiada pela cartas-technomagik.md)?**

A redação escolhida (efeito A do Planck, `_EFEITOS-ESCOLHIDOS.md` linha 22) descrevia a intenção ("dano cai em degraus fixos, elimina quase-acertos") mas deixava em aberto: (1) quantos degraus; (2) as chances são iguais ou ponderadas; (3) os degraus reusam a variância Knowledge (`v`) já existente, ou introduzem uma faixa nova; (4) a quantização muda a média de dano (nerf/buff embutido) ou preserva o balanço atual; (5) o preview mostra os degraus ou só a UI de sempre (min/max contínuo).

**Decisões do criador (2026-07-15, A1/A2/A5/A6):**
- **A1:** **3 degraus** da PRÓPRIA faixa da variância Knowledge — piso `base×(1−v)` / centro `base` / teto `base×(1+v)` (os mesmos `r=0.0`/`r=0.5`/`r=1.0` que já alimentavam `comum_channel_damage`, reusados sem fórmula nova). Chances **25% / 50% / 25%**, FIXAS (`//PLAYTEST`, não evoluem com Knowledge — só a LARGURA dos degraus encolhe, via o `v` de sempre). Média ponderada = `base` → **zero mudança de balance** vs. o canal COMUM contínuo de hoje.
- **A2:** o preview (`estimate_card_damage`) mostra os **3 valores + as 3 chances** (perfect information, Pillar 1/2 — incerteza transparente, nunca opaca).
- **A5:** o mecanismo SUBSTITUI o sorteio contínuo (`rng.NextDouble()`) por um sorteio de degrau (`rng.Next(100)`) — NÃO arredonda por cima nem soma um novo consumo de RNG: canal COMUM com Quantum-Lock continua em **exatamente 2 consumos** (canal + degrau), a MESMA contagem do canal COMUM de sempre; sem a passiva equipada, o caminho fica byte-idêntico (0 mudança).
- **A6:** crítico e FALHA ficam INTACTOS — a quantização só troca o `else` do canal COMUM, nunca os outros dois canais nem o curto-circuito de imunidade.

Decisão técnica derivada (escolha de implementação, não fórmula/curva nova): a quantização NÃO passa pelo dispatcher `techMagic::execute` (diferente de todos os outros `EffectKind`) — pluga DIRETO no resolvedor de dano (`resolve_use_card`) e no preview PURO (`estimate_card_damage`) via um helper de detecção próprio (`quantize_spec_of`), porque não há "alvo do evento" no sentido dos outros efeitos (Leech/Reflect/etc.) — é uma mutação da PRÓPRIA fórmula de dano, não uma reação a um evento. O `EffectSpec` da carta ainda existe no catálogo (`master_cards.cpp::planck`) e ainda precisa de um handler no dispatcher (fail-fast "sem handler = bug"), mas esse handler é um marcador no-op deliberado — o trigger `OnCast` nunca é despachado de fato pra uma Passiva nunca-jogada.

**AMB-07 (2026-07-15, implementação TECHMAGIC-EXECUTOR/manifesto item 6 — John Dee/Black-Mirror): a carta tem DUAS faces no `_EFEITOS-ESCOLHIDOS.md` original (linha 40: "passiva: revela ocultos no scan" + "ativa: revela a próxima ação de todos os inimigos por N turnos") — as duas entram nesta leva? O que acontece se o brain do alvo é o caótico (Patch-Zero)? Qual a duração/cadência da ativa?**

A redação canônica descrevia duas faces (passiva de exploração "Espelho Negro" + ativa de combate "Scrying"), mas faltava decidir: (1) as duas entram nesta implementação ou só uma; (2) a ativa "revela a próxima ação de TODOS os inimigos" é um dump único ou precisa se renovar; (3) o Patch-Zero (brain caótico, `IntentPreview.is_chaotic`) é uma exceção que a carta consegue furar, como o Gödel fura a roda de fraqueza; (4) mana da ativa.

**Decisões do criador (2026-07-15, D1-D4):**
- **D1 (as DUAS faces entram):**
  - **(i) Mundo** (baús/passagens ocultas na exploração): NÃO existe sistema de ocultos no overworld ainda → **STUB posse-only** (mesmo padrão do Faraday anti-PEM/Euler/Menger — a carta existe no catálogo, a face de mundo fica em comentário/ponto de extensão, ZERO código de combate). A query futura reusa o MESMO mecanismo do item (ii) abaixo (`equipped_special_ids()` contém uma carta com `EffectKind::RevealIntent`?) — nenhum campo/EffectKind novo será necessário quando o sistema de ocultos chegar.
  - **(ii) Scan de combate melhorado:** com a carta EQUIPADA, a ação Scan passa a revelar TAMBÉM: status ativos + posição na fila de iniciativa + o intent previsto do alvo escaneado. Restrição do criador: **só dados que JÁ EXISTEM no modelo** (status list, `InitiativeQueue::index_of`, `IntentPreview` via o mesmo brain do Gambito) — nenhum atributo oculto novo (nada de "atk/def/spd condicionais").
- **D2 (NÃO fura o caos do Patch-Zero):** se o brain do alvo é o caótico (`intent.is_chaotic == true`), a Scrying (tanto o dump da ativa quanto o Scan aprimorado) retorna RUÍDO — loga "sinal embaralhado/não-determinístico", NUNCA revela `predicted_action_id`/`predicted_target_id`/`predicted_damage`. Preserva a one-way door do boss (mesmo racional do `ignores_weakness_wheel` do Gödel NÃO se aplicar a mecânicas fora da roda de fraqueza — aqui, a Scrying não é um trunfo genérico, é telegraph honesto, e telegraph honesto de um sistema caótico É ruído por definição).
- **D3 (duração 3, renova por rodada):** a ativa aplica um buff `StatusId::Scrying` (dur 3, `StackRule::Refresh`) no PRÓPRIO conjurador, via `add_status` LEGADO (fora do portão de imunidade — é buff, não debuff ofensivo). Enquanto QUALQUER aliado vivo portar Scrying, a FSM RE-DUMPA os intents na FRONTEIRA DE RODADA (`CombatStateMachine::process_scrying_hooks`, gêmeo de `process_round_end_hooks`/`process_ally_turn_end_hooks`, mas orientado a STATUS — não a carta equipada, `execute_equipped` nunca despacha isto). Expira normal por tick de duração (mesmo motor de status de sempre, sem código dedicado).
- **D4 (mana padrão):** `kActiveManaCost` (~6), mesmo `//PLAYTEST` das outras Ativa/Híbrida (Volta/Newton/Faraday/Mandelbrot/Einstein).

**Fail-soft assimétrico (achado de implementação, não decisão de design nova):** a Scrying roda em contextos AUTOMÁTICOS (dump em massa no cast, re-dump de rodada) ou como bônus OPCIONAL do Scan — um inimigo sem `IEnemyBrain` no `brain_registry` NÃO pode derrubar o combate inteiro, então esses 3 sítios logam "sem sinal" e seguem (`techmagic.hpp::log_intent_for`). Isso é DELIBERADAMENTE assimétrico vs. o Gambito-Prever manual (`resolve_gambit_predict`), que continua lançando `std::out_of_range` — lá é uma ação DELIBERADA do jogador sobre 1 alvo específico (falha alta e clara é o comportamento certo); aqui é telemetria de fundo que não pode quebrar o resto da rodada.

Implementação: `EffectKind::RevealIntent` (ordinal 9, `combat_enums.hpp`); `StatusId::Scrying` (ordinal 18, `combat_enums.hpp`); `techmagic.cpp::handle_reveal_intent` + `log_intent_for`/`dump_reveal_intent` (funções PÚBLICAS novas, expostas fora do dispatcher `execute` — reusadas pelo re-dump de rodada e pelo Scan aprimorado); `TechMagicContext.brain_registry` (campo aditivo); `CombatStateMachine::process_scrying_hooks` + `resolve_scan` aprimorado + `has_reveal_intent_equipped` (`combat_state_machine.cpp`); `master_cards.cpp::dee` (Híbrida, Universal, `TargetShape::Self`, `OnCast -> RevealIntent`). Ver `docs/tech/adr/ADR-016-techmagic-effect-engine-data-driven.md` (step 8) e `techmagic_reveal_test.cpp` (TESTE-REI de determinismo: HP/dano/ordem-de-fila/contagem-de-RNG byte-idênticos com e sem Scrying ativo).

**Escopo (decisão do criador, fora do motor):** Planck é carta HISTÓRICA — só o Gus a equipa, decisão de progressão/roster, NÃO uma restrição do motor. O motor é agnóstico por-ator (a detecção varre as especiais equipadas do ATACANTE da ação corrente, sem nenhum hardcode de id/nome de personagem), mesmo padrão já usado pelo Reflect (Newton) e o NullProof (Gödel).

Implementação: `EffectKind::DamageQuantize` (ordinal 8, `combat_enums.hpp`); `master_cards.cpp::planck` (Passiva/Universal/mana 0, `OnCast -> DamageQuantize magnitude=50 percent=25`); `quantize_spec_of`/`quantize_step_r`/`quantize_log_suffix` + wiring em `resolve_use_card`/`estimate_card_damage` (`combat_state_machine.cpp`); `handle_damage_quantize` (`techmagic.cpp`, marcador no-op); `CardDamageEstimate` campos aditivos `quantized`/`mid_damage`/`step_low_pct`/`step_mid_pct`/`step_high_pct` (`combat_state_machine.hpp`). Ver `docs/design/mecanicas/combat.md` §11 ("Quantum-Lock (Planck)") e `docs/tech/adr/ADR-016-techmagic-effect-engine-data-driven.md` (addendum "manifesto item 5").

**AMB-08 (2026-07-16, implementação TECHMAGIC-EXECUTOR/manifesto item 12 — Maxwell/Spectra-Wave): a redação da linha (Lote 1) tem DUAS faces ("Híbrido novo: PASSIVA = iluminar áreas escuras + ATIVA = Onda Unificada") — a ativa "dano elétrico/luminoso em ÁREA" precisa de um `EffectKind` novo, e o que fazer com a passiva de exploração que não tem sistema nenhum ainda?**

A redação canônica (`_EFEITOS-ESCOLHIDOS.md`, Lote 1, linha 12) já descrevia a ativa como dano em ÁREA ("= opção A"), mas faltava decidir: (1) a ativa precisa de um `EffectKind`/handler C++ novo, ou o motor já tem uma primitiva de AoE reaproveitável; (2) a passiva "iluminar áreas escuras" entra nesta leva (o sistema de áreas escuras não existe no overworld ainda) ou fica adiada; (3) o `power` do dano-base (nenhum outro AoE puro existia — Newton é AoE mas com `power=0`, o dano dele é só o ATK do conjurador).

**Decisões (implementação, modo autônomo do orquestrador, líder ausente 2026-07-16):**
- **Nenhum `EffectKind` novo.** A ativa "Onda Unificada" reusa PURO o caminho `TargetShape::Grupo` já entregue pelo Newton (`resolve_targets`/`resolve_use_card`, addendum "Balde B, PR2"): qualquer carta `Grupo` já atinge todos os inimigos vivos do lado oposto, com a cadeia divisiva de sempre. Maxwell não precisa de `EffectSpec` nenhum — o dano-base (`card.power + actor.atk()`) É o efeito inteiro da carta. É a carta mais simples do catálogo até hoje (zero wiring, só dado).
- **`power = 5` (`kMaxwellPower`, `//PLAYTEST`, decisão AUTÔNOMA a confirmar):** um `power` igual ao dano-base do primário da Tesla/Coil-Arc (8) eclipsaria o nicho dela (burst concentrado decaindo por salto); 5 preserva a diferença de design entre as duas AoE-adjacentes (Tesla = burst único que decai por salto; Maxwell = dano uniforme mais fraco espalhado em TODOS os inimigos).
- **Modo-aliado é no-op PURO** (mesma regra geral "fogo amigo desligado" do Newton/Einstein/Faraday) — SEM nenhum benefício extra concedido (ao contrário do Newton, que ainda dá Reflect-status no modo-aliado): mirar um aliado com Maxwell literalmente não faz nada além do log de dissipação do guard de fogo amigo, porque não há `EffectSpec` pra rodar.
- **A passiva "iluminar áreas escuras" fica STUB posse-only, ZERO código de combate/exploração** — mesmo padrão do anti-PEM do Faraday/Euler/Menger/Turing/Dee(i). O sistema de áreas escuras no overworld não existe ainda; a feat **MAXWELL-AREAS-ESCURAS** (sub-brainstorm abaixo) continua pendente como trabalho de DESIGN do criador, não deste executor — quando existir, a query reusa o mesmo mecanismo `equipped_special_ids()` já usado pelas outras faces posse-only.
- **`CardCategory::Hibrida`:** mesma semântica já registrada pro Faraday (`cartas-technomagik.md` §2.3) — carta com duas FACES mecânicas (combate castável + face fora-de-combate irmã), não um efeito duplo dentro do MESMO combate.

Implementação: `master_cards.cpp::maxwell` (Hibrida/Eletrico/`TargetShape::Grupo`, `effects` vazio, `power=kMaxwellPower`) — NENHUMA mudança em `combat_state_machine.cpp`/`combat_enums.hpp`/`techmagic.cpp` (zero `EffectKind`, zero `StatusId`, zero wiring novo). Ver `docs/tech/adr/ADR-016-techmagic-effect-engine-data-driven.md` (addendum "manifesto item 12") e `GusEngine/domain/tests/techmagic_maxwell_test.cpp` (AoE em todos os inimigos vivos, paridade preview<->real, modo-aliado no-op).

## Sub-brainstorms / feats que emergiram (a fazer depois)
- **MAXWELL-AREAS-ESCURAS:** áreas escuras percorríveis só com a carta Maxwell (a "luz" dele); definir o que são, quantas, gate contornável (gdd §7.1).
- **VOLTA-LEECH-%:** a fração de conversão do leech do Volta (calor perdido, 2ª lei) + absorve % ou absoluto, com argumentos de termodinâmica.
- **CARTAS-FRASES-PEDAGOGICAS (criador 2026-07-12):** cada carta, ao ser usada, mostra uma **frasezinha didática** que desperta curiosidade sobre a figura/lei real (ex.: Volta = "Você recebeu X mana e Y HP; X% perdido pela 2ª lei da termodinâmica [texto simpático]"). Uma frase por mestre. É a função pedagógica do jogo. Handoff futuro (narrative-writer + criador). Cross-ref o molde de 2 movimentos do encontro (`techmagic.md`).
- **VFX-CARTAS (criador 2026-07-12):** brainstormar o efeito VISUAL de cada poder, do mais barato (sprite representando magia/eletricidade) ao efeito-de-tela via glintfx (existente ou a criar), pesando o CUSTO de criação (dev solo). 
  - **Pergunta pro dev do glintfx (Einstein):** o glintfx faz (ou pode fazer) um efeito de **onda/distorção varrendo a tela de um lado ao outro** (screen-space wave)? Mandar o prompt pro dev (não mexer na lib eu mesmo, [[feedback_glintfx_nao_mexer_so_pedir]]).
  - **VFX já definido:** Pythagoras = traça linha vertical + horizontal até o inimigo, poder sai pela hipotenusa (diagonal) — barato (linhas geométricas).
- **CARTAS-NOMES-EFEITOS (criador 2026-07-12):** brainstormar o NOME de cada poder/efeito, ligado ao contexto real da figura (ex.: Einstein = "Grav-Wave"). Uma alcunha por carta. Casa com CARTAS-FRASES-PEDAGOGICAS e o naming `cardExec-[figura]`.

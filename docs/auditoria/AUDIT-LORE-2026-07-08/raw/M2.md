# Relatório bruto — Fatia M2 (37 memórias restantes + C8 organização)

14 achados (3 CRÍTICOS, 9 IMPORTANTES, 2 COSMÉTICOS). Cobertura 37/37 (brief dizia 36; +1 = reference_glintfx_api criada depois). 29 verificadas sem achado.

## Memórias (ML-01 a ML-08)
- **ML-01 | CRÍTICO | (a) | `feedback_caminhos_assets_centralizados`** — diz header em `GusEngine/app/include/gus/app/asset_paths.hpp` ns `gus::app`; real é `GusEngine/core/include/gus/core/asset_paths.hpp` ns `gus::core` (movido pra fundação). Fix: atualizar caminho+namespace.
- **ML-02 | IMPORTANTE | (a) | `project_rmlui_ui_stack`** — diz RmlUi pin 6.2; real é 6.3 (SHA, ADR-010). Fix: 6.3.
- **ML-03 | COSMÉTICO | (a/d) | `reference_glintfx_api`** — cabeçalho diz v0.3.1; corpo já tem v0.6.0 (pin real). Fix: atualizar header.
- **ML-04+ML-05 | IMPORTANTE | (e) contradição | `reference_codeberg_lfs` × `reference_libs_vendorizadas`** — LFS diz `.glb` (Gus/Yakov) é "setup canônico vigente"; libs-vendorizadas diz que estão "sendo DELETADOS". Real: os 3 .glb existem intactos. As 2 memórias discordam. Fix: reconciliar (legado 3D ou deletar de fato).
- **ML-06 | IMPORTANTE | (a) | `project_agente_gameplay_engineer`** — caveat diz "agent ainda declara Godot"; o agent já é C++20/SDL3. Fix: remover sinalização.
- **ML-07 | IMPORTANTE | (a) | `project_qa_deploy_disciplina`** — diz "sem CONTRACT.md"; existe hoje. Fix: atualizar seção "Estado atual 2026-05-16".
- **ML-08 | IMPORTANTE | (c) escopo | `feedback_nao_automacao_gui_ambiente_medico`** — auto-declara escopo GLOBAL/máquina mas vive só no GusWorld; não está no índice global. Fix: promover pra `~/.claude/memory/` global.
- **29 memórias verificadas SEM achado** (incl. project_agents_model_opus OK, 4 DORMENTE com banner claro, i18n_canonico OK, easter-eggs-manual _secret existe).

## C8 Organização
- **ML-09 | IMPORTANTE | C8.1** — CLAUDE.md marca pillars/gdd/architecture/engine-modules/build "AGUARDA REVISÃO USER"; real: pillars=Canônico, gdd=v0.2, os 3 tech=SUPERADO. Fix: atualizar status.
- **ML-10 | CRÍTICO | C8.2** — "Estrutura de repositório" do CLAUDE.md descreve `engine/`(Godot)+`game/`(Godot)+`assets/{models,textures,...}`+`build/` na raiz; real é `GusEngine/`(C++ core/domain/platform/app)+`resources/`; engine/game são legado Godot dormente. Mapa de onboarding aponta pros lugares errados. Fix: reescrever a árvore.
- **ML-11 | COSMÉTICO** — árvore characters/ do CLAUDE.md lista 10; disco tem 13 (falta brunus-vetorial, -conto, prelore_vilao).
- **ML-12 | CRÍTICO | C8.2** — `deep/_INDEX.md` sistemicamente divergente: era-2-compilador→boom-tecnico, cult-mirage-reality→cult-mirage, magic/*→*-deep, stinger/*→*-deep, antologia ANT-NNN→NN-slug + sem _PLANO.md, **pasta `antagonists/` inteira (3 docs) invisível ao índice** (índice atribui Sterling/Patch-Zero/npcs a characters/). Só settings/ bate 100%. Fix: regravar do find real + revisar tabela "Status rodadas R2-R10 pendente" (há muito mais escrito).
- **ML-13 | IMPORTANTE | C8.3** — 3 camadas de "characters" (characters/ raso, deep/characters/, deep/antagonists/) sem índice explicando a fronteira; `npcs-antologia.md` colide semanticamente com pasta `antologia/`. Fix: nota no _INDEX + considerar renomear npcs-antologia→npcs-antagonistas.
- **C8.4 INCOHERENCES.md** verificado SEM achado (C1-C19 todos RESOLVIDO com data; nenhum aberto sem dono). Fica em `docs/narrative/`.

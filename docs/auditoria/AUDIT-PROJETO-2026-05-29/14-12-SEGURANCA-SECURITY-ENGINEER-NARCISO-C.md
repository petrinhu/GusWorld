# Cap. 14 — 12 Seguranca (security-engineer / Narciso CISO)

Saudavel. Postura adequada ao porte (offline puro, sem PII, sem rede, sem telemetria, sem exec) e nenhum problema critico. Integridade de save via HMAC-SHA256 correta e fail-secure; threat model honesto (EmbeddedKey e anti-cheat e obfuscacao, nao secret real). Supply-chain com zero deps NuGet de runtime; submodule pinado; scan no working tree mais history (1911 objetos) sem nenhum secret real (gitleaks instalado esta quebrado, e um stub Not Found). IMPORTANTE: cobertura de teste do HMAC fragil (unico controle de seguranca real com so smoke headless; SaveSerializerTests no harness POCO); sem gates de seguranca no CI (gitleaks real mais dotnet list package vulnerable mais pre-commit). COSMETICO: claim source-gen incorreto (quebra AOT sob trim), HMAC compare nao-constant-time (irrelevante offline), log vaza hashes (nao a chave, impacto nulo).
